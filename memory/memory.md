# 🧠 Project Memory Bank

**Version:** 0.49.1
**Last Updated:** 2026-08-17 15:10

> **INSTRUCTIONS FOR AI:**
>
> 1. DO NOT remove these instructions.
> 2. This file is the "Source of Truth" for the project state.
> 3. Be EXTREMELY concise. No prose. No "I hope this helps".
> 4. When updating: Bump Version, update Date, and add entry to Changelog.

---

## 1. Core Architecture (Immutable)

- **Tech Stack:** Python, SimPy, PyTorch/PyG, JSONL (`placements/placements.jsonl`)
- **Key Patterns:** 
- **Hard Constraints:**
  - **`placements/placements.jsonl` required** per co-sim `ds_*` (full placement–RTT sweep). Not optional. `--repair`+recache ≠ substitute. `memory/placements_jsonl_required.md`

## 2. Active Context (Mutable)

- **Current Sprint Goal:** **THE ENVIRONMENT WAS THE PROBLEM: the co-sim target is pointwise-separable, so the MLP was the *correctly specified* model class and no training could have made the GNN win.** Measured by new **M4** block in `separability_diagnostic.py` (fits `rtt(plan) ≈ μ + Σ_t f_t(plan[t])` over every sweep plan): additive R² **0.988** (contention_v2) → **0.9997** (v4/v5) → **1.0000** (highq_safe); additive argmin *is* the true optimum in 81–100% of datasets. **contention_v4/v5 moved the wrong way.** Root cause confirmed on all **899** v2 sweeps: queue depth *predicts* separability monotonically (shallowest quartile R² 0.978/+1.986pp collision gain → deepest 0.998/+0.181pp, **11× weaker**), because the additive term is `depth × exec_time` and grows with depth while the interaction `added_in_batch × exec_time` does not. **Fix = invert the lever:** new `shallow_v1` grid (depths ~0–8) gives coupled(>1%) **31.0% vs 7.1%**, coupled(>5%) **19.6% vs 2.4%**, pointwise worst-case regret **48.4% vs 3.6%**. ⚠ **Ablation on it is INVALID — 24/200 cache labels ≠ sweep optimum (mean regret 15.78%, max 92.5%).** Relabel before anything downstream.
- **`mp_parity` CLOSED 0.49.0 — both arms falsified, gate FAILED, and the failure mode matches the separability finding above.** Baseline 15-cell gate `gnn_mp_parity_gate_20260816/` (parity-fixed deployed ckpt): **PRIMARY 1/3, TAIL 1/3 — both pre-registered criteria FAILED**; `sparse_p25` 1.14× Kn / MLP 0.83×, `sparse_p35` 1.02× / 0.77×, **`sparse_p25_skew` GNN 0.84× / MLP 2.27×** (GNN 3/5 cells, p99 **71.0s vs MLP 498.5s**) ⇒ the pre-registered *collision cliff* reproduces, but on skew not `sparse_p35`; bounded robustness claim only. SUM GNN 1.05× / MLP 0.93×. **Arm B (candidate-restricted same-node edges) FALSIFIED** — trained *with* the edges and still worse than Arm A on every metric (val 62.6% vs 65.6%, test greedy regret 0.4944s vs 0.2621s). **Arm A (gated GIN residual) FALSIFIED LIVE — 15/15 complete** (`mp_residual_gate_20260817/`, `compare.json`): offline greedy regret **0.5682→0.2621s (−54%)**, `mp_gate` **1.08**, yet gate verdict **unchanged — PRIMARY 1/3, TAIL 1/3 FAILED**, paired wins identical to baseline (GNN 3/15 · MLP 10/15 · Kn 2/15), SUM 1.05→**1.12×**. **Effect splits by coupling:** `sparse_p25` +3.5%, `sparse_p35` +9.9% (both **0/5** vs MLP), but **`sparse_p25_skew` −4.3% (0.84→0.80×), p99 71.0→63.1s** ⇒ the residual pays only where interaction exists and costs RTT where the target is additive.
- **Superseded sprint goal (0.47.0):** train/serve message-passing mismatch, NOT the decode. `train_near_rtt.py` fits `self.gin(x, data.edge_index)` (bipartite only, never reads `node_edge_index`); `src/policy/gnn/gnn_model.py` concatenated **all** same-node platform↔platform edges — **~1428 vs ~57 = 25:1 flood** of edges the weights never saw. **87.5%** of argmax decisions flip on the training cache. Live `sparse_p35`/s42, one env var, no retrain: **276.0M (10.93× Kn) → 22.34M (0.88× Kn)**, `chosen_queue_vs_min` **4,007 → 98** (beats MLP's 142). Fixed: same-node edges now opt-in via `GNN_MP_NODE_EDGES` (default off = parity). 15-cell gate running. **GNN still loses to MLP (0.88× vs 0.78×)** ⇒ next = residual + candidate-restricted edges + retrain.
- **Hard stop:** **cite offline regret/acc as evidence a model will place better live** (Arm A: −54% offline regret → +3.5%/+9.9% *worse* live, `logit_tied_rate` rose) · **cite the 0.88× Kn `sparse_p35`/s42 figure** (irreproducible: current code gives **1.04×** on the same cell/seed/model; the arm that produced it set `GNN_DROP_NODE_EDGES=1`, implemented **nowhere** in the tree today) · **revive same-node edges without new physics** (Arm B falsified *while trained with them*) · RQ3 · **RQ3b** · **claim the GNN's live failure is "structural in the joint decode"** (falsified: LQB λ=1.5 no change 275.7M; `argmax_uniq` drove collisions to **0.000** and got *worse* 301.3M) · **cite ECT as a ceiling or distill teacher for Regime A** (0.98–1.13× Kn, and `contention_v2` is pull-free so `ect_pull`≡`ect`) · **run `ect_pull` with `ECT_PULL_DISTILL_DIR` on workload-125-225** (561,848 frames/cell ≈ 50GB; fills disk) · cite pre-`25732cf` Regime A tables as current · mix pre/post-serialization in one table · re-eval 873/v5.5 as if it transfers to serialized FilterStore · hub9 decode · claim pull-obs/cosim-retrain/`soft_combo_conc`/hard-CE-distill closed 125→31 · blind retrain scarce-warm 450 · reopen hub9/`total_rtt` primary · HeteroData/Set-Transformer · claim warm/busy v1 init helps FilterStore distill
- **GNN scope:** Deploy still 873/v5.5. INTEL keepers: CE dim16 argmax **~94–125s** (nondet pile depth); Phase 3 multiseed soft+hard distill `…-ect-pull-distill-multiseed.pt` (`f98f43f5…`) + `seq_reforward_pull` **31.66s**. Single-stub distill 62.63s superseded. Hard-CE-only / warm-poisoned multiseed → discard.
- **Why radical:** N=4 labels insufficient; live ect_pull + soft KL + **cold multi-infra/jitter corpus (6k)** teaches marginal FilterStore ordinal; warm/busy under `platform_reuse_v1` skips pull → poisons distill (375s).
- **Active Tasks (Max 3):**
  1. **[BLOCKING ALL DOWNSTREAM]** **Relabel `shallow_v1` from `placements.jsonl` minima, rebuild cache, re-run ablation.** 24/200 (12%) cache labels ≠ sweep optimum, mean regret **15.78%**, max **92.5%**. This is `gnn_necessity_separability.md` §1's open item ("repair graph labels to use sweep-min plans") reproduced on a fresh corpus — still unfixed since 2026-08-04, and it silently invalidated the 2026-08-17 ablation.
  2. **[IN FLIGHT]** Score `mp_residual_gate_20260817/` when 15/15 (10/15 now; only `sparse_p25_skew` left — the one config where the GNN beat both baselines), then flip the `mp_parity` LINEAGES row from "live re-gate running" to the Arm A verdict. Verdict is already determined (PRIMARY 0/2); skew decides only whether the bounded collision-robustness claim survives the residual.
  3. **[NEXT]** **Second gate: label provenance.** The M4/coupled-fraction gate proves a *corpus* has learnable structure; nothing checks that the labels handed to the model are the ones the corpus implies. Add a cache-vs-sweep label check to `prepare_graphs_cache.py` or the diagnostic, fail loud on mismatch. (Parked: `shallow_longexec_v1` amplifier — needs `workload_nofs-{cnn,rf}` in `combinations_simple_mapping.pkl`.)
- **Live scripts:** `harvest_ect_pull_distill_corpus.py` (multiseed), `build_regime_b_live_stub.py` (jitter/warm/busy), `train_ect_pull_distill.py`, `run_phase3_ect_pull_distill_eval.py`, …
- **Artifacts:**
  - Ablation ladder `gnn_encoder_ablation_20260816/` (seed 42; `manifest.json` + `compare.json`; ECT 3 cells + 6 GNN rungs on `sparse_p35`); runner `scripts_cosim/important/run_gnn_encoder_ablation.sh`, scorer `compare_encoder_ablation.py`
  - MP-parity gate `gnn_mp_parity_gate_20260816/` — **COMPLETE 15/15**, `compare.json` + `manifest.json`; the **baseline of record** for the residual arms (retires the 0.88× ablation figure)
  - Arm A/B re-gate `mp_residual_gate_20260817/` (15 GNN cells, 30 Kn+MLP symlinked to *resolved* targets so no symlink chain; `ML_PARALLEL=5`, `OMP_NUM_THREADS=6` held equal to baseline for FP comparability)
  - Arm ckpts `models/near-rtt-v2-full-corpus-siv1-dim14-ce-only-mp-residual{,-node-edges}.pt` + `.contract.json` (datalab jobs **693621/693622**, A100, wandb `gnn-full-corpus-siv1-aug2026`)
  - Runner `scripts_cosim/datalab/{run_mp_arm_gnn_train.sh,mp_arm_gnn_train.sbatch}` (`ARM=residual|residual_node_edges`), configs `experiments/full_corpus_siv1_gnn_mp_residual{,_node_edges}.yaml`
  - Regression guard `scripts_cosim/test_train_serve_mp_parity.py` (**9** tests: default ignores `node_edge_index`; opt-in flag still changes output; candidate restriction keeps only reachable pairs and survives zero-candidate graphs; `mp_gate` present iff residual and refuses cross-loading)
  - Full-corpus sweep `full_corpus_siv1_coupled_trio_20260815/` (45 cells; datalab **685484**; Knative arm symlinked from the v5.5 trio) + cache `graphs_cache_full_corpus_siv1_dim14` (2,651 graphs) + wandb `gnn-full-corpus-siv1-aug2026` (GNN `v06uk8gb`, MLP `5k7phldu`)
  - RQ3b sweep `contention_v2_873_v5.5_coupled_trio_20260813/` (Kn local 15 + ML datalab 30) — `compare.json` + `manifest.json` + `configs/` complete
  - Re-baseline `contention_v2_873_v5.5_sealed_holdout_rebaseline_20260813/` (datalab 60) — same, `manifest.json` marks `incomparable_with` 20260806
  - Datalab jobs **663480** (trio MLP+GNN) / **663482** (re-baseline) CPU-amd; launchers `scripts_cosim/datalab/{live_cpu_amd.sbatch,submit_live_cpu_amd.sh,transfer_and_submit_rq3_cpu_amd.sh}`
  - PR #3 **merged to main** `040fdcb` (Phase 3.1 `fb4e729`)
- **Pending Decisions:**
  - [Q: after MP-parity gate — residual+candidate-restricted retrain locally or datalab? | Status: **CLOSED** → datalab, both arms concurrent on one A100, ~50min each; local training also blocks the live gate for CPU]
  - [Q: add `git_commit` + `OMP_NUM_THREADS` to `run_provenance`? | Status: **OPEN**, and it already cost one irreproducible headline number]
  - [Q: pipenv lock nvidia-nccl | Status: Pending]
- **Closed:** RQ3 · **RQ3b** · RQ9 Regime B · pull-obs alone · absolute cold×T_pull as *primary* cost · N=4 CE · Phase 1 pull-ledger alone · Phase 2 soft_combo_conc · Set Transformer · **hard-CE-only distill** · **Regime B 125→31 on oracle_split_v1 via ect_pull distill** · warm/busy v1 distill mix · Phase 3.1 commit+merge · **corpus growth as a GNN fix** · **decode guards as the GNN lever** (LQB / `argmax_uniq` both falsified) · **ECT as Regime A ceiling or teacher** · **`mp_parity` (both arms)** — same-node edges falsified while *trained with*, GIN residual falsified live despite −54% offline regret

## 3. Knowledge Graph (Learned Lessons)

- **⛔ OFFLINE REGRET IS NOT A LIVE PREDICTOR, AND THE RESIDUAL'S SIGN FLIPS WITH COUPLING (2026-08-17, 15/15 final):** Arm A (gated GIN residual, `x = x0 + mp_gate·gin(x0, ei)`) cut offline test greedy regret **0.5682 → 0.2621s (−54%)**, top-5 0.0346 → 0.0239, `mp_gate` learned **1.08** (>init ⇒ it leans on MP *more*). Live it **split by config**: `sparse_p25` 1.14→**1.18×** Kn (+3.5%), `sparse_p35` 1.02→**1.12×** (+9.9%), but **`sparse_p25_skew` 0.84→0.80× (−4.3%), p99 71.0→63.1s**. Gate verdict unchanged: **PRIMARY 1/3, TAIL 1/3 FAILED**; paired wins identical to baseline (GNN 3/15 · MLP 10/15 · Kn 2/15); SUM 1.05→1.12×. ⇒ **A −54% offline gain bought nothing overall and cost RTT on the two near-pointwise configs, while paying only on the one config with real coupling** — exactly the separability entry's prediction (extra capacity buys ~1pp of interaction variance and blurs the ~99pp pointwise signal). **⚠ Correction to the mid-session read: `logit_tied_rate` is NOT the discriminator** — it rose on *all three* configs (0.5097→0.5542, 0.5374→0.5807, 0.5338→0.5511) including the one that improved. The sign of the effect tracks **the config's coupling**, not the tie rate. ⇒ **Never promote a checkpoint on offline regret/acc alone, and never gate a graph-capacity change on a pointwise-separable config** — measure per-config with M1 coupled-fraction alongside.
- **⛔ A RESULT WHOSE CODE NO LONGER EXISTS IS NOT A RESULT (2026-08-17):** The 0.47.0 headline "0.88× Kn on `sparse_p35`/s42" is **not reproducible** — the full gate gives **1.04×** for the identical cell/seed/model/config/decoder. Proof the code changed, without the old source: that arm ran `GNN_DROP_NODE_EDGES=1`, a variable implemented **nowhere** in the tree today; had it been a no-op then, the arm would have been the unfixed baseline (~276M), but it measured 22.34M ⇒ the flag worked then and the file was rewritten after (`GNN_DROP_NODE_EDGES` → opt-in `GNN_MP_NODE_EDGES`), with only the final state committed in `2a591ed`. **Root enabler: `run_provenance` records neither the git commit nor `OMP_NUM_THREADS`/`MKL_NUM_THREADS`** (ablation ran 4 threads, gate 6) — and with `logit_tied_rate ≈ 0.54`, FP reduction-order differences can flip argmax on half the decisions. **Add both fields to provenance; until then treat any cross-sweep GNN comparison as suspect and re-measure under one runner.**
- **⛔ ONE MODEL DEFINITION, AND MAKE ARCH FLAGS SELF-DESCRIBING (2026-08-17):** `train_near_rtt.py` no longer declares its own `TaskPlacementGNN` — it **imports** the canonical one from `src/policy/gnn/gnn_model.py` (−84 lines of duplicate class; training behaviour preserved via new ctor args `dropout`/`post_gin_dropout`/`normalize_platform_inputs`). Two copies is what caused the 25:1 flood; a second latent drift was already queued behind it (the trainer had a `platform_input_norm` LayerNorm the server lacked — absent from the deployed ckpt, so `strict=True` never fired, but one training run from silently serving wrong). **Pattern that prevents recurrence:** the residual carries a learnable scalar `mp_gate`, so it appears in the state_dict and a residual ckpt **cannot** strict-load into a non-residual model (or vice versa) — silent architecture drift becomes a loud `RuntimeError`; what weights can't encode (`mp_node_edges`) goes in the existing `<model>.contract.json` sidecar, which `load_gnn_model` reads back, and a **stale `GNN_MP_NODE_EDGES` that disagrees with the sidecar now fails loud** instead of being ignored. Candidate-restricted same-node edges measured on the full corpus: **1428 → 20.6 mean edges (25.9× → 0.37× bipartite)**, 80% of graphs retain ≥1 pair, 20% correctly degenerate to bipartite-only.
- **⛔ DATALAB: TRAINING PORTS, SERVING DOES NOT — and never `rm -rf` the repo dir (2026-08-17):** Remote `gnn-herosim` sat on divergent history where `constants.py` lives in **`src/motivational/`**, local has **`src/placement/`** ⇒ `train_near_rtt.py` runs on both (its imports happen to be satisfied) but `executesimulation.py` `ImportError`s ⇒ **the live gate cannot be moved to datalab without a real branch sync.** It was also still serving the **pre-fix** `gnn_model.py` (unconditional `node_edge_index` concat = the 12.4× regression) because earlier syncs were file-by-file rsync — **code must travel by git, only data by rsync.** **Safe re-sync tactic (data survives):** `git fetch` → `git checkout --force -B <branch> origin/<branch>` → `git clean -fd`; `clean` skips **ignored** files, so all 65G of `simulation_data/` + 7.3G `logs/` + gitignored ckpts survive. A clone-and-move-data-dirs plan would have silently dropped `src/notebooks/models/*.pt`, `src/notebooks/logs/`, and 673M of wandb runs under `src/legacy/` — **gitignored data is scattered inside the source tree, not confined to data dirs.** Verify by diffing `git ls-files` against a throwaway clone. ⚠ **`--partition=GPU-rtx6000` is unusable**: the `gnn` env's torch 2.5.1+cu121 has no kernel image for those cards; both arms died at weight init. Use `GPU-a40,GPU-a100s,GPU-l40s,GPU-a100` (the existing `full_corpus` sbatch omitted rtx6000 deliberately).
- **⚠ the pre-registered gate's own criteria are the bar, and a losing config can still be the interesting one (2026-08-17):** MP-parity gate: **PRIMARY 1/3, TAIL 1/3 — FAILED**, yet the single config the GNN wins (`sparse_p25_skew`: GNN **0.84×** Kn / MLP **2.27×**, p99 **71.0s vs 498.5s**) is where the pre-registered *collision cliff* actually reproduces — on skew, not the `sparse_p35` where the n=1 probe saw it. Aggregate `SUM` (GNN 1.05× / MLP 0.93×) **hides** this because it is dominated by the two large-RTT configs. ⇒ report per-config wins **and** the declared criteria, never the SUM alone; the honest claim is *bounded collision-robustness on skewed queues*, not a general win. **Process note: cells were twice dismissed as "record completion" and twice held the only GNN win — score every cell before concluding.**

- **⛔ THE CO-SIM TARGET IS POINTWISE-SEPARABLE — the MLP was correctly specified all along (2026-08-17):** New **M4** block in `scripts_cosim/separability_diagnostic.py` fits `rtt(plan) ≈ μ + Σ_t f_t(plan[t])` by least squares over every plan in `placements.jsonl` — exactly what `PointwiseEdgeMLP` can express. Additive R²: `contention_v2` **0.98812** (additive argmin = true optimum in **81%** of ds), `contention_v4_pilot` **0.99973** (**100%**), `contention_v5_quick_test` **0.99973** (97%), `highq_safe` **1.00000** (**100%**). ⇒ **No amount of data or architecture work could have made a GNN win.** Reframes the whole `mp_parity` line: the GNN didn't lose to a bug, it lost because nothing non-pointwise was left to learn. **The entire interaction is one integer** — adding a collision-count column takes v2 0.98812→0.99912 (+1.10pp); on v4/v5 it is +0.03pp. Mechanism (`src/placement/scheduling_cost.py:108-132`): every term of `current_work + queue_work + cold_start + exec_time + comm_time + network` depends on `(task, platform)` alone **except `added_in_batch`**. Network latency is a static table lookup paid once per task (`infrastructure.py:985-993`), never congestible. **Co-located platforms share NOTHING** — the only `capacity` in `infrastructure.py` is disk cache; `memoryRequirements` is never enforced. Measured RTT split on a real optimum: queue **1.330s (~95%)**, network 0.0719s (5.1%), exec 0.0239s, cold-start and pull **exactly 0**. ⚠ **Trap: platform ids are globally unique, so `platform_only_r2 == additive_r2` by construction** — the node/platform split says *where* the separable signal sits, NOT whether topology matters; use the network share of RTT for that.
- **⛔ DEEP QUEUES ARE THE WRONG LEVER — confirmed on all 899 `contention_v2` sweeps (2026-08-17):** Queue depth **predicts** separability, monotonically: shallowest quartile (depth 27.6) → additive R² **0.97822**, collision gain **+1.986pp**; middle (35.5) → 0.99211/+0.672pp; deepest (50.8) → **0.99803/+0.181pp**. `corr(depth, additive_r2) = +0.256`, `corr(depth, collision_gain) = −0.259`. **Coupling is 11× weaker in the deepest quartile.** Arithmetic reason: additive term `depth × exec_time` grows with depth; interaction `added_in_batch × exec_time` does **not** ⇒ depth dilutes the only coupling there is. This is why `contention_v4/v5` landed at 0.9997 — **the contention series' core lever was backwards.** Inverse works: `shallow_v1` grid (depths ~0–8, stock dnn1/dnn2, otherwise v2 axes, 200 ds) → coupled(>1%) **31.0%** vs v2 **7.1%**, coupled(>5%) **19.6%** vs 2.4%, additive-argmin optimal **62%** vs 92%, pointwise worst-case regret **48.4%** vs 3.6%. **First corpus where the node-collision column outexplains the platform one.**
- **⚠ MEAN ADDITIVE R² IS THE WRONG GATE STATISTIC — the target is bimodal (2026-08-17):** `shallow_v1` median R² **0.99990** against mean **0.96074** — most datasets stay perfectly separable and *all* the structure sits in a large minority, so averaging hides exactly what matters. Also n-sensitive: the same corpus read 0.93838 at n=12 and 0.96074 at n=168 (would **fail** a 0.95 gate at full size while passing at pilot size). **Use the coupled fraction (M1 regret >1%) instead** — new `--gate-coupled-fraction`, which cleanly separates the arms (shallow **0.167 PASS** / baseline **0.000 FAIL**). Both gates live in `separability_diagnostic.py`; run on a ~30-ds pilot **before** generating a full corpus.
- **⛔ A SHARED RESOURCE ONLY COUPLES IF PLACED TASKS HOLD IT LONG ENOUGH TO OVERLAP (2026-08-17):** Built `node_contention_v3` — `Node.compute_slots` (`simpy.Resource`) that co-located platforms contend for, opt-in via `--compute-slots-per-node` / `config.nodes.compute_slots_per_node`, **default None = bit-identical `node_disk_v2`**. Matched 12-ds pilots: metrics disagreed and n was underpowered (additive R² 0.98847 vs 0.98584 wrong way; additive-argmin optimality 75% vs 92% right way) — **but `nodeContentionTime` was exactly 0.0 on EVERY placed task in every dataset.** Placed tasks never overlap: exec is ~0.024s and the seeded backlogs drain first, so slot contention only serialized the backlogs — a per-`(task, node)` **additive** term. Candidates that *would* satisfy the rule: memory held across the whole residency (cold start + exec, cold starts reach **38s**), or a slot held for a replica's warm lifetime. Guarded by `scripts_cosim/test_node_contention.py` (9 tests). **Non-obvious trap found building it:** queue depth is seeded as a compressed warmup backlog (`Platform.virtual_warmup_total_time`) drained in a **single** `env.timeout`, NOT as `queue.items` — wrapping only the per-task execution path left contention at exactly 0 while bypassing ~95% of RTT.
- **⛔ CACHE LABELS ≠ SWEEP OPTIMA — silently invalidates any ablation (2026-08-17):** `prepare_graphs_cache.py` labels graphs from `optimal_result.json` (`sample.placement_plan`), which is **not guaranteed** to be the `placements.jsonl` minimum. On fresh `shallow_v1`: **176/200 match, 24/200 (12%) do NOT**, mean regret of a wrong label **15.78%**, max **92.5%**, 0 absent. Symptom that exposed it: the ablation reported **0/30 coupled test datasets** (greedy baseline regret 0.00% mean/p90/max) against a corpus-level M1 coupled(>1%) of **31.0%** — the ablation is self-consistent *within* the corrupted label space but disconnected from the sweep. **This is `gnn_necessity_separability.md` §1's open item reproduced on a new corpus, unfixed since 2026-08-04.** ⇒ The 2026-08-17 `shallow_v1` ablation (`gnn_base` 91.7%/0.90% beating pointwise 90.0%/2.68%, and `gnn_node` re-falsified) is **NOT established** — both arms measured on contaminated labels. **Always verify label provenance before running or citing an ablation.**
- **⚠ `queue_feature_contract` in METADATA was a hardcoded guess (2026-08-17):** `extract_dataset_metadata.py` had `queue_contract = "legacy_v0"  # Default assumption` while `compute_compatibility_matrix.py` decides **what may be trained together** from that field. Now read from the collection's graph cache `metadata.json`. **The contract is NOT single-valued per collection** — `contention_v2` has both `graphs_cache_contention_v2_873_v5.5` (no field ⇒ legacy_v0) and `…_873_v5.7_siv1_dim14` (scale_invariant_v1); on conflict the extractor records `legacy_v0` and **warns**. A cache with no contract field predates it and *is* legacy_v0 — skipping such caches hides real conflicts. `shallow_v1` now sits alone in group `scale_invariant_v1_node_disk_v2_4task`, unmixable with the 15 `legacy_v0` collections.
- **⚠ new-corpus runbook gaps (2026-08-17):** (1) The generator does **not** emit `system_state_captured_unique.json`; `prepare_graphs_cache.py` hard-requires it and dies with `FileNotFoundError: Missing system_state_captured_unique.json for ds_00000`. Run `refresh_optimal_full_stats.py --base-dir <collection> --rewrite-ssc` first — same trap recorded for `contention_v2` in June. (2) New task types need a `workload_nofs-<type>` param in the sampled space (`combinations_simple_mapping.pkl`), not just `wsc`/`prewarm`/`replicas` (now auto-synthesized). (3) Two loud-failure fixes: `prepare_workloads` silently dropped apps missing from the sample mapping; `calculate_workload_stats` returned `average_rps` while `flatten_workloads` reads `stats['rps']`, so an empty workload died as a bare `KeyError: 'rps'`. (4) `contention_v5_quick_test` has **3/38 ds with no `placements/` at all** (violates the mandatory-JSONL rule); `highq_safe`'s sampled sweep can't satisfy M1's strictness — M1 needs relaxing before batch size >8.

- **⛔ TRAIN/SERVE MESSAGE-PASSING MISMATCH — the real cause of the GNN's 12× live failure (2026-08-16):** Three *different* architectures were in play. **Offline ablation** `scripts_cosim/gnn_necessity_ablation.py` (the RQ3 "GNN>MLP" evidence): GIN with a **residual** (`x = x0 + gin(x0, ei)`) **and candidate-restricted** same-node edges (`synth_node_edges(candidates_only=True)`) — its own comments say full same-node edges "flood the GIN and oversmooth". **Training** `train_near_rtt.py:270`: `self.gin(x, data.edge_index)` — bipartite only, **never references `node_edge_index`**, no residual. **Serving** `src/policy/gnn/gnn_model.py`: bipartite **+ ALL** same-node edges, no residual. Measured on `graphs_cache_contention_v2_873_v5.7_siv1_dim14`: **1428 same-node vs 57 bipartite = 25:1 flood**; on the deployed ckpt **87.5% of argmax decisions flip** (480 tasks / 420 flips) and the chosen platform's dim7 is 1.25× deeper. Co-located platforms get averaged, erasing dim7 — the one feature that separates them. Live `sparse_p35`/s42 (ladder `gnn_encoder_ablation_20260816/`, seed 42, full-corpus siv1 ckpt): control **276.0M / 10.93× Kn / qvsmin 4,007 / collide 0.411** → **drop same-node edges 22.34M / 0.88× Kn / p99 213 / qvsmin 98 / collide 0.335**, i.e. **beats Knative and ECT with no retrain**. No-MP is 22.44M (0.89×) ⇒ once the flood is gone, bipartite MP is ~neutral, so rung B is still a real GNN. **This is why "offline accuracy is decoupled from live placement" — offline was scored on a different graph than serving used.** Root cause of the drift: `train_near_rtt.py` runs training at import so it can't be imported as a library; the model class was copy-pasted and diverged. Fixed: `GNN_MP_NODE_EDGES` (default **off** = parity) + guard `scripts_cosim/test_train_serve_mp_parity.py`. **Any future GNN arch change must land in BOTH files.**
- **⛔ DECODE GUARDS ARE NOT THE GNN LEVER (2026-08-16):** Same ladder, same cell. `GNN_LQB_LAMBDA=1.5` → **275.7M (10.92×)**, i.e. *no change* vs control 276.0M — the λ·log1p(4000)≈12 penalty is swamped by corrupted logit margins (`logit_chosen_minus_minq` mean **+229.9**, `confident_worse_queue_rate` 0.95). `GNN_DECODE_MODE=argmax_uniq` drove `collision_batch_rate` to **exactly 0.000** (550,851 tasks enforced, 81 relaxed) and got **WORSE: 301.3M (11.93×)**, qvsmin 4,508. ⇒ **intra-batch collisions were a symptom, not the cause**; a decode guard cannot rescue an inverted ranking. `GNN_QUEUE_FILTER_MAX_DELTA=8` does reach 21.80M (0.86×) but only by overriding the model (qvsmin→4). Retires the 0.45.0/0.46.0 conclusion that the failure is "structural in the joint 4-task argmax".
- **⛔ ECT IS NOT A CEILING AND NOT A TEACHER FOR REGIME A (2026-08-16):** First-ever run of `--knative_network_ect` on the coupled trio (seed 42, `node_disk_v2`): `sparse_p35` **25.87M (1.02× Kn)**, `sparse_p25` **16.79M (0.98×)**, `sparse_p25_skew` **4.95M (1.13×)** — vs MLP 0.78×/0.84×/2.73×. A greedy that knows queue length, exec time, cold start, comm and network latency *exactly* performs at Knative level ⇒ better myopic cost estimation is not the lever, and there is **no ECT headroom to distill** (Regime B's 125→31 does not carry over: `contention_v2` has 0/900 datasets with any cold start or pull, so `expected_completion_with_filterstore_pull` degenerates to plain `ect`). **Do NOT infer "no headroom ⇒ pivot to scenario design"** — the MLP already beats both. ⚠ `ECT_PULL_DISTILL_DIR` dumps a PyG frame **per decision**: on `workload-125-225` that is 561,848 frames/cell (~5.1GB in 15min across 3 cells, ~50GB/cell projected) — keep harvesting opt-in (`HARVEST_DISTILL=1`).
- **`argmax_uniq` aborted on sparse cells (2026-08-16):** `uniq_platform_chosen_idx` raised when a task's only feasible platform was already taken (116 hits on `sparse_p35`), and the scheduler caught it and fell back to **shortest-queue** — silently substituting a Knative-like policy into what reads as a GNN arm. Now relaxes to plain argmax and records `uniq_platform.{enforced_tasks,relaxed_tasks,relaxed_rate}` in the decode sidecar. Also: an unrecognized `GNN_DECODE_MODE` used to fall through to plain argmax with no error — now validated against `KNOWN_DECODE_MODES` at scheduler construction.

- **⛔ CORPUS SIZE IS NOT THE GNN LEVER (2026-08-15/16):** 3.04× more training data (873 → **2,651** graphs; merged `contention_v2`/`v3`/`v4_pilot` + `1060_warmth_v2` + `sparse_warmth_v2` + `highq_safe`, `scale_invariant_v1`/dim14, cache `graphs_cache_full_corpus_siv1_dim14`) improved the GNN **offline** and changed **nothing live**. Offline test acc **55.7% → 65.3%** (val 62.6→66.3, task_acc 89.4%) — a real gain, well outside the ±4.2 seed noise in the entry below. Live 45-cell trio (`full_corpus_siv1_coupled_trio_20260815/`, datalab **685484**): **GNN 0/15, SUM 12.40× Kn** (was 15.07× at 873) — marginal, still catastrophic. **MLP 10/15, SUM 0.93× Kn.** Decisive: `chosen_queue_vs_min` **mean 3,329** across all 15 GNN cells (median chosen ~1,100 vs min-available ~0–50), collisions 0.31–0.48 — i.e. **queue-blindness is unchanged despite the offline gain**. ⇒ **Offline accuracy and live placement quality are decoupled for the joint decode; stop generating/merging corpora as a GNN fix.** Both ckpts served under a **verified-identical** `scale_invariant_v1` contract (grepped from cell logs), so this is **not** a train/serve mismatch. wandb `gnn-full-corpus-siv1-aug2026` (GNN `v06uk8gb`, MLP `5k7phldu`).
- **⚠ datalab repo has divergent history + a regressed `BASE_DIR` (2026-08-15):** Remote `gnn-herosim` sits on its own older history (`a9c217a` mlp), so a file can differ from local **even when local `git status` is clean** — transitively-imported modules must be synced too (hit `CACHE_VERSION` and `split_by_parent_three_way` ImportErrors before a full `src/{policy,placement,notebooks}/` rsync fixed it). The KG's known `run_simulation.py` `BASE_DIR` gotcha (see entry below) had **regressed on datalab** to hardcoded `/root/projects/my-herosim` — killed all 30 cells instantly with `PermissionError`. Fixed by rsync; **re-check after any remote-side edit**.
- **queue feature contracts (2026-08-13):** `src/placement/queue_features.py` is now the single source for platform dim7 divisor + dim13 usage_ratio (previously duplicated in `prepare_graphs_cache.py`, `feature_builder.py`, `seq_decode.py`, `gnn_snapshot_inference.py`). Two named contracts: **`legacy_v0`** (default; historical formulas, verified numerically identical to the deployed 873/v5.5 cache across `task_features`/`platform_features`/`y`/`edge_attr`/`edge_index`) and **`scale_invariant_v1`** (dim7 p90 divisor **uncapped** + busy-platform fallback when ≥90% idle; dim13 `log1p(q/tc)/log1p(5)` so 5× target concurrency reads 1.0). On `contention_v2` v1 leaves **dim7 bit-identical** (cap binds 0/873) and only compresses dim13 (max 3.16→1.57) ⇒ the dim7 half is a pure *inference* repair needing no retrain. Provenance: `CACHE_VERSION` **5.7** + `metadata.queue_feature_contract`; MLP ckpt dict field; GNN gets a `<model>.contract.json` sidecar (bare state_dict has nowhere else); `load_gnn_model` + `MLPBatchScheduler` adopt it and **fail loud** via `QueueFeatureContractMismatchError` if `QUEUE_FEATURE_CONTRACT` disagrees; recorded in `run_provenance`. New `--platform-feature-dim {14,16}` on the cache builder emits dim22 (14) or dim24 (16) so a retrain can be compared to a dim22 checkpoint. Tests `scripts_cosim/test_queue_features.py` (28).
- **⚠ golden feature-parity test was blind to queue features (2026-08-13):** `verify_cache_live_feature_parity.py`'s default fixture (`regime_b_cold_burst_v1_oracle_split_cosim/ds_00003`) has **8 queue keys all at 0**, so dim7/dim13 were 0 on both sides and any cache↔live divergence in them passed. Added `--queue-depth-scale` (synthesizes a 0/1/7/23/61/150 depth spread ×scale) — at ×400 legacy dim7 reads **28–600** and dim13 **up to 570** vs v1's 0.007–1.0 and 1.7–4.4. Also surfaced a **pre-existing, unrelated asymmetry**: the cache decides dims 9–11 estimation per *snapshot* (`if temporal_state:`) while live decides per *platform* (queue>0 and no recorded remainder), so a queued platform with a zero remainder diverges (~0.08). Real snapshots pair depth with a running task, so the fixture now synthesizes temporal state alongside depth; **the asymmetry itself is still open**. 6/6 layout×contract parity combos PASS.
- **🏆 siv1 5-SEED GATE: MLP BEATS KNATIVE 13/15 CELLS + p99 3/3 (2026-08-13, datalab job 664414):** The seed-42 smoke replicates across seeds 42–46. `contention_v2_873_v5.7_siv1_coupled_trio_20260813/` (30 ML cells on CPU-amd; Knative arm **symlinked** from the v5.5 trio sweep — policy-independent, identical configs/seeds/workload/`node_disk_v2`), `manifest.json` + `compare.json` written. **Seed-averaged `mlp/kn`: `sparse_p35` 0.74× (5/5 cells), `sparse_p25` 0.79× (4/5), `sparse_p25_skew` 0.90× (4/5); SUM 0.775×, paired wins MLP 13/15 · Kn 2/15 · GNN 0/15.** **Tail: MLP wins p99 3/3** (108.4 vs Kn 183.4 · 102.8 vs 130.0 · 144.9 vs 254.1) and p90 2/3 (loses skew 22.4 vs 21.6). `sparse_p35` is the tightest cell (0.71–0.80× every seed). The 2 losses are `sparse_p25` s45 (1.07×) and `sparse_p25_skew` s43 (**2.14×**, the entire ±2.17M MLP std in that config) ⇒ an occasional tail blow-up survives; claim "beats Knative on average + on p99", **not** "uniformly". **GNN siv1 remains catastrophic: 0/15, SUM 15.07× Knative** (p90/p99 ~10× worse).
- **⛔ GNN's live collapse is NOT the queue features and NOT model quality (2026-08-13):** Ran init-seed variance with the canonical-parent split held fixed (new `NEAR_RTT_TRAIN_SEED`, default 42; the split's own `random_state=42` untouched), n=3 per contract. **Test acc: `legacy_v0` 55.5% ±4.2 (52.7/53.4/60.3) vs `scale_invariant_v1` 53.9% ±2.5 (51.1/55.0/55.7); ranges overlap, greedy regret 0.880s vs 0.980s.** So the deployed v5.5 GNN's 60.3% was the **top of its own seed distribution**, and the siv1 GNN's "regression" to 55.7% is **within seed noise** — the features neither help nor hurt the GNN offline. Since siv1 fixed the pointwise MLP decisively (0.78× Kn) but left the GNN 15× worse, the GNN's failure is **structural in its joint decode**, not an input-scaling or accuracy problem: it is argmax over 4 tasks per batch and its `intra_batch_platform_collisions` *rose* 0.179→0.236 while `chosen_queue_vs_min` median went 734→3676. Next GNN lever must target the decode/coupling, not features or more offline accuracy. Checkpoints: `models/near-rtt-v2-cv2-873-{siv1,legacy}-dim14-ce-only-seed{43,44}.pt`.
- **🎯 siv1 LIVE (seed-42 smoke, superseded by the 5-seed gate above):** First post-`25732cf` win for an ML policy. `…_v5.7_siv1_coupled_trio_smoke_20260813/`, Kn s42 symlinked from the v5.5 sweep, `num_tasks` **561848 identical** in all runs (win is not dropped work). **MLP siv1 vs Knative:** rtt `0.747× / 0.748× / 0.747×` (p25 12.86M vs 17.21M · p35 18.89M vs 25.25M · skew 3.26M vs 4.36M), p99 `0.40× / 0.60× / 0.43×`, p90 lower 3/3, avgQueue `22.8/33.6/5.8` vs Kn `30.6/44.9/7.7`; **loses p50** on p25/p35 (2.83 vs 0.70 · 4.64 vs 0.73). vs MLP v5.5: rtt **0.16–0.29×**, p99 **0.05–0.07×**. Not a degenerate shortest-queue: `chosen_queue_vs_min` **median rose** 7→20 / 9→36 / 0→0 while the **tail collapsed** p95 3911→522 / 12668→980 / 1356→259 — i.e. the model still deviates deliberately but no longer saturates. **GNN siv1 got WORSE than GNN v5.5** on all 3 (rtt 236.9M vs 118.0M · 332.7M vs 282.9M · 77.2M vs 49.4M; qvsmin median 492→1909 / 734→3676 / 210→1554) and remains 14–19× Knative. ⚠ **Confounded**: siv1 features *and* fresh weights (test acc 60.3→55.7, greedy_regret 0.848→0.911s); not isolated. Pointwise-vs-joint asymmetry is the leading hypothesis (scale-free dim7 directly fixes per-edge ranking; the GNN's joint argmax over 4 tasks also raised `intra_batch_platform_collisions` 0.179→0.236 on p35). Seed 42 only — **not** the 5-seed pre-registered gate.
- **⚠ two contract-plumbing defects found by the smoke, both fixed (2026-08-13):** (1) **provenance lied for `mlp_batch`**: `build_run_provenance` runs before `MLPBatchScheduler.set_models` adopts the ckpt contract, so the MLP results record `legacy_v0` although inference served `scale_invariant_v1` (proved by re-running the adopt block: trained `siv1`, declared `''` ⇒ resolved `siv1`; the runner exports no `QUEUE_FEATURE_CONTRACT`). Fixed via `apply_mlp_checkpoint_queue_feature_contract()` called **before** provenance; GNN was already correct (`load_gnn_model` → sidecar, recorded `siv1` in both `env` and resolved). **Older MLP result JSONs' `queue_feature_contract` field is unreliable — trust the ckpt key.** (2) `train_near_rtt.py` `_refresh_queue_dependent_platform_features` / `_queue_norm_from_values` still hardcoded legacy cap-100 + `ratio/5.0`, bypassing `queue_features.py`; now delegated. **Did not affect the retrain**: that path runs only when `PHASE_B_CHECKPOINT_METRIC == "seq_reforward_regret"`, which requires `TRAIN_INIT_CHECKPOINT` and non-CE-only (log shows `seq_reforward=0.0000s`, 21×).
- **✅ scale_invariant_v1 shipped + retrained, offline cost ≈ 0 (2026-08-13):** Fix for the OOD entry below. **Single source of truth** `src/placement/queue_features.py` (`queue_depth_norm` / `usage_ratio_feature`) replaces the duplicated formulas in `prepare_graphs_cache.py`, `feature_builder.py`, `seq_decode.py`, `gnn_snapshot_inference.py`. Two **versioned contracts**: `legacy_v0` (bit-exact history, cap 100 + `ratio/5`) and `scale_invariant_v1` (dim7 divisor **uncapped** ⇒ 400× deeper infra gives identical features; dim13 `log1p(ratio)/log1p(5)`). Contract travels with the artifact: cache `metadata.json`, MLP ckpt dict key, GNN **`.contract.json` sidecar** (bare state_dict), `run_provenance`; mismatch raises `QueueFeatureContractMismatchError` (absent key ⇒ `legacy_v0`, so v5.5 ckpts still serve). Validation: 28 contract tests; `legacy_v0` recache **numerically identical** to deployed `…873_v5.5` on all shared dims+labels; parity verifier passes 6/6 (contract × depth-scale 0/1×/live) — at live scale legacy dim7 reads **28–600** vs v1 **0.007–1.0**. Cache `graphs_cache_contention_v2_873_v5.7_siv1_dim14` (`CACHE_VERSION` 5.7, new `--platform-feature-dim 14` keeps `dim22` inference layout for apples-to-apples). **Retrain (wandb `gnn-queue-scale-invariant-aug2026`, identical splits/seed/hparams):** GNN val **62.6%** (v5.5 61.1%) test **55.7%** (60.3%) task_acc 85.9% (87.2%) greedy_regret 0.911s (0.848s); MLP val edge **91.6%** (91.4%) test **87.3%** (88.3%), early-stop e44. ⇒ offline ≈ unchanged, so any live delta is attributable to the feature scaling, not to model quality.
- **⛔ LIVE FAILURE IS QUEUE-FEATURE OOD, NOT LABELS (2026-08-13):** Models only see platform **queue depth (count)** at dim 7 (`raw_q / queue_norm`) and derived `usage_ratio` at dim 13 (`(raw_q/target_concurrency)/5`); task `queueTime` is **never** an input. `queue_norm = clamp(p90(depths), 1, **100**)` in *both* `prepare_graphs_cache.py:813` and `feature_builder.py:86-94` — formula parity holds, **magnitude parity does not**. Training (`contention_v2`): per-dataset p90 depth **26–70**, cap **binds 0/200** → dim7 ∈ ~[0,2], dim13 ~1.6. Live (`sparse_p35` s42 GNN): `averageExecutionTime` **0.035s** with `averageQueueTime` **503s** ⇒ chosen-platform depth ≈ **14.5k tasks**; cap always binds → dim7 ≈ **145** (~100× OOD), dim13 ≈ **580** (~360× OOD, **no cap at all**). No clipping ⇒ **silent** shift, not a loud failure. Behavioural proof from `*.decode_stats.json` `chosen_queue_vs_min` (n=117): **GNN** median **+411** / p95 **+6399** extra queued tasks vs the shortest available platform (worst `client_heavy_p50` 748 / `sparse_p35` p95 18373), **MLP** median **+7** / p95 **+1232** — matches MLP's better live p50 (0.69s vs 68s). Knative wins because its rule is scale-free. The cap is the only train/serve asymmetry and provably a **no-op on the training corpus**, so removing it is parity-preserving; dim13 has no normalizer and needs one (⇒ recache + retrain).
- **⛔ contention_v4_deepq pilot FAILED the coupling gate (2026-08-13):** Deep pre-seeded queues **destroy** coupling instead of creating it. 27 ds on datalab (job **663969**, CPU-amd, 60–290s/ds, best_rtt 13–116s vs `contention_v2` 1.8–16.4s): greedy == optimum **27/27**, regret **0.00%**, coupled>1% **0.0%** (vs `contention_v2` **7.1%**); best **colliding** plan is **−6.3%** *better* than best spread ⇒ the grid would teach **more** piling. Mechanism: depths sampled **176–648** per platform, so cross-platform depth differences swamp the marginal cost of one co-located task (v2: 26–55). Coupling needs marginal self-interference ≳ inter-platform spread. Corollary: **`contention_v2` labels already punish piling** — pile penalty (all tasks on one platform vs best spread) mean **+333%** / median **+455%**, and 0/900 have any cold start or pull. Labels are not the defect. Corpus kept at `simulation_data/gnn_datasets_4tasks_contention_v4_pilot/` (9.2M, JSONL intact) as the negative record; **do not train on it**. Probe: `/tmp/colocation_cost.py`.
- **RQ3b FAIL (2026-08-13):** Pre-registered coupled-trio × seeds 42–46 × Kn/MLP/GNN, 873/v5.5, `node_disk_v2` strict, post-`25732cf`. PRIMARY GNN rtt < MLP **0/3**; TAIL GNN p99 < MLP **0/3**; `sparse_p35` MLP>3×GNN p99 **0/5**. Kn wins all 3 cells. Seed-mean: sparse_p25 Kn **16.54M**/p99 **183** vs GNN 115.0M/1650 vs MLP 41.5M/1389; sparse_p35 Kn **24.21M**/254 vs GNN 185.0M/2929 vs MLP 118.7M/2889; skew Kn **4.14M**/130 vs GNN 36.7M/723 vs MLP 8.9M/440. Provenance OK (env `node_disk_v2`, dim22, argmax, 873 ckpt). GNN `sparse_p35` s42: `averageQueueTime` **503s**, pull ~0.017s, penalty~80% — pile + serialized FilterStore, not a wiring bug. Pre-serialization n=1 MLP cliff (783.5s) was an artifact under parallel pulls. Sweep `contention_v2_873_v5.5_coupled_trio_20260813/`.
- **Sealed re-baseline HEAD (2026-08-13):** Same 60 cells as `…_sealed_holdout_20260806` on post-serialization physics. Paired wins **G/M/K = 0/0/20** (was **1/13/6**). Kn rtt 10.3–18.9M vs MLP 34.6–49.4M vs GNN 104–215M; Kn p99 152–191 vs MLP ~1.9–2.0k vs GNN ~1.9–3.5k. **Do not put 20260806 and 20260813 in one table.** Sweep `contention_v2_873_v5.5_sealed_holdout_rebaseline_20260813/`.
- **⚠ per-cell SLURM execution skips whole-sweep provenance (2026-08-13):** Sweep runners write `manifest.json` (phase 0) and `compare.json` (last phase), but the datalab path submits **one SLURM task per cell** via `run_sealed_holdout_one.sh`, so neither phase ever runs and only `results/` is rsynced back — the sweep dir arrives with no physics/commit/ckpt-hash record and no compare. Fixed: manifest writer extracted to `scripts_cosim/important/write_sweep_manifest.py` (CLI, fail-loud on missing model/workload/empty seeds, refuses silent overwrite without `--force`), callable from the per-cell path; `run_contention_v2_873_sealed_holdout.sh` now calls it with overridable `MANIFEST_KIND`/`MANIFEST_NOTE`/`MANIFEST_EXTRA_JSON` so a re-baseline cannot inherit the original's identity. Old inline heredoc also had dead code (built `configs` wrong, then overwrote it with a hardcoded list). **After any datalab sweep: write the manifest + run compare locally.**
- **datalab live-sim CPU-amd (2026-08-13):** One SLURM task per cell, `--partition=CPU-amd` **no `--gres`**, 8 CPU / 32G / 6h. 90 tasks started immediately (17 idle nodes). Do **not** reuse `sealed_holdout_gpu.sbatch`. Login SSH can banner `Not allowed at this time` (transient; retry later). Scripts: `live_cpu_amd.sbatch`, `run_sealed_holdout_one.sh` (now sets `HEROSIM_REQUIRE_EXPLICIT_PHYSICS=1`), `transfer_and_submit_rq3_cpu_amd.sh`. Env: micromamba `gnn`, rsync `src/` for HEAD physics. Local MLP/GNN must be killed before submit or cells double-run.
- **⚠ warmth_physics CONFOUND (2026-08-13):** Live sim default is **`platform_reuse_v1`**, not `node_disk_v2`. `HEROSIM_WARMTH_PHYSICS` unset on the `regime_b_transfer_gate3` cell → total_rtt **1.019e9** vs **9.30M** with explicit `node_disk_v2` (**~110×**), because v1 serializes cold pulls per node when the platform has a `previous_task`. **Every launcher must export `HEROSIM_WARMTH_PHYSICS` explicitly.** Now enforced: `build_run_provenance()` in `executesimulation.py` writes `run_provenance` (physics + source `config|env|default`, defer-cold-init, batch/decode/feature-layout/model-path env) into every result JSON and prints a banner; `HEROSIM_REQUIRE_EXPLICIT_PHYSICS=1` fails loud on implicit physics (`ImplicitWarmthPhysicsError`). Helpers `describe_warmth_physics` / `require_explicit_warmth_physics` in `src/placement/warmth.py`. **All 26 pre-2026-08-13 sweeps have `warmth_physics: null`** — physics is not recoverable from those JSONs, only from the launcher script; do not mix them with post-provenance sweeps.
- **⚠ pull-serialization re-baseline `25732cf` (2026-08-12):** Commit moved the image-pull timeout **inside** the node FilterStore hold in `KnativeAutoscaler.initialize_replica`. Before: N co-located cold pulls run **parallel**; after: **serialize per node**. Identical cell (`sparse_p25` / knative / seed 42 / `node_disk_v2` / workload-125-225): total_rtt **6.90M → 17.21M (2.50×)**, carried entirely by `averageQueueTime` **12.23s → 30.57s**, not by per-task pull time. Intentional for Regime B FilterStore realism, but **pre-2026-08-12 Regime A sweeps are NOT comparable to HEAD** (incl. `contention_v2_live_gate_20260615`, `contention_v2_873_v5.5_sealed_holdout_20260806`). Historical dirs kept as the pre-serialization record; re-baseline lands in `…_sealed_holdout_rebaseline_20260813/`.
- **Tail metrics + MLP p99 cliff (2026-08-13):** `total_rtt` alone hid the collision-cliff signature the offline ablation predicted (pointwise max regret 1578–3164%). `scripts_cosim/sweep_metrics.py::load_metrics()` streams p50/p90/p99 from `taskResponseTimeDistribution` without full-parsing the ~120MB JSONs (~0.01s vs ~1s/file). Retro-scored **26 sweeps** → per-sweep `tail_metrics.json` + `simulation_data/normal_sim_sweeps/tail_metrics_index.json`. Findings: **sealed holdout (dense, 4 cells × 5 seeds) MLP wins p99 4/4** (24.4–31.6s) but **GNN beats Kn p99 4/4** (25.3–57.2 vs 33.1–90.6); on **sparse coupled** `contention_v2_live_gate_20260615` the order **inverts** — `sparse_p35` MLP p99 **783.5s** vs GNN **110.2s** / Kn **164.7s** (7.1× / 4.7×) with GNN also winning total_rtt (11.08M < Kn 12.77M < MLP 16.79M), while `sparse_p25` MLP p99 53.2 is best. **n=1 seed, pre-serialization physics** → replication pre-registered as RQ3b. Scripts: `retro_score_sweeps.py`, `test_sweep_metrics.py`; p90/p99 columns in `compare_sealed_live_holdout.py` + `compare_contention_v2_live_gate.py`.
- **Phase 3.1 multiseed ect_pull distill CLOSED (2026-08-12):** Cold-only harvest 500 rollouts ×12 = **6000** dim24 frames (latency grid × jitter∈{0,0.5,2.0} × seed). Soft+hard distill 40ep α=0.5 τ=0.25 from CE dim16. Live oracle_split_v1: `seq_reforward_pull` **31.66s** ≡ ect_pull/oracle **31.65s** (regret ~0); distill argmax **125.28s**. Warm/busy init mix (prior 6k) → **375.87s** REJECTED — under `platform_reuse_v1` busy queues set `previous_task` → warm → zero pull, poisons ordinal. Default harvest `--init-presets 0:0`. Ckpt `…-ect-pull-distill-multiseed.pt` (`f98f43f5…`). Scripts: multiseed `harvest_ect_pull_distill_corpus.py`, jitter/warm/busy `build_regime_b_live_stub.py`, `warm` flag in `simulation.py` precreate.
- **Phase 3 ect_pull distill PARTIAL (2026-08-12):** Live DES harvest of ect_pull (State,y*,soft-ECT) → 12 dim24 frames (ledger-injected). Distill L=(1-α)CE+ατ²KL from CE dim16 init (α=0.5,τ=0.25). Live: `seq_reforward_pull` **62.63s** (beats CE **93.95s** by 31s) · distill argmax **125.28s** · ect_pull **31.65s**. Cross-batch `pulls_committed` persists on GNNScheduler. **Hard-CE-only (α=0, 800ep) → 125.28s REJECTED** — soft targets required. 62.63s ≡ absolute cold×T_pull cost — partial avoidance ≠ marginal ordinal. Superseded by 3.1. PR #3 `129a24a`. Scripts: `harvest_ect_pull_distill_corpus.py`, `train_ect_pull_distill.py`, `run_phase3_ect_pull_distill_eval.py`, `distill_log.py`.
- **Phase 2 soft_combo_conc REJECTED (2026-08-12):** capacity-matched dim16 on oracle_split_cosim (48 graphs, τ=0.25, γ=0.02, CE anchor). Offline val joint **0%** (same as CE); Conc≈0. Live oracle_split_v1: `frozen_topk` **125.28s** ≡ soft `argmax` **125.28s** vs CE argmax **93.95s** (hurt −31s) vs ect_pull **31.65s**. b≤4 Boltzmann over `placements.jsonl` + additive joint decode insufficient for N=12 continuous arrivals — discard Set Transformer (same capacity/label regime). Harness `run_phase2_soft_combo_conc_ablation.py`; train `run_regime_b_oracle_split_soft_combo_conc_train.sh`; ckpt `models/near-rtt-v2-regime-b-oracle-split-cosim-dim16-soft-combo-conc.pt` (`5cb6ddd2…`). PR #3 `d6f1999`.
- **Phase 1 pull-ledger decode REJECTED (2026-08-12):** `GNN_DECODE_MODE=seq_reforward_pull` — CE argmax + queue refresh + `pulls_committed[node]` rewriting dim24 cold_count/pull_remaining + re-forward. oracle_split_v1 + oracle_split dim16 CE: argmax **93.95s**, pull-decode **125.28s** (hurt −31s); ect_pull **31.65s**. cold-burst dim16 under post-parity live → **375.7s** both modes. Decision-time FilterStore bookkeeping alone ≠ ect_pull intelligence. Harness `run_phase1_pull_decode_ablation.py`.
- **Phase 0.1 cache↔live parity FROZEN (2026-08-12):** `verify_cache_live_feature_parity.py` — SSC extract → `prepare_graphs_cache.build_graph` vs `build_pyg_inference_graph`; **48/48** oracle_split dim24 PASS. Contract bugs fixed in live: (1) `target_concurrency` used rf/cnn + `max(1.0)` vs cache dnn1/dnn2; (2) undirected `edge_attr` naive clone misaligned reverse edges after `to_undirected`; (3) live/`prepare_graphs_cache_seq` missing `node_edge_index`. Meta now carries `initialized` for pull decode. PR #3.
- **Post-parity zeroshot caveat:** Pre-parity live zeroshots (125.28s) used buggy live builder; after parity freeze, INTEL ckpt argmax improves to **93.95s**; cold-burst dim16 collapses to **375.7s**. Re-baseline before citing 125s as current argmax floor.
- **Oracle-split cosim retrain NEGATIVE (2026-08-12):** 48 ds (latency×scarce-attract grid) → CACHE 5.6 (48 graphs, plat16, contract OK). GNN val joint **0%** (topk fallback ckpt `1bdcc446…`); MLP edge **25%** (`5703f002…`). Live zeroshot: ect_pull **31.65s**, MLP **125.28s** (=873), GNN **187.93s** (≈Kn), Kn 188.17s. N=4 FilterStore labels ≠ live N=12 residual close. CE-only now also checkpoints on topk regret when joint acc stuck at 0.
- **Physics ceiling + oracle_split co-sim (2026-08-12):** Intel default policies include `ect_pull`. Canonical `…_physics_ceiling_zeroshot`: ect_pull **31.65s**. Co-sim pilot then scaled to **48** ds FilterStore-scale (best≈31.65 / contended≈125.57).
- **Pull-obs diagnosis (2026-08-12):** Live features OK — node0 rem/100=**3.756** vs remote **0.313**; `shared_fate`=1.0 both. Implied FilterStore depth: GNN≈**4** (125s), Kn≈**6** (188s), MLP≈**12** (376s), oracle=**1**. Co-sim `ds_00000` optimal elapsed max **1.67s**, pullTime=0 (warm-path) — labels do not teach N×T_pull split. Absolute `cold_count×T_pull` as cost → **62.63s** (prefers depth-2 remote over unused node0). **Marginal** ordinal `(committed+1)×T_pull` via `ect_pull` → **31.65s = oracle**. Vanilla ECT (no pull) → **376s**. Residual is solvable without decode/RQ3; learned path must use marginal depth or matching labels.
- **Pull-obs zeroshot NEGATIVE (2026-08-12):** CACHE 5.6 / GNN dim16 val **58.2%** (`23fcf158…`) / MLP dim24 val edge **89.9%** (`3e869afa…`). Live `oracle_split_v1_pullobs_zeroshot`: GNN **125.28s** (=873), Kn **188.17s**, MLP **375.71s** (full scarce pile 11.87×), oracle **31.65s**. Residual 125→31 **NOT closed**. Intel margin still PASS (GNN vs Kn 62.9s).
- **Pull observables CACHE 5.6 (2026-08-12):** Platform dim **14→16**. Dim 14=`node_cold_count` (absolute); dim 15=`estimated_pull_remaining_sec/100` (`cold_count×T_pull`). Layout `dim24` (3+16+5). Helpers in `src/placement/warmth.py`; wired in `prepare_graphs_cache.py` + `feature_builder.py`. 873/dim22 unchanged. Unit test `scripts_cosim/test_pull_observables.py`.
- **Regime B 450 retrain (2026-08-12):** Cache 450/CACHE 5.5 contract OK. GNN val **61.2%** (`b97187c1…`); MLP val edge **91.0%** (`4c25cb6a…`). oracle_split_v1 zeroshot: MLP **125.28s** (same as 873), GNN **156.87s** (worse), Kn **219.48s**. Residual 125→31 **NOT closed** — co-sim scarce-warm labels lack absolute FilterStore-depth signal (`shared_fate` saturates). Keep 873 for intel deploy.
- **Regime B oracle_split_v1 (2026-08-12):** `scarce_preinit_v2` cannot yield intelligence (action space=node0 only → Kn≡MLP≡GNN). Probe: parallel seeds → Kn≡oracle 1.00×; **union seeds** (N on node0 + 1/other) → Kn ~5.9–6.9×, MLP/GNN 873 **125.28s** beat Kn **188.17s** (margin **62.9s**). Gate cell stays v2 (≥10×). Kn shortest-queue nondeterministic on union (~188–219s).
- **Regime B scarce_preinit_v2 (2026-08-11):** Root causes of all-cold 1.01×: (1) Kn/GNN ignored `initial_replicas` → one cold replica warms after first pull; (2) Knative/GNN `initialize_replica` released FilterStore *before* pull timeout (determined holds during timeout). Fix: seed N cold deferred on node0 + integrate seeds + hold FilterStore during pull. Free Kn **11.88×**; MLP/GNN 873 zero-shot **~11.87×** (tied, not intelligence). MLP fail-loud if `mlp_model_path` missing.
- **Regime B zero-shot lesson (2026-08-11, superseded by v2):** Forced oracle vs contended = **11.88×** PASS. Free Kn/MLP/GNN all **~31.88s (1.01× oracle)** on all-cold live stub — historical; do not cite as current stub.
- **Regime B co-sim (2026-08-11, datalab CPU-amd 648835):** grid `regime_b_cold_burst_v1` **450/450** JSONL under `platform_reuse_v1`; shard script post-count `[[ -s ]] && echo` + `set -e` caused false FAILED exit — fixed to counting loop + `exit 0`.
- **Simulator landscape (2026-06-16):** `memory/simulator_landscape_pureedgesim_noserver.md` — five-way compare; **HeroSim fork** stays native for `placements.jsonl`/warmth/contention_v2; **EdgeCloudSim** per-task VM fit (no sticky) but static VMs, no warmth; **PureEdgeSim** mist/energy but sticky; **NoServer** LB-only port feasible; EdgeCloudSim needs Java 21 + `vm_load_check_interval≠0`
- **contention_v2 finisher gotcha:** `--only-missing-jsonl` skips ds without `best.json` — timed-out shards need plain `--resume` only (fixed in `contention_v2_finish.sbatch`)
- **contention_v2 pipeline scripts:** `run_contention_v2_train_and_live_gate_nohup.sh`, `run_contention_v2_live_gate_one.sh`, `compare_contention_v2_live_gate.py`; datalab `contention_v2_finish.sbatch`, `contention_v3_regen.sbatch`, `run_contention_regen_shard.sh`
- **contention_v3 pipeline DONE (2026-06-20, datalab):** corpus **900/900** · cache `graphs_cache_contention_v3` (900 graphs, `--rewrite-ssc` before recache) · GNN `near-rtt-v2-contention-v3-dim14-ce-only.pt` (val **65.6%**) · MLP `batch_edge_mlp_contention_v3_dim22_batchcache.pt` (val edge **88.2%**) · live gate **9/9** `contention_v3_live_gate_20260620/` · sums Kn **20.27M** < GNN **36.00M** < MLP **43.76M** · wins **Kn 2 / MLP 1 / GNN 0** · **REJECT for deploy** (+81% vs v2 GNN sum) · scripts `transfer_contention_v3_pipeline_to_datalab.sh`, datalab `contention_v3_{recache,gnn_train,mlp_train,live_gate_gpu,compare}.sbatch`
- **Merge deployment live gates CLOSED (2026-06-27, mitrix):** strategic merge **9+9** + weighted **12/12** sim JSONs; **30/30** valid; Kn wins all strategic gates; weighted kn 2/3 + gnn_uniq 1/3; **ship contention-only GNN** for sparse ER. Compare tables: `strategic_merge_wss_live_gate_20260616/compare.txt`, `strategic_merge_contention_live_gate_20260616/compare.txt`, `merged_contention_weighted_live_gate_20260616_105500/compare.txt`; log `logs/finish_live_gate_sweeps_20260627_131232.log`. Phase markers: `logs/strategic_merge_pipeline/phase_{sweep_wssm,sweep_contention,all}.done`, `logs/merged_contention_pipeline/phase_{live_gate,all}.done`. Scripts: `finish_live_gate_sweeps.sh`, `run_strategic_merge_train_sweep.sh` (skip-if-exists, `TIMEOUT=18000`)
- **Placement integrity + separability audit DONE (2026-08-04):** retained warmth **487**, sparse **351**, contention_v2 **873** (was 899; −26 network-incoherent JSONL), contention_v3 **900**; exclusions explicit; 12.29M→**3.25M** cont_v2 rows. Manifest `simulation_data/placement_integrity_manifest.json`; frozen separability `simulation_data/separability_audit_4corpus_20260804.json` (still quotes pre-exclusion n=899 collision stats — rebind if paper cites n)
- **Training-contract A/B DONE (2026-08-04):** `simulation_data/training_contract_ab_20260804.json`. OLD labels nonopt/absent: warmth **210/487**, sparse **166/351**, cont_v2 **44/899** → NEW **0**. Warmth SSC prev_type **25–33%**; 8× parent-leak **100%→0%**. Code CACHE_VERSION **5.5**
- **contention_v2 v5.5 recache + RQ3 ablation DONE (2026-08-04):** cache `graphs_cache_contention_v2_873_v5.5` (873 graphs, validator OK); excluded 26 best_json_rtt_mismatch whose JSONL plans are network-unreachable from task sources (prepare now fails loud). Multi-seed ablation seeds 42/43/44, parent 70/15/15, pointwise vs gnn_base: top1 **87.1±0.9% vs 88.9±0.8%** (GNN 3/3); p90 **3.88±0.53 vs 3.03±0.55**; opt-recov **58.8 vs 64.1**; cliff seeds **1 vs 0** (pw max **1578%** seed42). Coupled-tail mixed. Report `gnn_necessity_ablation_contention_v2_873_v5.5_multiseed.json`
- **873/v5.5 deploy retrain DONE (2026-08-06):** GNN best val **61.1%** test **60.3%** → `models/near-rtt-v2-contention-v2-873-v5.5-dim14-ce-only.pt` (md5 `3efed472…`); MLP val edge **91.4%** test **88.3%** → `models/tabular/batch_edge_mlp_contention_v2_873_v5.5_dim22_batchcache.pt`.
- **RQ3 ANSWERED (2026-08-11):** offline GIN>MLP on v5.5/873; sealed live MLP **13/20** · Kn **6/20** · GNN **1/20** (sums MLP 0.82×Kn, GNN 1.05×Kn). Sweep `contention_v2_873_v5.5_sealed_holdout_20260806/compare.json`.
- **Leakage/parity fix (2026-08-04):** SSC warmth+replicas, parent-first 70/15/15, fail-loud `@os/@seq` RTT, MLP real val split. Helpers `non_unique_lib/training_contract.py`; A/B `scripts_cosim/ab_training_contract.py`; validator `scripts_cosim/validate_training_cache_contract.py`.
- **Data gaps (2026-08-04):** skew_warmth_v2 has only **98/288** mandatory JSONL sweeps and is absent from the integrity manifest; local contention_v3 cache/models/live-gate and hub9 ce-reduced result JSONs are absent (documented datalab-only/deleted locally). RQ1's 7/7 headline remains pre queue-map-fix; post-fix evidence is 3/7.
- **contention_v3 mitrix state:** corpus **900/900** jsonl local; **no** local cache/models/live-gate sweep — pull from datalab or run `run_contention_v3_train_and_live_gate_nohup.sh` if local copies needed
- **Non-unique backfill DONE (datalab, 2026-06-15):** warmth_v2 **492 SUCCESS + 9 SKIPPED** / **498 jsonl** / **490 non_unique_meta**; sparse **348 SUCCESS** / **351 jsonl** (ds_00081, ds_00224, ds_00251 cartesian-full, delta=0); contention_v2 **900/900 jsonl**; scripts `generate_non_unique_placements_fast.py --datasets-dir`, datalab `warmth_non_unique_{warmth,sparse}.sbatch`, monitor `cosim_health_report.sh` + `cosim_monitor_loop.sh` (~6h, 12 ticks)
- **Monitor gotcha (sparse):** SUCCESS count **348/351** ≠ incomplete — 3 ds already full cartesian; mark done at **non_unique_meta≥348** + jsonl=351; do not resubmit on stall alone
- **wssm autoscale sensitivity (2026-06-15):** `wssm_autoscale_bipartite3_20260614/` · ql∈{50,100,200} · Kn wins **8/9** vs GNN (only hub_k8@ql200); at ql=100 Kn wins all 3 — **opposite** bipartite v2 wssm gate @ default ql=100; autoscaling knob materially shifts ranking
- **Live gate contention_v2 DONE (2026-06-15):** `contention_v2_live_gate_20260615/` · GNN `near-rtt-v2-contention-v2-dim14-ce-only.pt` (val 66.2%) vs MLP `batch_edge_mlp_contention_v2_dim22_batchcache.pt` (val 90.0%) vs Kn · workload-125-225 · 3 sparse configs · sums **GNN 19.88M < Kn 20.73M < MLP 25.35M** (−4% vs Kn, −22% vs MLP) · per-config wins **GNN 1 / MLP 1 / Kn 1** (split) · offline GNN advantage does not uniformly transfer (MLP wins sparse_p25 live)
- **contention_v3 DONE datalab 900/900 + live gate (2026-06-20):** final offline coupling **decreased** vs v2 (4.2% vs 7.1%); end-to-end train+deploy **confirms rejection** — GNN sum **36.00M** vs v2 **19.88M** (+81%); sparse_p35 cliff worse (GNN 26.4M vs v2 11.1M)
- **GNN-necessity ROOT CAUSE + final audit — `memory/gnn_necessity_separability.md`:** original models used unique-replica labels (`--allow-non-unique-replicas` default False), making the training pass separable by construction. **Post-backfill retained inventories are not separable:** warmth collision **39.8%**, coupled>1% **9.7%**; sparse **33.0% / 10.3%**; cont_v2 **40.3% / 7.1%**; cont_v3 **36.3% / 4.2%**. Existing warmth/sparse checkpoints do not incorporate these backfilled labels until recached/retrained.
- **Separability is hard to break (2026-06-15):** `contention_v1` (all-cold, sparse, heavy q) → collisions **~43%** but greedy regret still **~0%** (when colliding is optimal both tasks prefer it anyway). Greedy only breaks with **anti-correlated prefs**: 2 tasks share #1 platform AND co-locating is expensive → optimum must split → needs **scarce-warm replica + heavy queue + sparse fallback** = `contention_v2`.
- **`contention_v2` corpus (873 retained jsonl):** pre-exclusion 899 had optimum collides **40.3%**; greedy regret mean 0.33% / p99 7.93% / max 22.4%; **coupled (>1%) 7.1%**. Cache `graphs_cache_contention_v2_873_v5.5` — **873 graphs**, CACHE 5.5, validator OK; offline multi-seed ablation frozen
- **Ablation `gnn_necessity_ablation.py` (contention_v2, test n=142, identical feats/loss/split):** gnn_base **top1 90.7% / p90 regret 2.39% / opt-recov 69.0% / max 7.03%** beats pointwise (89.1% / 2.81% / 64.8% / **max 3164%**) on every robust metric + **eliminates catastrophic collision-cliff tail**. Mean regret unreliable (opt_rtt≈2 units → single bad plan = 3164%). **gnn_node adds nothing** (same-node edges redundant w/ `shared_fate_signal` dim8; needs residual+candidate-restrict just to train). GNN advantage = **collision robustness, not avg accuracy**.
- Determined scheduler must capture full_queue_snapshot and temporal_state_at_scheduling in _process_task_batch() for co-simulation data generation
- --quiet and --fast-forward-warmup default to True in generate_gnn_datasets_fast.py (use --no-quiet/--no-fast-forward-warmup to disable)
- State capture fields (queueSnapshotAtScheduling, fullQueueSnapshot, temporalStateAtScheduling) must be populated on Task object before placement decision
- GNN scheduler batch: `GNN_BATCH_SIZE` / `GNN_BATCH_TIMEOUT` env vars (default 4 / 0.002s); **>4 fails loud**; MLP inherits via `GNNScheduler`; **do not sweep b≠4** without retrain
- **`run_simulation.py` BASE_DIR:** must be `Path(__file__).parents[1]` — hardcoded `/root/projects/...` breaks datalab
- **Tiered-hub asymmetric latency:** `generate_infrastructure.py` uses `latency_core_ms` + `latency_periphery_ms`; generator CLI `--latency-core-ms` / `--latency-periphery-ms`
- **Bipartite sweep v1 (DONE 2026-06-10):** `sweep_bipartite_coordination_v1/` — k∈{4,6,8} × seek∈{35,50,65}%, 5/30ms, b=4; sums GNN **17.90M** · MLP **21.49M** · Kn **18.37M**; GNN vs MLP **8/9**; 3-way **GNN 5 · Kn 3 · MLP 1**; MLP collapse k6 seek50 (3.46M)
- **seq_reforward decode (2026-06-10):** `GNN_DECODE_MODE=seq_reforward` in `seq_decode.py` + `gnn/scheduler.py`; per-task argmax + `_refresh_queue_dependent_platform_features` + full `model(graph)` re-forward; k6 seek65 **1.63M** (−3.3% vs Kn 1.68M, −7.4% vs argmax 1.76M); k6 seek50 **1.55M** (still +7.1% vs Kn 1.45M); collision batch rate **~30%→~15%**; qvm p95 **118→0**; decode **~33ms/batch** vs argmax **~0.1–0.2ms/batch** (~158k batches → **~88–97 min** decode wall vs **~17–30s**)
- **seq_reforward cost model:** b=4 → **4× GNN forwards/batch** (~8ms each) + O(n_platforms≈208) feature refresh per task; total ~632k forwards vs ~158k for argmax; SimPy RTT unchanged by inference wall
- **Datalab GPU queue:** GPU-l40s saturated → duplicate submit on **GPU-a40** OK; shared results dir + skip-if-exists
- **125-225 result JSON ~120MB each:** `stats` blob dominates; compare needs `total_rtt` only — trim after tables captured
- **Storage (2026-06-10):** deleted overnight graph caches + legacy 6.8G/4.2G monolith JSONs + logs (~37G freed); **kept** `gnn_datasets_4tasks_overnight_260422` (34G)
- Batch size logging added to GNN scheduler: tracks frequency of 2/3/4 task batches and wait times
- GNN training supports merged datasets: prepare_graphs_cache.py loads 2tasks, 3tasks, and 4tasks datasets; cache dir: graphs_cache_merged_2_3_4_tasks
- GNN generalization: Task-centric graph construction (local subgraph view) enables generalization across network topologies (training on 20-90% connectivity, inference on 50%)
- GNN architecture (homogeneous family): same `TaskPlacementGNN` — MLPEncoder task/platform encoders (3-dim task, **14-dim platform**), GIN (3 layers), EdgeScorer (5-dim edge attrs); additive joint score = sum per-task logit scores over placement combo; live policy `gnn` + sequential argmax
- **Platform feature dim 14→16 layout:** `[type_onehot(5), has_dnn1(1), has_dnn2(1), queue(1), shared_fate(1), current_task_remaining(1), cold_start_remaining(1), comm_remaining(1), target_concurrency(1), usage_ratio(1), node_cold_count(1), estimated_pull_remaining_sec/100(1)]`; dim 8 = `shared_fate_signal` = `cold_count / total_platforms_on_node`; dims 14–15 break shared_fate saturation (CACHE 5.6 / `INFERENCE_FEATURE_LAYOUT=dim24`)
- **FilterStore serialization proof:** `test_storage_contention.py` — SimPy-only N=1..5; theory match <0.01s; queue=0 blind spot
- **Cold-start / queueTime / last-task audit (2026-06-11):** `test_cold_start_queue_last_task_ab.py` — N=1..5 multiplier 1.00×…4.96×; last-task penalty = (N−1)×T_pull; platQ=0; shared_fate=1.0 saturates; queueTime linear 31/63/94/125s; warm → 0.35s; 4×flash → 31.65s; invisible_gap task0=93.56s; total_rtt 501s vs last 126s (3.99×)
- **Storage contention reference map:** `memory/storage_contention.md` — FilterStore N× RTT, cold-start/queueTime/last-task mechanics, metric traps, GNN/MLP feature gap, counterfactuals A–D; verified A/B scripts → `simulation_data/{memory_contention_ab,cold_start_queue_last_task_ab}/summary.json`
- **Co-sim warmth gap (1060 audit):** `memory/cosim_warmth_gap.md` — **historical** 1060 pullTime 0%; warmth_v2 regen partial; skew3 live MLP **2/3** — disk feature + hub co-sim grid still missing
- **Warmth model (`node_disk_v2` shipped):** `src/placement/warmth.py` — pull=disk-only, sandbox=`previous_task`; flag `platform_reuse_v1`|`node_disk_v2`; co-sim default v2+defer_cold; Gate B 125s→31.65s; pilot PASS; **regen ~96% on datalab** — see `memory/cosim_grid_and_regen.md`
- **Co-sim grid (2026-06-11):** Old corpus **1242 ds**; warmth_v2 **500 ds** (1×5×20×5, conn=0.50); dir name `1060` legacy — `memory/cosim_grid_and_regen.md`
- **Co-sim fast-path regen:** datalab single finisher **`warmth_regen_finish.sbatch`** (array 0-0, `--resume` ds_0..499); cancel competing warmth jobs via `submit_warmth_regen_finish.sh`; **`output_dir/.bf_scratch`** per dataset; inline `import shutil` bug fixed
- **Co-sim runtime driver:** brute-force cost ∝ **placement count** (`new=` in logs), not conn as separate step; warmth_v2 @conn=0.50: (1,2) med **~3.7k/~60s**; (1,3) med **~9.6k/~7min**; tail **~40k**; lower conn + (1,1)/(1,2)/(2,2) → much faster than (1,3)/(2,3)
- **Sparse expansion Option A (351 ds):** conn **{0.25,0.30,0.35}** × rep **{(1,1),(1,2),(2,2)}** × queue **{pois12,norm16,pois16}** × seeds **121–133** → subdir **`gnn_datasets_4tasks_sparse_warmth_v2`**; **`--grid sparse_warmth_v2`** in `generate_gnn_datasets_fast.py`
- **Sparse co-sim datalab (DONE 2026-06-11):** **351/351** `best.json`; job **480275** 4×96 CPU (shards 0–2) + **480288** shard3 **64 CPU**; range264 **480302–480308** 4×48 CPU; finisher **480322** 128 CPU recovered **48** gaps; partition **GPU-a100s** when a100 saturated; scripts `warmth_sparse_regen.sbatch`, `submit_warmth_sparse_range264_parallel.sh`, `warmth_sparse_regen_finish.sbatch`, `transfer_warmth_sparse_from_datalab.sh`
- **Sparse co-sim cost:** @conn≤0.35 + light reps → **~3–15s/ds** typical; finisher instant-fail cells retried OK
- **Merged warmth+sparse cache (mitrix):** `graphs_cache_warmth_v2_sparse_merged` — **824 graphs** (473 warmth + 351 sparse), **6.06M** RTT rows, 31 chunks; **350/351** sparse indices share `ds_XXXXX` suffix with warmth (distinct path prefix; different grid cells)
- **Merged seq cache (mitrix, done):** `graphs_cache_warmth_v2_sparse_merged_seq` — **4097 seq graphs** (817 parents × ~5 incl. @seqneg1); **6.06M** RTT rows; v7.1-atomic21; MLP tabular source; build `prepare_graphs_cache_seq.py --base-dirs warmth_v2 sparse_warmth_v2`
- **Merged GNN dim14 from-scratch (done):** `models/near-rtt-v2-warmth-sparse-merged-dim14-ce-only.pt`; cache `graphs_cache_warmth_v2_sparse_merged`; random init; 100ep; val acc **29.3%**; test top5 **0.084s** / greedy **6.29s**; mitrix `run_warmth_sparse_merged_train_nohup.sh`
- **Merged GNN ce-reduced from-scratch (done):** `models/near-rtt-v2-warmth-sparse-merged-ce-reduced.pt`; same 824-graph cache; datalab job **480345** (train-only after cache rsync); val acc **29.3%**; test top5 **0.069s** / greedy **6.66s**; launcher `train_near_rtt_v2_warmth_sparse_merged_ce_reduced.py`; `INFERENCE_FEATURE_LAYOUT=ce_reduced`
- **Datalab merged-train pattern:** **rsync** `graphs_cache_warmth_v2_sparse_merged` (~290M) from mitrix — do **not** rebuild on datalab (warmth `ds_00235+` missing SSC); `transfer_warmth_sparse_merged_ce_reduced_train_to_datalab.sh`; run script skips SSC/cache when `graphs.pkl` present; **`NEAR_RTT_DATALOADER_WORKERS=0`** on SLURM (default 4 → `Too many open files`)
- **Merged MLP full (done):** `models/tabular/batch_edge_mlp_warmth_sparse_merged.pt`; seq cache → parquet `batch_edges_warmth_sparse_merged.parquet` (27,515 rows / 3,296 graphs); val edge acc **70.0%** @ep43 early stop; script `run_warmth_sparse_merged_mlp_full_train_nohup.sh`
- **Merged MLP reduced (done):** `models/tabular/batch_edge_mlp_warmth_sparse_merged_ce_reduced.pt`; in-process slice from merged seq cache; val edge acc **70.2%** @ep36; 11-d; script `train_mlp_v2_warmth_sparse_merged_ce_reduced.py` + `run_warmth_sparse_merged_mlp_train_nohup.sh`
- **hub9 live gate (DONE 2026-06-11):** `warmth_sparse_merged_ce_reduced_hub9_20260611/` — job **480403** 27/27 baseline; sums Kn **7.44M** · GNN **12.31M** · MLP argmax **202.53M**; Kn **9/9**; MLP `chosen_queue_vs_min` **~1122**
- **hub9 ablation (DONE 2026-06-11, job 480562, 18/18):** Test B **seqblend1** 9/9 mean **647K** (**0.78× Kn**, **0.029× MLP argmax**); q_mean **~0**; **~500K p1 overrides/run**; Test C **dim22 argmax** 9/9 mean **1.27M** (**1.54× Kn**, **0.057× MLP argmax**); q_mean **~38**; scripts `*_hub9_ablation_*`
- **Decode pathology (hub9, code truth):** MLP/GNN batch = **one forward** on batch-start snapshot; plain **`GNN_DECODE_MODE=argmax` ignores `live_queues` for pick** (roll-forward telemetry only); seqblend fixes collapse without retrain; **not** missing roll-forward logic
- **Environment simplicity (hub9 lesson):** warm 125-225 steady-state → Knative ≈ ceiling; seqblend+override only **~22%** RTT gain vs Kn — insufficient headroom to justify "intelligent actor"; **`total_rtt` dilutes** burst catastrophes — use last-task / max-burst RTT per `storage_contention.md`
- **Regime B blueprint (agreed):** (1) metrics harness first — prove oracle–Kn regret on cold bursts; (2) workload + Knative **ECT** baseline; (3) Tier-3 `pulls_in_flight` + `estimated_pull_remaining_sec` before claiming learned > physics-aware greedy
- **Task types inventory:** active `data/nofs-ids/task-types.json` + `application-types.json` = **dnn1**, **dnn2** only; legacy `task-types-og.json` + `application-types-og.json` adds **rf** (`nofs-rf`, Random Forest) and **cnn** (`nofs-cnn`); `peak_configs.json` references nofs-rf/nofs-cnn; `generate_task_types(count=…)` can synthesize arbitrary types at runtime
- **MLP requires seq cache:** non-seq merged hom cache (824 graphs) fails `train_mlp_ce_reduced.py` — need `sequential_counterfactual=true`; seq = one CE softmax per decision step with roll-forward queue snapshots
- **Sparse finetune (mitrix, done):** `models/near-rtt-v2-warmth-sparse-finetune-dim14-ce-only.pt`; init `near-rtt-v2-warmth-dim14-ce-only.pt`; 50ep LR **1e-4**; best val acc **25.6%**; test top5 **0.069s** / greedy **7.20s** / sidecar_hit **41%** — **offline weak vs warmth-only**; pipeline `run_warmth_sparse_recache_finetune.sh` + `train_near_rtt_v2_warmth_sparse_finetune.py`
- **Pipeline gotcha:** `scripts_cosim/important/*.sh` must `cd "$(dirname "$0")/../.."` (not `/..`) or `models/` checkpoint paths fail
- **warmth_v2 datalab finisher:** **480188_0** cancelled ~491/500 for sparse priority; mitrix warmth still **473** cached / **480** best.json — full 500 rsync pending
- **warmth_v2 GNN full (done):** `models/near-rtt-v2-warmth-dim14-ce-only.pt`; best val acc **23.4%** @ep78; test acc **20.8%** / task_acc **68.8%** / top5 **0.033s** / greedy **8.03s** / oracle_top5 **0.0005s**; wandb `gnn-near-rtt-warmth-v2-jun2026` run `ngom850z`; launcher `train_near_rtt_v2_dim14_ce_only.py`
- **warmth_v2 GNN reduced (done):** `models/near-rtt-v2-warmth-ce-reduced.pt`; task=3/plat=6/edge=2; best val acc **21.3%**; test top5 **0.048s** / greedy **7.92s** / oracle_top5 **0.0009s**; wandb run `s2z7dntc`; launcher `train_near_rtt_v2_warmth_ce_reduced.py` (in-process slice, no regen)
- **warmth_v2 GNN offline ablation:** full dim14 **beats** reduced on val acc (+2.1pp) and test top5 (0.033 vs 0.048s); per-task acc ~69% both; greedy ~8s both — offline ≠ live; reduced **skew3 live** done
- **warmth_v2 skew3 old models (DONE 2026-06-11):** `warmth_v2_physics_skew3_20260611/` — node_disk_v2 · Knative + **v1** `batch_edge_mlp.pt` + `near-rtt-v2-dim14-ce-only.pt`; 9/9; sums Kn **5.85M** · MLP old **4.25M** · GNN old **5.02M**; MLP old wins default+degree_skew; Kn≈GNN old on sparse
- **warmth_v2 skew3 ce-reduced live (DONE 2026-06-11):** `warmth_v2_ce_reduced_skew3_20260611/` — job **480265** 6/6; `near-rtt-v2-warmth-ce-reduced.pt` + `batch_edge_mlp_warmth_ce_reduced.pt`; 3cfg×MLP+GNN; **v1 MLP still wins 2/3** despite train/sim mismatch; v2 GNN degree_skew **0.66M** (old GNN 0.84M); v2 GNN sparse **1.23M** regresses vs Kn **0.93M**; scripts `run/submit/transfer_warmth_v2_ce_reduced_skew3_*`
- **datalab skew3 elapsed sacct false-FAIL:** final `python3 -c` used Python `{elapsed}` not bash `${elapsed}` — JSON OK, sacct exit 1; fixed in `run_warmth_v2_{ce_reduced,physics}_skew3_one.sh`
- **warmth_v2 MLP full (done):** seq cache `…_warmth_v2_seq` (2354 graphs); `batch_edge_mlp_warmth_v2.pt` val edge acc **68.2%**; pipeline `run_warmth_v2_mlp_train_nohup.sh`
- **warmth_v2 MLP reduced (done):** `batch_edge_mlp_warmth_ce_reduced.pt` val edge acc **70.3%**; **5249** params; 11-d; `train_mlp_ce_reduced.py` + `reduced_features.py`; `INFERENCE_FEATURE_LAYOUT=ce_reduced` on load
- **GNN graph topology (code truth):** **Bipartite task→platform edges only** — no client→hub physical edges; “multi-hop” = per-edge latency + GIN mixing on shared hub candidates (`prepare_graphs_cache_seq.py`)
- **GNN vs MLP decode parity:** `MLPBatchScheduler` inherits `GNNScheduler`; same `_decode_placements`; plain **argmax ignores `live_queues` for pick** (roll-forward telemetry only); seqblend/LQB/seq_reforward use live queue; batch advantage = **GIN coupling** when logits correct, not collision repair under argmax
- **GNN vs MLP feature exclusivity:** `src_norm` live on **dim22 MLP already**; `node_disk_hit` helps **both** if wired to dim13/dim22 — separation needs **k>b hub topology in co-sim labels** + batch coupling; see `memory/gnn_v2_sparse_topology_and_features.md` §4
- **Offline metric trap:** compare GNN **`val/task_acc`** ↔ MLP **`val_edge_accuracy`** (~69% vs ~68%); **NOT** GNN `val/acc` (~23%) vs MLP edge acc
- **MLP train logging:** stdout + `.meta.json` only — **no wandb** yet (GNN logs CE/acc/regret via `train_near_rtt.py`)
- **Post-regen enrichment:** `refresh_optimal_full_stats.py --rewrite-ssc` sufficient when `optimal_result.json` has taskResults (473/473 ok); 7 missing optimal copied from `.bf_scratch/simulation_1_optimal.json`
- **warmth_v2 cache (2026-06-11):** `graphs_cache_gnn_datasets_4tasks_1060_warmth_v2` — **473 graphs**, **5.68M** RTT rows (29 chunks), CACHE_VERSION 5.3; rsync via `transfer_warmth_pilot50_from_datalab.sh`
- **dim14 train env:** `PYTHONPATH=$(pwd)` + `NEAR_RTT_CACHE_DIR=…`; `train_near_rtt.py` infers dims from cache — **hom dim14: task=3, plat=14**; **seq/atomic21 warmth recache: task=2** (no `src_norm` — train/serve landmine)
- **warmth_v2 MLP pipelines:** `run_warmth_v2_mlp_train_nohup.sh` (full 21-d); `run_warmth_v2_mlp_ce_reduced_train_nohup.sh` (reduced 11-d, slice seq cache in-process); env `MLP_MODEL_PATH`
- **datalab CPU-only jobs:** no `--gres`; prefer **GPU-a100 @ 64 CPU** when a100s saturated; `sbatch --test-only` before submit; no CPU partition
- **P0 autoscaler fix (2026-06-11):** 12 Knative-family autoscalers — pull I/O block indentation inside `needs_image_pull(..., active_storage=)`
- **GNN win-zones + feature gaps:** `memory/gnn_v2_sparse_topology_and_features.md` — merged 824 cache, bipartite mechanism, minimum separation stack
- **Warmth model expert reference:** `memory/warmth_model.md` — v1/v2 flags, decoupled pull/sandbox, §9.4 disk feature gap, §10c warmth_v2 status
- **initialized_snapshot backfill (done 2026-06-08):** `scripts_cosim/backfill_initialized_snapshot.py` re-runs Phase 1 only (~0.5s/ds); writes SSC top-level `initialized_snapshot` from `schedulingStateCapture`; **1242/1242** ds in `artifacts/run_queue_big/gnn_datasets_4tasks_1060` — real variance (e.g. ds_00000: 37 warm / 171 cold of 208 platforms); preinit% in config drives warm count
- **is_warm edge attr:** hom `prepare_graphs_cache.py` uses `previous_task_type_name` (sandbox match); **seq recache still degenerate** — replica flag `has_dnn1/2` → always 1 on feasible edges; live `feature_builder.py` uses sandbox match — **must fix seq path before retrain**
- **`initialized_snapshot` SSC contract:** top-level key in `system_state_captured_unique.json` (preferred); fallback `task_placements[0]` for GNN-scheduler SSCs; `prepare_graphs_cache.load_extended_state_data()` reads both
- **`determined/scheduler.py` now passes `initialized_snapshot`** in `capture_task_placement()` for new co-sim runs
- **`load_all_datasets` bug (fixed 2026-06-08):** `initialized_snapshot` was read by `load_extended_state_data` but NOT forwarded into dataset dict — all cached graphs had `shared_fate_signal=0`; fix: add `'initialized_snapshot': extended_state.get('initialized_snapshot', {})` to the dict in `prepare_graphs_cache.py`
- **`graphs_cache_gnn_datasets_4tasks_1060` (dim-14 rebuild, 2026-06-08):** 1230 graphs (12 ds skipped — no `optimal_result.json`), 24,324,791 RTT rows, 122 chunks, v2 sidecar 731,683 combos (near=18,694 close=161,263 mid=209,870 far=205,909 trash=134,717); `shared_fate_signal` populated (mean=0.71); is_warm=0 throughout (Phase-1: no warm history); rebuild cmd `--trash-cap 192`
- **`near-rtt-v2-dim14-1060` (`models/near-rtt-v2-dim14-1060.pt`):** launcher `train_near_rtt_v2_dim14_1060.py`; cache `graphs_cache_gnn_datasets_4tasks_1060` (**14-dim**, `shared_fate` mean≈0.82); v2 trash-exp loss; best val top5 **0.063s** @ep67; test greedy **0.540s**, top5 **0.085s**; 7-config argmax sweep (`gnn_near_rtt_v2_dim14_1060_20260608/`): wins **4/7 vs dim14-ce** (`01`/`02`/`04`/`05`); `default` **11.62M** vs dim14-ce **4.14M**; qvm p95 **2857** vs dim14-ce **408**
- **dim14 cache (active GNN only):** `graphs_cache_gnn_datasets_4tasks_1060` — **1230 ds, 14-dim**, `shared_fate_signal`, `initialized_snapshot` backfill; do not load dim14 checkpoints on 13-dim caches
- **Key dim14 GNN checkpoints:**
  - **near-rtt-v2-dim14-ce-only** (`models/near-rtt-v2-dim14-ce-only.pt`, wandb `2n1dyc0t`): **GNN anchor**; `train_near_rtt_v2_dim14_ce_only.py`; CE-only; val acc **24.4%** @ep90; 7-config sweep `gnn_near_rtt_v2_dim14_ce_only_20260609/` — **7/7 vs Knative/HRC**; wins **3/7 vs dim14-full** (`default`/`00`/`05`); `default` **4.14M**, qvm p95 **408**
  - **near-rtt-v2-dim14-1060** (`models/near-rtt-v2-dim14-1060.pt`): CE+ranking v2; val top5 **0.063s**; wins dense **4/7 vs dim14-ce**; `default` collapse **11.62M**, qvm p95 **2857**
  - **Track B** (`near-rtt-v2-dim14-ce-init-regret*.pt`): CE init → low regret; **r030 rejected** post-fix 3cfg (+1.9% sum vs ce-only); r010 stopped ep17; **r002** next
- **dim14 CE-only 7/7 sweep (2026-06-09):** wins default/00/05 (3/7 vs dim14-full); loses 01/02/04 to dim14-full by 0.2–2.7%; **7/7 vs Knative/HRC**; qvm p95: default=408, 00=261, 01=139, 02=118, 03=161, 04=101, 05=144; sweep `gnn_near_rtt_v2_dim14_ce_only_20260609/`
- **Logit sharpness vs log-scale queue probe (2026-06-09):** temperature T=3 on dim14-full (probe baseline 13.84M) → 10.74M — insufficient; `GNN_LQB_LAMBDA=1.5` probe → **`default` 4.26M** (qvm p95 **122**); 7/7 LQB sweep **`default` 4.61M** — probe ≠ sweep; confirms ranking logits too sharpened for temperature alone
- **LQB λ=1.5 7/7 sweep (2026-06-09):** sum **18.55M vs CE 17.89M (+3.7%)**; wins **2/7** (`00` −0.2%, `01` −3.2%); loses `default` +11.2%, `05` +6.9%, `02`/`03`/`04` +0.6–3.1%; qvm p95 91–121 all configs; sweep `gnn_near_rtt_v2_dim14_1060_lqb15_20260609_100843/`; **rejected for deploy**
- **LQB λ tune probe default (2026-06-09):** λ=1.0 **4.30M (+3.8%)** best; λ=0.8 **4.64M (+12.0%)** — lower λ restores hot-spotting (p95 227); λ=1.0 sweep skipped; pivot Track B
- **Track B two-phase training:** Phase A = `near-rtt-v2-dim14-ce-only.pt` (done); Phase B infra shipped (`train_near_rtt_v2_dim14_ce_init_regret.py`, `run_dim14_two_phase_regret_train_and_sweep_nohup.sh`); `train_near_rtt.py` Phase B: greedy checkpoint, CE-init baseline seed, acc+greedy collapse guard; **r010 ramp stopped ep17** — greedy 2.33→1.14s best offline, top5 flat ~0.11s, acc 22–25%; **no live sweep**; partial ckpt deleted
- **Track B checkpoint gate (2026-06-09):** `train_near_rtt.py` Phase B default checkpoint metric now `val/regret_seq_reforward` (`NEAR_RTT_PHASE_B_CHECKPOINT_METRIC=seq_reforward_regret`): per-task argmax, increment selected platform queue, refresh platform feature dim7 `queue_depth` + dim13 `usage_ratio`, re-run GNN before next task; fail-loud on missing queue metadata; logs static greedy/top-k as diagnostics only
- **Track B first retry recipe:** `train_near_rtt_v2_dim14_ce_init_regret.py` default `NEAR_RTT_REGRET_WEIGHT=0.02` (`r002`), `NEAR_RTT_MARGIN_CAP=4.0`, `NEAR_RTT_REGRET_RAMP=0`, CE anchor retained; pipeline default tag updated to `r002`; still require live gate before accepting 40ep result
- **CE-only vs ranking train/serve:** CE = per-task CE on `data.y` ≡ live sequential argmax; ranking = `combo_score=Σlogits` margin pairs — optimizes joint combo order, not per-task greedy; offline `decode_greedy` on static graphs ≠ live queue roll-forward
- **Offline greedy not predictive:** dim14-ce test greedy **2.23s** but live `default` **4.14M** RTT; dim14-full test top5 **0.085s** but live `default` **11.62M**; do not trust static-val greedy/top-k alone for sparse deploy
- **Track B rethink options:** sequential val eval in train loop; 2-config live probe (`default`+`01`) per N epochs; constant regret 0.01–0.05 (no ramp); margin_cap 4.0; soft_combo path; accept CE-only 3/7 wins
- **dim14-ce roll-forward A/B (2026-06-09):** CE-only: **`argmax` ≡ `frozen` for placement** (both per-task `logits.argmax()`); roll-forward only increments `live_queues` for decode telemetry (`chosen_queue_vs_min` / qvm p95) — do not compare qvm across modes; RTT A/B: `default` +1.14%, `01` +0.30%, `03` −1.23%; roll-forward not consistent win
- **Option 1 inference parity (2026-06-09, shipped):** `gnn/scheduler.py` + `gnn_hetero/scheduler.py` — `_capture_full_queue_snapshot()` at batch start, `platform.queue_length()`, `_capture_temporal_state_snapshot()` wired into `_build_inference_graph`, p90 norm over all graph platforms; no retrain/recache
- **Option 1 3cfg gate sweep** (`gnn_near_rtt_v2_dim14_ce_only_opt1_frozen_3cfg_20260609_135559/`, seed 42): `default` 4.28M (+3.4% vs anchor argmax, +2.2% vs frozen_pre 4.19M), `02` 2.01M (+9.3%), `04` 1.80M (+7.7%); 3cfg sum +5.7%; qvm p95 `default` 262 vs anchor 408 (telemetry def differs) — **correct inputs, no deploy lift**
- **Post queue-map fix 3-model 3cfg sweep (2026-06-09, DONE):** Option 1 inference on all three checkpoints · argmax · seed 42 · `default`/`02`/`04` · sweep `dim14_3model_3cfg_queuefix_20260609/` · wall ~111min; **ce-only wins 2/3** (default 4.31M, 02 1.91M, 04 1.80M, sum **8.03M**); dim14-1060 wins `04` only (default **5.48M** vs pre-fix 11.62M −53%); r030 sum **8.18M (+1.9% vs ce-only)** — **rejected**; pre-fix 7/7 anchor RTTs are pre-Option1 — do not mix
- **Random + RoundRobin 7/7 weak baselines (2026-06-09, DONE):** sweep `random_rr_3cfg_20260609/` · wall ~28min · **CE-only 7/7 vs Random/RR/Kn**; Δ vs **pre-fix CE**: Random **+250–455%**, RR **+78–124%**, Kn **+25–36%**; Knative `default` from `baseline_default_100100/`; `random_network` = Knative stack + uniform random; fair RR = per-arrival `KnativeScheduler` subclass; Δ `(policy−ce)/ce` positive=worse
- **RoundRobin fair baseline fix (2026-06-09):** `roundrobin_network/scheduler.py` → `RoundRobinNetworkScheduler(KnativeScheduler)` RR placement; `simulation.py` `rr_network_rr_network` uses `KnativeNetworkOrchestrator`+`KnativeNetworkAutoscaler` (not legacy batch scheduler)
- **dim14 train objectives:** `ce_only` = per-task CE ≡ live sequential argmax; `ranking` = `CE + NearRttRankingLoss` on combo-sum Σlogits margin pairs; objective switch + Phase B sequential-reforward checkpoint in `train_near_rtt.py`
- **LQB decode mode (2026-06-09):** `GNN_LQB_LAMBDA=λ` env activates log1p blend: `Score=Logit − λ*log1p(queue)`; impl `logit_queue_blend_idx()` in `src/policy/gnn/seq_decode.py`; takes priority over seqblend/queue_filter in `decode_sequential_placement`; **rejected** — runtime topology fork, sum loses to CE
- **adaptive_nonzero queue norm mode (2026-06-09):** `GNN_QUEUE_NORM_MODE=adaptive_nonzero` (inference) + `--queue-norm-mode adaptive_nonzero` (cache build); fixes p90 collapse-to-1.0 when most platforms idle; `_scheduler_adaptive_queue_norm_nonzero()` added to `prepare_graphs_cache.py` + `scheduler.py`; requires cache rebuild to use as training feature — inference-only change has no effect on trained weights
- **train_near_rtt checkpoint path:** cwd `src/notebooks/` → saves `src/notebooks/models/{wandb_run_name}.pt`; copy to repo `models/` before sim (`GNN_MODEL_PATH`)
- **train.py loss:** α·CE + β·`StructuredRegretLoss` (hard-negative pools from hash; mostly catastrophic negatives → regret plateaus ~0.3s by epoch 3)
- **train_near_rtt.py loss:** α·CE + β·`NearRttRankingLoss` — stratified margin ranking on near-optimal RTT bands (not structured hard negatives)
- Replica-only decode: `build_allowed_placements_per_task` from valid combos (hash); decode + accuracy mask non-allowed; random neg in StructuredRegretLoss only from allowed → 100% hash hit (legacy unique pipeline same idea)
- Hash hit root cause (confirmed): model action-space (network+type compatible) >> simulated placements (replica-feasible); decoder can pick unsimulated combos → hash misses
- Task ordering mismatch disproven: 0/10380 datasets had mismatch; placement_plan task IDs are contiguous 0..n-1
- Cartesian completeness check: many 2/3-task datasets missing cross-combinations (not full cartesian over observed per-task choices); 4-task datasets mostly full cartesian
- Replica-limited edges added to `src/notebooks/non_unique/prepare_graphs_cache.py` (CACHE_VERSION=4.1); cache must be regenerated for effect
- Collapse detection: val acc <5% or hash hit <10% → revert to best checkpoint, reduce LR; never save best when collapsed
- gnn_datasets_4tasks generated with UNIQUE replicas only (default allow_non_unique_replicas=False in generate_gnn_datasets_fast.py)
- generate_non_unique_placements_fast.py: adds missing non-unique (cartesian - unique) placements to existing datasets; skips ds_02485-ds_03211 (missing); progress log compact format: `e=existing n=new t=total rtt=best`
- Label/edge-order bug fixed in non-unique graph cache: sort compat_plats before edge/label construction and use to_undirected with edge_attr to keep CE targets aligned with logits
- GNN training stability tweaks: removed per-task logits clamp in 24-01-15-16_non_unique.py and changed LEARNING_RATE=5e-4, BATCH_SIZE=32 to reduce loss/accuracy spikiness
- plot_policy_comparison.py: result JSONs can be 2GB+; read only first 8KB and regex-extract policy/workload_file/total_rtt; discover workloads from disk; filenames simulation_result_.json; charts under simulation_data/results//policy_comparison_.png
- 24-01-15-16_non_unique.py: USE_MERGED_CACHE=False → CACHE_DIR=graphs_cache_gnn_datasets_4tasks (4-task only); USE_MERGED_CACHE=True → merged 2+3+4
- OffloadNetworkScheduler: subclass of KnativeScheduler that never executes on client_node*; only uses reachable server nodes (via network_map) and keeps knative_network’s shortest-queue selection
- Real simulation path (prepare_infrastructure_for_real_simulation) ignores preinit/replicas/prewarm from space_with_network.json; all policies, including HRC, start cold with autoscaling from zero
- Non-unique training (`src/notebooks/train.py`): preload `rtt_chunk_*.pkl` hash at startup; `build_regret_training_lookups` precomputes hard-negative pools + placement→logit maps once; epochs use O(1) `random.choice` + hash eval lookup; lightweight `GraphRttDataset` (no per-sample valid_combos)
- `prepare_graphs_cache.py` (CACHE_VERSION 5.2): builds chunked `(dataset_id, combo)->rtt` hash (`rtt_combos_backend=hash_table_chunked`); removes legacy LMDB/single-file artifacts
- `cache_io`: `load_rtt_hash_table_from_cache`, `build_valid_combos_map`, `build_regret_training_lookups`
- Graph features: sanitize JSON/priors divisions in `prepare_graphs_cache.build_graph`; `TaskPlacementGNN` forward NaN clamps removed—bad inputs fail at cache bake
- Grid search loading: load graphs (+ shared LMDB path for combos) once, loop configs single process to avoid repeated loads
- Orchestrator stats contract now exposes `statsSchemaVersion`, `taskResultsIncluded`, and `taskResultsOmittedReason`; `taskResults`/`applicationResults` retained for small-task runs (`num_tasks <= 20`)
- New analysis pipeline entrypoint: `src/analysis/sim_gap_analysis.py` writes `task_metrics_all.csv`, `metric_summary.csv`, `divergence_by_metric.csv`, and `summary.json` for co-sim vs full-sim comparisons
- prepare_graphs_cache now hard-fails on missing/empty `stats.taskResults` (strict mode) and exports `task_metrics_analysis.csv` for downstream gap analysis
- Co-sim is snapshot-based (2-5 task placement decision point); workload duration is not a primary tuning lever for snapshot fidelity
- Real-vs-co-sim calibration showed strongest mismatch in queue depth and offloading ratio; network latency/communications are much closer
- `NUM_CLIENT_NODES=10` currently matches observed real-sim source-node coverage (`client_node0..client_node9`); raising to 20 adds cost without current evidence benefit
- Virtual warmup backlog optimization added: avoid per-warmup Task object creation when fast-forward is enabled; expose `Platform.queue_length()` (real queue + virtual backlog)
- Benchmark (`ds_03001`, 4-task, workers=8): fast-forward/compressed path ~~445.6s vs no-fast-forward projected ~10200s (~~23x slower)
- Co-sim speedup parity issue confirmed: fast-forward warmup changed RTT labels on sampled datasets (5-case A/B had non-zero deltas before fix)
- Truthful+fast warmup fix (current): in `src/placement/simulation.py` prewarm path always materializes internal warmup tasks; fast mode skips execution in `Platform.platform_process()` via aggregate timeout
- Warmup parity fix (current): `src/placement/infrastructure.py` fast-forward warmup timing now honors existing `previous_task` warm state and removed forced first-task cold-start override
- Validation (2-case A/B): `simulation_data/ff_ab_test_fast_truthful_2cases_v3/summary.json` shows exact RTT ties with ~1.2x runtime speedup for fast-forward ON vs OFF
- Next perf hotspots agreed: cache deterministic infra per worker in `src/executecosimulation.py` (`_init_worker`/`process_placement_fast`), gate `src/policy/determined/scheduler.py` debug prints behind env flag, reduce verbose prints in `generate_brute_force_placement_combinations`
- Non-unique cache prep script moved to `src/notebooks/prepare_graphs_cache.py`; invoking old path `src/notebooks/non_unique/prepare_graphs_cache.py` fails
- `prepare_graphs_cache.py` default `--project-root` must resolve to repo root (`parents[2]`) for no-arg run compatibility
- Pipenv caveat: adding packages can fail `pipenv lock` on `nvidia-nccl-cu1`; install `lmdb` with `pipenv run pip install lmdb` if lock breaks
- Sim OOM mitigations (large workloads): `GNN_CAPTURE_DATASET_STATE=0` default; auto low-memory stats (`initial_event_count>=10000`); `slim_completed_task()`; `_stats_low_memory()` single-pass aggregates; peak RAM 150-100 ~4GB vs ~23GB before
- Inference GNN model (`src/policy/gnn/gnn_model.py`) uses `MLPEncoder` (`task_encoder.net.*`) to load `desert-galaxy-26.pt`; old `TaskEncoder` (`fc1/norm1/fc2`) checkpoints incompatible without remap
- GNN vs knative 150-150 (450729 tasks): old desert-galaxy-26 +17.3% total RTT vs knative; seqblend inference −14.3% vs knative (20.36M vs 23.76M s); avg queue 45.1 vs 52.7 s; result `simulation_result_gnn_seqblend_150-150.json`
- GNN vs knative root cause: co-sim labels are joint brute-force optima (0% match per-task min-queue); inference used frozen-snapshot independent argmax → hot-spotting; knative is sequential online min-queue (better online algo)
- GNN inference fixes (scheduler.py): scheduler_adaptive queue norm; sequential decode — if GNN argmax queue > live min-queue pick min; increment live queue per task in batch; default model desert-galaxy-26.pt
- Large-sim snapshot gating: `knative_network` + `herocache_network` schedulers only capture full_queue_snapshot when `GNN_CAPTURE_DATASET_STATE=1` (matches GNN scheduler)
- 150-150 GNN seqblend run: peak RSS ~2.5GB; streaming stats (`v2_streaming`, taskResultsIncluded=false); wall ~12 min
- Fair HRC 150-150 baseline done (kn-autoscale); legacy HRC deprioritized for comparisons
- `executesimulation.py` uses `QUEUE_LENGTH=100`, `task_priority=fifo` for all policies (upstream HRC uses `least_penalty`)
- `train.py` sequential eval decode (done): `decode_frozen_placement`, `decode_sequential_placement(use_seqblend=)`; metrics `regret`, `regret_seq`, `regret_seqblend`; wandb logs all three; checkpoint still on frozen `val/regret`
- `cache_io.enrich_graphs_with_decode_metadata`: loads `queue_snapshot` + `task_logit_to_queue_key` from dataset dirs; sidecar `decode_metadata.pkl` in cache dir; node names from `optimal_result.json` config.infrastructure.nodes
- `prepare_graphs_cache.build_graph` now stores `queue_snapshot` + `task_logit_to_queue_key` on new graphs (existing caches need sidecar or regen)
- Seq argmax ≡ frozen in eval (expected): queue updates don't affect argmax path; only seqblend uses live queues for pick override
- `pleasant-mountain-28.pt` (100 ep, 4-task cache): test frozen/seq regret 0.282s (~25%), seqblend 1.369s (~88%); graph acc ~11%; wandb run `3efsrf5h`
- Offline hash-table eval: seqblend ~5× worse than raw argmax — labels favor exec/network optimum, not min-queue spreading
- Infra scaling: replicas ∝ `per_client×clients + per_server×servers`; more nodes + fixed workload → lower utilization → queues less differentiating IF prewarm moderate; brute-force co-sim cost grows with replica count; train/sim utilization must match
- Seq argmax default live decode: `GNN_DECODE_MODE=argmax` (train/eval parity)
- Seqblend+1 live decode: `GNN_DECODE_MODE=seqblend` + `GNN_SEQBLEND_QUEUE_MARGIN=1` → override min-queue only if `live_queue(gnn) > min_queue(candidates)+1`; margin=0 = classic seqblend; impl `src/policy/gnn/seq_decode.py`; runner `scripts_cosim/run_gnn_seqblend_p1_150150.sh`; stats → `SeqblendDecodeStats` + sidecar `*.decode_stats.json` + `result_summary.decode_stats`
- 150-150 full sim RTT (seed 101, 450729 tasks, `simulation_data/results/150-150/`): classic seqblend 20.36M (−14.3%); seqblend+1 dg-26 21.91M (−7.8%); Knative 23.76M; **fair HRC (kn-autoscale) 24.94M (+4.9%)**; GNN argmax dg-26 27.88M (+17.3%); brisk-41 28.57M; zesty 66.48M; **legacy HRC 166.87M (+602%)** — autoscale bug not placement
- HRC +602% root cause: old `herocache_network` autoscaler (no `source_node_name`, client replica churn, 6s rolling scale signal) vs scheduler co-sim ~2s regret; fix = knative-equivalent autoscaler + orchestrator monitor in `herocache_network/{autoscaler,orchestrator}.py` + scheduler passes `source_node_name`
- Fair HRC 150-150: scale-downs 298 vs Knative 313; sim time 672s vs legacy 3124s; wall ~97 min vs Knative ~6 min (HRC placement scoring CPU-heavy)
- 4-task batch co-sim audit (177 snap): HRC 2.01s avg regret vs Knative 5.67s (146/177 HRC wins) — live fair HRC still +4.9% RTT (joint load + online effects)
- Seqblend+1 dg-26 decode stats (444582 steps): p1 override 32.3%; classic would 55.7%; classic-only (p1 kept GNN at min+1) 23.4%; on p1 override gnn_q mean 358→final 352 (saved ~6); classic-only gnn_q mean 475
- Canvas: `.cursor/projects/root-projects-my-herosim/canvases/150-150-policy-comparison.canvas.tsx` — RTT table/chart + seqblend+1 override analysis
- Filtered seq cache (`graphs_cache_gnn_datasets_4tasks_seq_filtered`, 883 ds `avg_queue≥0.5s`) + `TRAIN_STRATIFIED_NEGATIVES=1` + `build_valid_combos_map(dataset_ids_filter)` → `train_seq_exp.py`; model `brisk-cosmos-41.pt`
- Live 150-150 (no seqblend): Knative 23.76M RTT; desert-galaxy-26 GNN 66.48M; brisk-cosmos-41 GNN 28.57M (+20% vs Knative); seqblend-era GNN 20.36M (deprecated path)
- Live oracle audit: `LIVE_AUDIT_SNAPSHOT_PATH`, `LIVE_AUDIT_MAX_SNAPSHOTS`, `LIVE_AUDIT_HORIZON`, `LIVE_AUDIT_STRIDE` on `knative_network` scheduler; analyzer `scripts_cosim/live_snapshot_oracle_audit.py`
- Horizon-1 audit (500 snap): median 1 candidate; oracle=Knative=GNN regret 0; queue longest but no placement choice
- Batch audit (`knative_network_batch`, 490 snap): 122 multi-task batches; oracle non-shortest 17%; Knative regret ~0.99, HRC-like ~0.01, GNN ~0.43; regret mostly queue then network
- `knative_network_batch`: `src/policy/knative_network_batch/scheduler.py`; strategy `kn_network_batch_kn_network_batch`; env `KNATIVE_BATCH_SIZE` (4), `KNATIVE_BATCH_TIMEOUT` (0.002); reuses Knative shortest-queue per task in batch
- Batching is not root failure: exposes joint placement signal; horizon-1 starvation (0–1 candidates) is; train/serve mixes sequential Knative baseline with batched GNN deploy
- Co-sim queue regime gap vs live 150-150 (**verified 2026-06-10**, `verify_queue_gap_1060.py`): metric = `averageQueueTime`; **1060** max **11.54s** (p99 **5.48s**), per-task queueTime max **18.4s**, snapshot depth max **98 tasks**; **3705** max **6.50s**; live 150-150 Kn **52.7s** (GNN 45–63s); live default dim14-ce **20.6s**; avgQT scale **~4.6–8×** (not ~200× — that was qvm p95 ~2857 vs CE ~408); fast-forward warmup A/B on 1060: RTT/avgQT identical ff on/off
- `executesimulation.py`: `GNN_MODEL_PATH` env for checkpoint; policies include `knative_network_batch`
- GNN load for audit/inference: `TaskPlacementGNN(embedding_dim=64, hidden_dim=64, num_layers=3)` — must match training checkpoint
- `train_seq.py`: `EXTRA_CACHE_DIR` merges second seq cache at train time; default skips extra RTT hash (CE-only for extra graphs — avoids OOM); `EXTRA_CACHE_LOAD_RTT=1` + `dataset_ids_filter` in `cache_io.load_rtt_hash_table_from_cache` for filtered RTT load
- Overnight highq seq cache: `graphs_cache_overnight_highq_seq` — 1710 ds (lambda≥40) from `gnn_datasets_4tasks_overnight_260422` → 8535 seq graphs (v6.3); merged with `graphs_cache_gnn_datasets_4tasks_seq_filtered` (4396) = 12931 train graphs
- Combined train model `models/good-plasma-43.pt` (wandb `good-plasma-43`): init `brisk-cosmos-41.pt`, 15 ep; val regret 0.63s / acc 51.9% vs brisk val 0.69s
- GNN audit rescore (fast): reuse baseline CSV `oracle_rtt`; run `choose_gnn` + one `policy_rtt_cosim` per snapshot per model — do NOT rerun oracle brute force (~10s for 177 snap)
- Live audit rescore (177 snap): HRC 2.01s, Knative 5.67s, GNN brisk 5.81s, GNN good-plasma 5.69s (3 oracle ties); train val gain ≠ live audit gain
- HRC near-oracle in batch co-sim audit: greedy per-task score ≈ single-task co-sim cost; misses joint batch queue contention; remaining HRC regret = cross-task effects
- Audit `choose_gnn` mismatch vs live GNN scheduler: fixed in `gnn_snapshot_inference.py` — full-infra graph + sequential argmax + queue roll-forward from `full_queue_snapshot`
- WandB seq train misleading curves: `val/acc` mixes prefix steps (1 CE target) with final (all tasks); `train/regret_loss` averaged CE-only batches to ~0; `wandb.watch` gradient noise; EXTRA_CACHE CE-only → `val/regret_coverage` <<1. Fixed: log `val/acc_final`, `val/per_task_acc_final`, `val/regret_coverage`, `train/*_final`, regret avg over regret steps only; `WANDB_WATCH=0` default
- Co-sim train vs live 500-batch gap: queue med ~26 train filtered vs ~60 live; cold-start metrics **re-audit pending** (prior ~28%/~88% unverified; see `audit_doc_claims.py`); 76% datasets dropped by seq cache filter (3705→883 base)
- `slim_completed_task()` bug (fixed): cleared `full_queue_snapshot`/`temporal_state_at_scheduling` at task completion before `stats()` → all co-sim exports had null snapshots; now only `system_state_snapshot` slimmed
- `schedulingStateCapture` (DeterminedScheduler batch start): top-level SSC fields (replicas, available_resources, scheduler_state, timestamp) must not use end-of-sim `systemStateResults[-1]`
- `build_system_state_captured()` in `executecosimulation.py`: shared SSC writer; per-task adds `full_temporal_state_at_scheduling` (all platforms); `prepare_graphs_cache` prefers full temporal when present
- `scripts_cosim/refresh_optimal_full_stats.py`: `--repair --force` re-runs optimal placement + writes SSC; `--rewrite-ssc` from optimal only; normalizes `placement_plan` int keys + `forced_placements`
- `gnn_datasets_4tasks_1060`: **1139** ds; `refresh_optimal_full_stats.py --repair` → audit **ok: 1139** (1 skip_no_optimal); real `full_queue_snapshot` **208 platforms** vs old infra fallback ~60 keys
- **prepare_graphs_cache\*** strict SSC: no `infrastructure.json` / empty-queue fallback; raises on missing/incomplete `system_state_captured_unique.json`; near-RTT safe-patch removed
- **graphs_cache_gnn_datasets_4tasks_1060** (near-RTT variant, rebuilt 2026-06-07): 1139 graphs, **14.4M** RTT rows; v1 sidecar **518,726** combos; v2 sidecar **613,125** combos (far 173k, trash 112k) — used by **ssc-trash-1**, not woven-totem
- **graphs_cache_gnn_datasets_4tasks_1060_scheduler_adaptive**: **1230** graphs (rebuilt 2026-06-08), **~24.3M** RTT rows, CACHE_VERSION 5.2 — **woven-totem-52**, **ethereal-lake-8**, **near-rtt-v2-clean-1230**; v1 sidecar 612k / v2 sidecar **731k** (trash-cap 192); hetero bake → `…_hetero` subdir
- **train_near_rtt.py** default wandb project: `gnn-near-rtt-jun2026` (override via `--wandb-project`)
- **worthy-bush-5** cache used infra-fallback queues; **fresh-microwave-6** (`a27rbw00`) retrained same recipe on repaired cache, stopped early ~ep40 — best val top5 **0.0556s** @ep33; greedy still ~8s
- **Near-RTT pipeline files:** `src/notebooks/train_near_rtt.py`, `train_near_rtt_v2.py`, `train_near_rtt_v2_ce_only.py`, `train_near_rtt_v2_dim14_1060.py`, `train_near_rtt_v2_dim14_ce_only.py`, `prepare_graphs_cache_near_rtt.py`, `build_capped_near_rtt_sidecar.py`; sidecar in `non_unique_lib/cache_io.py`
- **NearRttRankingLoss v1 bands (ΔRTT vs optimal):** near (0–0.05s, w=3), close (0.05–0.30s, w=2), mid (0.30–1.0s, w=1), far (>1.0s, w=0.25); `pairs_per_graph`≈32; margin = clamp(ΔRTT/`RTT_SCALE`, floor 0.05, cap 1.0)
- **NearRttRankingLoss v2:** `loss_variant=near-rtt-v2-trash-exp`; far 1–5s (w=0.75), trash >5s (w=1.0), exp margin (`cap=8`, `scale=0.75`, `clip=4`), `NEAR_RTT_UNMAPPED_PENALTY=8.0`
- **near-rtt-v2-ssc-trash-1** (`05il3atn`, `models/near-rtt-v2-ssc-trash-1.pt`): cache `graphs_cache_gnn_datasets_4tasks_1060` + v2 sidecar; best val top5 **0.0253s**; final test greedy **9.2843s**, top5 **0.0879s**, oracle_top5 **0**, sidecar hit **40.4%**; 7-config live (`gnn_near_rtt_v2_ssc_trash_1_20260607/`): wins **2/7** (`02`,`04`), beats dg-26 on **3/7**, Knative **5/7**; `default` 11.17M (+143% vs dg-26), `00` 6.98M (+121%)
- **Capped near-RTT sidecars:** v1 on 1230 cache **612,286** combos; v2 on 1230 cache **731,683** (far 206k, trash 135k); legacy 1060 v2 **613,125**; full combos via `NEAR_RTT_ALLOW_FULL_COMBOS=1` OOM risk
- **prepare_graphs_cache_near_rtt.py:** wraps `prepare_graphs_cache.py` + builds **capped** sidecar; `--skip-cache-build`; `--full-combos` optional; strict SSC (no infra fallback)
- **train_near_rtt eval decodes:** `decode_greedy` (per-task argmax), `decode_topk_joint` (cartesian top-k per task, pick best additive score), `oracle_topk` (best RTT within top-k pool — measures candidate quality not model ranking)
- **train_near_rtt metrics:** `regret_greedy`, `regret_top{k}`, `regret_oracle_top{k}`, `rank`, `active_pair_frac`, `greedy_sidecar_coverage`, `greedy_unmapped`; unmapped greedy → `max(worst_sidecar_regret, NEAR_RTT_UNMAPPED_PENALTY)`; env `NEAR_RTT_TOP_K`, `NEAR_RTT_PAIRS_PER_GRAPH`, `NEAR_RTT_*_WEIGHT`, `NEAR_RTT_MARGIN_*`, `NEAR_RTT_SIDECAR_NAME`
- **silvery-sun-4** (`graphs_cache_gnn_datasets_4tasks` ~3705 + capped sidecar, near-RTT v1): test `greedy_regret` ~0.13s, `top10_regret` ~0.14s, `oracle_top10` ~0 — broke ~0.3s plateau offline; **live RTT worse than desert-galaxy-26** (train/serve: frozen greedy/top-k eval vs live sequential argmax + SQ fallback for batch ∉ [2,4])
- **worthy-bush-5** (`train_near_rtt.py`, 1060, 100ep, wandb `jf1j32o8`): best val top10 **0.0378s** @ep90; test top10 **0.051s**; test greedy **6.15s** (penalty, 39% sidecar hit); oracle top10 **0**; graph acc 13.2% test / 24.5% val; learning 3.04s→0.038s top10 by ep90; `active_pair_frac`→0.88 plateau
- Near-RTT live deploy: use `frozen_topk` not argmax; honest greedy metric ≠ silvery-sun-4 ~0.13s (old eval skipped unmapped)
- `custom_collate` + `num_workers=4` verified on 1060 cache sample (`OK batch num_graphs=32`)
- **desert-galaxy-26** trained on **`graphs_cache_gnn_datasets_4tasks`** (~3705 ds) — not 1060; default inference checkpoint (`GNN_MODEL_PATH`); 7-config sweep baseline for homogeneous GNN
- 1060 `train.py` run **woven-totem-52** on **`graphs_cache_gnn_datasets_4tasks_1060_scheduler_adaptive`** (CE+StructuredRegret β=0.3, dg-26 hyperparams); log `logs/train_1060_dg26_recipe.log`; avoid `import train` (triggers full 3705 default run)
- Normal sim 7-config sweep (`workload-100-100`, 201076 tasks): default 20×20 p0.5 + 6 variants in `simulation_data/normal_sim_sweeps/`; runners `run_normal_sim_config_sweep.py`, `run_hrc_all_configs_nohup.sh`, `run_gnn_silvery_sun_4_all_configs_nohup.sh`
- `scripts_cosim/run_simulation.py` accepts `--config` for per-scenario `space_with_network.json`; GNN checkpoint via `GNN_MODEL_PATH` (default `models/desert-galaxy-26.pt`)
- 7-config policy RTT (total): **desert-galaxy-26 GNN wins 6/7** vs Knative + fair HRC; **HRC wins only** `sparse_40_40_p25`; GNN most dominant on `balanced_40_40_p50` (+17.5% vs 2nd-best Knative)
- silvery-sun-4 vs desert-galaxy-26 (same 7 configs, GPU): **dg-26 wins 7/7**; avg +163% RTT for sun-4; worst gaps on small topologies (30×30 +360%, 20×20 +334%); closest 50×50 +40%
- Normal sim wall times (GPU GNN ~6–7 min/run; HRC ~11–14 min/run); bash nohup scripts need CRLF strip (`sed -i 's/\r$//'`) on Windows-saved `.sh`
- Policy comparison guide: `memory/compare.md` — **active top:** bipartite v1 · dim14-ce · dim22 MLP · skew-4 · triangle; **archive:** tiered-hub symmetric · dim14-full/r030/LQB/Option1/Random+RR; `/compare` + datalab handover in `.cursor/commands/compare.md`
- **woven-totem-52** normal sim 7-config sweep done (`gnn_woven_totem_52_20260607/`): **wins 4/7** (02–05 incl. sparse); dg-26 wins default+00+01; baseline default RTT 6.36M (+38% vs dg-26 4.60M, +14% vs Knative); beats dg-26 on 4/7 configs
- **GNN_DECODE_MODE** live decode (`src/policy/gnn/seq_decode.py`): `argmax` (default) and `frozen` = **same per-task argmax picks for CE-only**; argmax roll-forwards queues for telemetry only; `frozen_topk` = joint top-k combo search (`GNN_DECODE_TOP_K`, default 10)
- **GnnDecodeRunStats** on every GNN sim: sidecar `*.decode_stats.json` + `result_summary.decode_stats` — decode_time_ms, combo_search_size, intra_batch_platform_collisions, chosen_queue_vs_min; impl `run_decode_with_timing()` in seq_decode.py
- **worthy-bush-5 decode ablation** (`default_20_20_p50`, `gnn_worthy_bush_5_decode_ablation_20260607_123215/`): argmax 6.98M RTT best; frozen +11.7%; frozen_topk +12.4%; all +51–70% vs dg-26 — decode mode ~12% spread, model quality dominates; ~20% GNN batches have intra-batch platform collisions
- **Near-RTT unmapped combo penalty:** v1 default `1.0s`; **v2/ssc-trash-1** `NEAR_RTT_UNMAPPED_PENALTY=8.0`; sidecar bands near ≤0.05s, close ≤0.30s, mid ≤1.0s, far 1–5s, trash >5s — live sim queue gaps (tens of s) far outside training envelope on baseline topologies
- frozen_topk decode CPU ~2.6ms/batch (batch-3) vs argmax ~0.17ms — negligible vs RTT; worthy-bush topk wall 481s vs argmax 344s on default
- Scripts: `run_dim14_ce_only_train_and_sweep_nohup.sh`, `run_dim14_two_phase_regret_train_and_sweep_nohup.sh`, `run_gnn_near_rtt_v2_dim14_ce_init_regret_all_configs_nohup.sh`, `run_gnn_near_rtt_v2_dim14_ce_only_all_configs_nohup.sh`, `run_gnn_near_rtt_v2_dim14_1060_all_configs_nohup.sh`, `run_gnn_woven_totem_52_all_configs_nohup.sh`, `run_gnn_near_rtt_v2_ssc_trash_1_all_configs_nohup.sh`, `run_gnn_near_rtt_v2_clean_1230_all_configs_nohup.sh`, `run_gnn_near_rtt_v2_clean_1230_seqblend_m2_all_configs_nohup.sh`, `run_gnn_near_rtt_v2_clean_1230_ce_only_all_configs_nohup.sh`, `run_gnn_ethereal_lake_8_all_configs_nohup.sh`, `run_gnn_worthy_bush_5_frozen_topk_all_configs_nohup.sh`, `run_worthy_bush_5_decode_ablation_default.sh`
- **clean-1230 argmax collapse (small topologies):** offline strong but live `chosen_queue_vs_min` p95 **4217** on `default` — catastrophic argmax hot-spotting; co-sim snapshot depth max **98 tasks** vs live qvm p95 **~2857–4217** (decode-time task-count, not avgQueueTime); v2 ranking sharpens near-band logits; **seqblend m2** or **CE-only** fixes without full retrain on same cache
- **clean-1230 seqblend m2 sweep** (`gnn_near_rtt_v2_clean_1230_seqblend_m2_20260608/`, margin **2**, seed 42): `default` **4.10M** (−74% vs argmax, −11% vs dg-26); dense configs +1–7% vs argmax; wins dg-26 **6/7**; `default` qvm p95 **2**, collision **10.1%**
- **clean-1230-ce-only live RTT (M, argmax, seed 42):** default **4.34**, 00 **3.07**, 01 **2.20**, 02 **1.87**, 03 **2.46**, 04 **1.71**, 05 **2.62** — beats dg-26 all 7; vs full clean-1230 argmax wins **5/7** (loses `02`/`05` by ~3–4%)
- **near-rtt-v2-clean-1230 live RTT (M, argmax, seed 42):** default **15.76**, 00 **4.90**, 01 **2.25**, 02 **1.80**, 03 **2.47**, 04 **1.74**, 05 **2.56** — vs dg-26 wins 01–05 only
- **train_near_rtt.py CE-only path:** `CE_ONLY_TRAINING` when `regret_loss_weight=0` — skips ranking forward; checkpoints on **val acc**; launcher `train_near_rtt_v2_ce_only.py`
- **train_near_rtt.py objective switch:** `NEAR_RTT_TRAIN_OBJECTIVE=ce_only|ranking|soft_combo|soft_combo_conc`; soft-combo modes checkpoint on **val regret**, use capped `EXACT_RTT_MAP`, and preserve existing CE-only/ranking behavior
- **Soft combo objective files:** `non_unique_lib/soft_combo_loss.py`, `train_near_rtt_v2_soft_combo.py`, `train_near_rtt_v2_soft_combo_conc.py`; `S(combo)=Σ logits[t][combo[t]]`, target `P*(combo)∝exp(-regret/τ)`, defaults `τ=0.25`, `max_combos=4096`; launchers use CE anchor `ce_loss_weight=1`, unanchored soft-combo requires `NEAR_RTT_ALLOW_UNANCHORED_SOFT_COMBO=1`
- **Concentration penalty:** `soft_combo_conc` adds `γ·L_concentration` with defaults `NEAR_RTT_CONC_GAMMA=0.02`, `NEAR_RTT_CONC_CAP=1.5`; key must be physical queue `node_name:platform_id`; requires `queue_key_to_platform_meta` and fails loudly when missing/incomplete
- **Queue metadata contract:** homogeneous graph caches now store `queue_key_to_platform_meta` keyed by `node_name:platform_id` with `platform_type`, `target_concurrency`, `node_name`, `platform_id`, `node_id`; added to `prepare_graphs_cache.py`, `prepare_graphs_cache_exact_rtt.py`, `prepare_graphs_cache_seq.py`, and live `src/policy/gnn/scheduler.py`
- **clean-1230 cache regen after metadata fix:** active cache `graphs_cache_gnn_datasets_4tasks_1060_scheduler_adaptive` regenerated and verified: 1230 graphs, 1230 ids, v2 sidecar 731,683 rows; v2 sidecar rebuilt with `--trash-cap 192` to preserve expected trash reservoir shape
- **Soft-combo 5-config sweep subset:** run `default`,`01`,`02`,`03`,`05`; skip `00` and `04`; scripts: `run_gnn_near_rtt_v2_soft_combo_5cfg_{argmax,frozen_topk}_nohup.sh`, `run_gnn_near_rtt_v2_soft_combo_conc_5cfg_{argmax,frozen_topk}_nohup.sh`; shared runner `run_gnn_near_rtt_v2_5cfg_sweep_common.sh`
- **memory/compare.md** updated 2026-06-09: Random+RR 7/7 weak baselines; Δ convention `(policy−ce)/ce`; fair RR scheduler note; result path `random_rr_3cfg_20260609/`
- **Hetero plan:** [Hetero GNN Pipeline](/root/.cursor/plans/hetero_gnn_pipeline_4e0b40c7.plan.md) — parallel `gnn_hetero` path; do not mutate `src/policy/gnn` or `train_near_rtt.py`
- **HeteroData pipeline files:** `src/policy/gnn_hetero/{data.py,gnn_model.py,scheduler.py}`, `src/notebooks/prepare_graphs_cache_hetero.py`, `prepare_graphs_cache_near_rtt_hetero.py`, `train_hetero.py`, `train_hetero_v2.py`
- **train_hetero.py fix:** removed dead homogeneous `TaskPlacementGNN`/GIN block; trainer uses imported `HeteroTaskPlacementGNN` + `HeteroData` types only; hom checkpoints (`desert-galaxy-26`, etc.) fail `load_state_dict` (missing `convs.*`/`HeteroConv`)
- **Hetero model contract:** manual `HeteroConv` over `('task','can_run_on','platform')` + reverse relation; `forward(HeteroData)->List[Tensor]`; per-task logit order must match `task_logit_to_placement`
- **Hetero sim wiring:** policy `gnn_hetero`, strategy `gnn_hetero_gnn_hetero` (registered in `scheduling_strategies`), env `GNN_HETERO_MODEL_PATH`; `GNN_DECODE_MODE`/`GNN_DECODE_TOP_K` shared with hom via `gnn_hetero/seq_decode.py`
- **Hetero cache** `graphs_cache_gnn_datasets_4tasks_1060_scheduler_adaptive_hetero`: 1060 `HeteroData` graphs; v1 sidecar 467,620 combos; **hetero v2 sidecar** `valid_combos_near_rtt_v2_capped.pkl` **520,902** combos (trash-cap 128 → **70,535** trash rows)
- **soft-forest-1** hetero v1 (`models/soft-forest-1.pt`, `gnn-hetero-jun2026`): near-rtt v1 sidecar; best val top5 **0.0065s**; live default argmax **13.6M** RTT, collision 14.3%
- **hetero-v2-trash-regime** (`models/hetero-v2-trash-regime.pt`, `gnn-hetero-v2-jun2026`): `train_hetero_v2.py` — v2 trash-exp loss, `margin_cap=15`, `unmapped_penalty=15`, `trash_weight=2`; best val top5 **0.0071s**; test greedy **11.51s**, top5 **0.0186s**; live default **`frozen_topk` k=10** **10.54M** RTT (`simulation_result_gnn_hetero_v2_frozen_topk.json`), collision **15.9%**, combo_search mean **5125**, decode **2.77ms**/batch
- **Two-regime tabular benchmark (2026-06-09):** **Regime A** = batch loop (GNN peer): 2ms collect + sequential argmax + queue roll-forward; **Regime B** = per-arrival loop (Knative/HRC peer): one task per wakeup, no batch delay
- **Regime A XGBoost files:** `src/policy/tabular/{constants,graph_extraction,train_ranker,scheduler,orchestrator}.py`, `src/notebooks/prepare_tabular_dataset.py`; policy `xgboost_batch` → strategy `xgb_batch_xgb_batch`; env `XGB_MODEL_PATH`
- **Regime A v1 artifacts (2026-06-09):** cache `graphs_cache_gnn_datasets_4tasks_seq_tabular_v65`; models `batch_edge_ranker.json` (64.4%) + `batch_edge_mlp.pt` (67.8% val edge acc); 30k smoke `regime_a_compare_20260609/` (669.8k / 886.9k / 1.62M)
- **Skew-4 dim14 vs dim22 MLP (2026-06-10):** configs in `atomic21_skew_configs/`; `INFERENCE_FEATURE_LAYOUT=dim22`; scripts `run_dim14_old_models_skew4_sweep.sh`, datalab `dim14_old_models_skew4.sbatch` + `run_dim14_old_models_skew4_one.sh`; env `SWEEP_DIR` + `WORKLOAD` override
- **Skew-4 100-100 (local):** `dim14_old_models_skew4_20260610/results/` — GNN wins **2/4** (degree-skew only); MLP wins uniform
- **Skew-4 125-225 (datalab):** `dim14_old_models_skew4_125225_20260610/results/` — 8× GNN/MLP + 4× Knative (job 477953); sums GNN 44.0M · MLP **32.7M** · Knative 41.2M; wins GNN 2/4 · MLP 2/4 · Kn 0/4; GNN uniform RTT explodes (30.7M default) while skew ~1.2–1.8M
- **workload-125-225:** `pipenv run python3 -m src.generator -d data/nofs-ids --generate-traces -r 125 -s 225` → `data/nofs-ids/traces/workload-125-225.json` (562k events, ~151MB); **not** legacy `src/legacy/.../workload-125-225.json` (139k, `node0`)
- **Skew-4 Knative datalab:** `run_skew4_knative_one.sh` + `skew4_knative.sbatch` (array 0–3); output `{cfg}_knative_network.json`; CRLF fix required before `sbatch`
- **Tiered-hub sweep (2026-06-10, datalab, 100-100 seed 42):** `tiered_hub_gnn_mlp_20260610/` — 9 configs (`hub_k{2,4,6}_seek{30,50,80}`, `degree_skewed_core` via `generate_tiered_hub_configs.py`); policies gnn_dim22 · gnn_atomic21 · mlp_dim22 · mlp_atomic21 · knative; models `near-rtt-v2-dim14-ce-only.pt`, `batch_edge_mlp.pt`, `batch_edge_mlp_atomic21.pt`; `GNN_DECODE_MODE=argmax`; SLURM `tiered_hub_gpu.sbatch` (477929, 36/36 ML **DONE**); Knative array failed (system Python 3.10 `type MoneyEuro` syntax) then **cancelled** (478075); fix = always `micromamba activate gnn` in `run_tiered_hub_gnn_mlp_one.sh` (matches `run_skew4_knative_one.sh`); no CPU partition on datalab — knative sbatch uses `GPU-l40s` without `--gres`
- **Tiered-hub ML results (9 configs, pulled):** 9-config sums — mlp_dim22 **13.30M** (best) · gnn_dim22 13.41M (+0.8%) · mlp_atomic21 15.43M (+16%) · gnn_atomic21 20.47M (+54%); per-config best: gnn_dim22 **5/9**, mlp_dim22 **3/9**, mlp_atomic21 **1/9**; **dim22 beats atomic21 on every config** (both GNN and MLP); higher `k_core` → lower RTT (best rows at k6); compare `compare_tiered_hub_gnn_mlp_sweep.py`; transfer `transfer_tiered_hub_gnn_mlp_{to,from}_datalab.sh`
- **Tiered-hub 125-225 dim22 (2026-06-10, datalab):** `tiered_hub_gnn_mlp_125225_20260610/` — 11 configs (9 hub + `default` + `05_sparse`); job **478100** ML **21/22** (GNN **10/11** — `hub_k2_seek30` timeout @3600s; MLP **11/11**); paired hub wins **GNN 5 · MLP 5**; GNN best **k4/k6 seek30–50** (−8% to −33% vs MLP); MLP best controls + **k2** (+96% GNN on k2 seek50); Knative **0/11** (CRLF `jobs.tsv` fail → fixed; job **478314** `--gres=gpu:l40s:1` pending); scripts `tiered_hub_dim22_gpu.sbatch`, `tiered_hub_knative_125225.sbatch`, `transfer_tiered_hub_dim22_125225_{to,from}_datalab.sh`, `submit_tiered_hub_dim22_retry.sh`
- **GNN-favorable hub regime (125-225):** `k_core≥4`, `hub_seeker_fraction` 30–50%, avoid k=2 under heavy load; GIN batch coupling vs MLP pointwise edges on shared Xavier hubs; `latency_periphery_ms` in JSON **unused** (generator uses single `latency_core_ms` for all links)
- **datalab CRLF gotcha:** `*.tsv` manifests (`jobs_dim22.tsv`, `knative_jobs.tsv`, `retry_jobs.tsv`) with `\r` → `config missing` on compute nodes; `sed -i 's/\r$//'` before `sbatch`
- **Topology sketches:** `scripts_cosim/important/sketch_topologies.py` → `simulation_data/topology_sketches/` (4-panel overview, seek compare, degree bars, concept schematic)
- **atomic21 inference wiring (2026-06-10):** `INFERENCE_FEATURE_LAYOUT=atomic21|dim22` in `executesimulation.py`; GNN pads 2-d task → 3-d in `feature_builder.py`; MLP sets layout when `input_dim==21`; dim14-ce checkpoint `task_dim=3` works with atomic21 layout but **live RTT worse** on tiered hubs
- **Reviewer Triangle all7 (2026-06-09/10, datalab, 100-100 seed 42):** sweep `reviewer_triangle_all7_20260609/results/` — **7× MLP + 7× XGB** pulled to mitrix; MLP 7-config sum **17.96M** (+0.4% vs pre-fix anchor GNN 17.89M); XGB **30.34M** (+69.6%); MLP wins anchor 2/7 (`default` −9.6%, `03` −2.1%); XGB 0/7; scripts `run_reviewer_triangle_regime_a_all7_nohup.sh`, `transfer_reviewer_triangle_from_datalab.sh`, `scripts_cosim/datalab/*`
- **Regime A = batch loop only:** dim14-ce anchor 7/7 on mitrix **already is Regime A** — no separate rerun unless JSON co-located in triangle sweep dir
- **Regime A compare wiring:** `scripts_cosim/run_simulation.py` `--xgboost_batch` / `--knative_network_batch`; `memory/compare.md` + `/compare` command Regime A section
- **ML inference vs SimPy RTT:** `task.gnn_decision_time` = wall-clock batch inference amortized per task (GNN + XGB); **NOT** charged to `elapsedTime`/`total_rtt` (`infrastructure.py` env.timeout commented out); only 2ms `batch_timeout` is SimPy time
- **`total_rtt` = SimPy sum:** `orchestrator._stats_low_memory()` sums `task.elapsed_time` (= `done_time − dispatched_time` in `infrastructure.py`); cross-machine RTT drift = **different placement trajectories** (ML argmax → queue/cold-start paths), not wall-clock hardware; causes: numpy/torch version on feature build, env vars (`GNN_QUEUE_NORM_MODE`), code commit skew — seed 42 fixes topology only
- **MLP batch device gap:** `executesimulation.py` `mlp_batch` omits `device` in models dict → `GNNScheduler.set_models` defaults **CPU** (`torch.device('cpu')`); GNN policy passes cuda via `load_gnn_model`
- **datalab runtime:** repo `/home/nikola.lukic/gnn-herosim`; **micromamba `gnn`** (not pipenv); **all SLURM jobs must activate `gnn`** before `python3` — system Python 3.10 fails on PEP 695 aliases (`type MoneyEuro = float` in `placement/model.py`); rsync artifacts (models/tabular, workload-100-100, configs) — gitignore blocks binary push; no `CPU` partition — use `GPU-l40s` without `--gres` for Knative-only jobs
- **Regime A MLP files (2026-06-09):** `mlp_model.py`, `train_mlp.py`, `mlp_scheduler.py`, `mlp_orchestrator.py`; policy `mlp_batch` → `mlp_batch_mlp_batch`; env `MLP_MODEL_PATH`
- **rtt_overview (2026-06-09):** `executesimulation.py` + `orchestrator.py` export `total_inference_time`, `total_rtt_plus_inference`, `averageGNNDecisionTime`, top-level `rtt_overview` on result JSON; ML policies include `xgboost_single`
- **Quick sim workload:** `simulation_data/workload-quick-1000.json` (1k tasks) for fast policy smoke tests
- **XGB wall-clock scaling:** full `workload-100-100` XGB batch ~**80min** wall vs GNN dim14-ce ~**10min** GPU (CPU graph build + XGB predict; inference absent from RTT either way)
- **prepare_graphs_cache_seq.py fix (2026-06-09):** added dim-14 `shared_fate_signal` + `initialized_snapshot` forward (was 13-dim → extraction 21≠22); bump CACHE_VERSION `6.5-seq-tabular`; run with `PYTHONPATH=<repo_root>`
- **Tabular extraction invariant:** emit rows ⟺ `task_idx == seq_step`; iterate via `task_logit_to_placement` (not raw `edge_index`); global directed edge index = sum(len(candidates) for s<task_idx)+logit_idx; use first half of `edge_attr` after `to_undirected`
- **`platform_pos` in `queue_key_to_platform_meta`:** required for extraction; cache versions `5.3` (hom) / `6.5-seq-tabular` (seq); live `gnn/scheduler.py` also sets `platform_pos` at inference
- **Tabular training:** `rank:pairwise` grouped by `graph_id`; split by `parent_dataset_id`; exclude `@seqneg1` by default; `--regime single` = `@seq0` bootstrap only (not paper-grade Regime B)
- **Scheduler asymmetry (baseline):** default `knative_network`/`herocache_network` = per-task wakeup; GNN + `XGBoostBatchScheduler` = batch; compare `knative_network_batch` in Regime A sweeps
- **Per-arrival plan:** `scripts_cosim/PER_ARRIVAL_XGBOOST_PLAN.txt` — **IMPLEMENTED** (bootstrap + marginal v2); scale blocked on generator grid size (25 combos only)
- **Regime B XGBoost files (2026-06-09):** `feature_builder.py`, `single_scheduler.py`, `single_orchestrator.py`; policy `xgboost_single` → `xgb_single_xgb_single`; env `XGB_SINGLE_MODEL_PATH`; `GNNScheduler._build_inference_graph` delegates to `feature_builder`
- **1-task co-sim (`gnn_datasets_1task`):** `scripts_cosim/generate_single_task_datasets.py` + `--num-tasks 1`; marginal oracle = brute-force **feasible replicas only** (network+replica filters, not all platforms); ~**1.6s/dataset**; **25/25** ds full config grid (`simulation_data/gnn_datasets_1task/`)
- **1-task SSC:** `generate_gnn_datasets_fast.py` auto-writes `system_state_captured_unique.json` from optimal `taskResults` (fail-loud); cold-start skips Phase-1 capture — use optimal rewrite path
- **Regime B cache/train:** `prepare_graphs_cache_single.py` → `graphs_cache_gnn_datasets_1task` (25 graphs, 311 rows); models `single_edge_ranker.json` (bootstrap @seq0 **67.8%** test) + `single_edge_ranker_v2.json` (marginal **60.0%** test)
- **Regime B smoke (1k):** `smoke_xgboost_single_1k.json` — RTT **33.1M s**, inference **20.5s** wall (not in RTT)
- **tabular `__init__.py`:** no scheduler imports — avoids `gnn.scheduler` ↔ `tabular.scheduler` circular import when loading `feature_builder`

---

## 4. Human-Readable Changelog


| Ver   | Date             | Type  | Summary of Changes                                                                                                                                                                                                                                                                                               |
| ----- | ---------------- | ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0.49.1 | 2026-08-17 15:10 | Patch | **Arm A re-gate 15/15 — verdict unchanged, but the residual's sign flips with coupling.** PRIMARY **1/3**, TAIL **1/3** FAILED; paired wins identical to baseline (GNN 3/15 · MLP 10/15 · Kn 2/15); SUM 1.05→**1.12×**. `sparse_p25` +3.5%, `sparse_p35` +9.9%, but **`sparse_p25_skew` −4.3% (0.84→0.80×), p99 71.0→63.1s** ⇒ the residual pays only on the coupled config and costs RTT on the two near-pointwise ones — the separability prediction, measured. **Correction to the mid-session read:** `logit_tied_rate` is NOT the discriminator (it rose on all three configs, including the one that improved); the config's coupling is. |
| 0.49.0 | 2026-08-17 14:54 | Minor | **`mp_parity` CLOSED — both arms falsified, and the residual failed in exactly the way the separability finding predicts.** Baseline 15-cell gate `gnn_mp_parity_gate_20260816/` complete: **PRIMARY 1/3, TAIL 1/3 — both pre-registered criteria FAILED** (`sparse_p25` 1.14× Kn / MLP 0.83×; `sparse_p35` 1.02× / 0.77×; **`sparse_p25_skew` GNN 0.84× / MLP 2.27×**, GNN 3/5, p99 **71.0s vs 498.5s** ⇒ the pre-registered collision cliff reproduces on **skew**, not `sparse_p35`; SUM 1.05× / 0.93× hides it). Trained both arms concurrently on one datalab A100 (jobs **693621/693622**, ~50min): **Arm B (candidate-restricted same-node edges) FALSIFIED** — worse than Arm A on every metric *while trained with the edges* (val 62.6% vs 65.6%, greedy regret 0.4944s vs 0.2621s). **Arm A (gated GIN residual) FALSIFIED LIVE** — offline greedy regret **0.5682→0.2621s (−54%)** and `mp_gate` learned **1.08**, yet live *worse*: `sparse_p25` +3.5%, `sparse_p35` +9.9%, **0/5 and 0/5** vs MLP, PRIMARY 0/2 ⇒ **offline regret is not a live predictor**; the tell is `logit_tied_rate` **rising** (0.5097→0.5542, 0.5374→0.5807) — extra capacity buys ~1pp of interaction variance and blurs the ~99pp pointwise signal that decides placement. **Structural fixes:** trainer now **imports** the one `TaskPlacementGNN` (−84 lines duplicate; caught a second latent drift, a trainer-only `platform_input_norm` the server lacked); residual carries a learnable `mp_gate` so a residual ckpt **cannot** strict-load into a non-residual model; `mp_node_edges` recorded in the `.contract.json` sidecar and a stale `GNN_MP_NODE_EDGES` now fails loud; guard grown 5→**9** tests. **Retraction:** the 0.47.0 "0.88× Kn" figure is **irreproducible** (current code gives **1.04×**, same cell/seed/model) — that arm used `GNN_DROP_NODE_EDGES=1`, implemented nowhere today; enabled by `run_provenance` recording neither git commit nor `OMP_NUM_THREADS`. **Infra:** local ↔ datalab fully synced at `b90570c` (branches `cleanup/experiment-configs` + `feat/gnn-mp-residual` pushed; 13/13 active collections matched) — datalab had been serving the **pre-fix** `gnn_model.py`, and its `constants.py` lives in `src/motivational/` vs local `src/placement/`, so **training ports but serving does not**. Re-synced in place (`fetch` + `checkout --force -B` + `clean -fd`) to preserve 65G of gitignored data scattered inside `src/`; `GPU-rtx6000` unusable (no torch kernel image). |
| 0.48.0 | 2026-08-17 14:20 | Minor | **THE ENVIRONMENT WAS THE PROBLEM — the co-sim target is pointwise-separable, so the MLP was the *correctly specified* model class.** New **M4** variance decomposition + `--gate-additive-r2` / `--gate-coupled-fraction` in `separability_diagnostic.py`: fits `rtt(plan) ≈ μ + Σ_t f_t(plan[t])` (exactly `PointwiseEdgeMLP`'s expressive class) over every sweep plan. Additive R² **0.98812** (v2, additive argmin = optimum in **81%** of ds) → **0.99973** (v4/v5) → **1.00000** (highq_safe) ⇒ **no data or architecture change could have made a GNN win**; reframes `mp_parity` entirely. Whole interaction is **one integer** (collision count: +1.10pp on v2, +0.03pp on v4/v5). **Deep queues falsified as a lever on all 899 v2 sweeps**: depth predicts separability monotonically (shallowest quartile R² 0.978/+1.986pp → deepest 0.998/+0.181pp, **11× weaker**) because `depth × exec_time` grows with depth while `added_in_batch × exec_time` does not — **the contention series was optimizing backwards**. Inverse works: new **`shallow_v1`** grid (depths ~0–8, 200 ds, registered + own compat group `scale_invariant_v1_node_disk_v2_4task`) → coupled(>1%) **31.0% vs 7.1%**, coupled(>5%) **19.6% vs 2.4%**, pointwise worst-case regret **48.4% vs 3.6%**, and the **node**-collision column outexplains the platform one for the first time. Built `node_contention_v3` (`Node.compute_slots`, default-off = bit-identical `node_disk_v2`, 9 tests) — **no usable effect**: `nodeContentionTime` exactly **0.0** on every placed task ⇒ *a shared resource only couples if placed tasks hold it long enough to overlap*. ⚠ **Ablation INVALID — 24/200 (12%) cache labels ≠ sweep optimum** (mean regret 15.78%, max 92.5%), reproducing `gnn_necessity_separability.md` §1's open item; the `gnn_base`-beats-pointwise result is **retracted** pending relabel. Also fixed: `queue_feature_contract` was a hardcoded guess driving compatibility decisions (now read from cache, warns on conflict); `prepare_workloads` silent app-drop; `calculate_workload_stats` `average_rps`/`rps` key mismatch. **Corrections made mid-session:** node-vs-platform R² read backwards (platform ids subsume nodes), n=12 gate pass regressed to 0.961 at n=168, and mean R² is the wrong gate for a bimodal target. |
| 0.47.0 | 2026-08-17 08:45 | Minor | **ROOT CAUSE: train/serve message-passing mismatch — the GNN was served on a graph it was never trained on.** `train_near_rtt.py` fits `self.gin(x, data.edge_index)` (bipartite only; **never** references `node_edge_index`), while `src/policy/gnn/gnn_model.py` concatenated **every** same-node platform↔platform edge: **1428 vs 57 = 25:1 flood**, erasing dim7 by averaging co-located platforms. On the deployed ckpt **87.5% of argmax decisions flip** (420/480). Ablation ladder `gnn_encoder_ablation_20260816/` (`sparse_p35`/s42, full-corpus siv1): control **276.0M / 10.93× Kn / qvsmin 4,007** → **drop same-node edges 22.34M / 0.88× Kn / p99 213 / qvsmin 98** — **one env var, no retrain, now beats Knative and ECT**; no-MP 22.44M (0.89×) ⇒ bipartite MP is ~neutral, rung B is still a real GNN. **Decode guards falsified**: LQB λ=1.5 → 275.7M (no change); `argmax_uniq` → collisions **exactly 0.000** but **worse** 301.3M ⇒ collisions were a symptom. **ECT measured for the first time and is NOT a ceiling**: 0.98–1.13× Kn on all 3 cells (MLP 0.78×) ⇒ no distill teacher for Regime A. Fixed: same-node edges opt-in via `GNN_MP_NODE_EDGES` (default off = parity) + guard `test_train_serve_mp_parity.py` (5 tests); `argmax_uniq` now relaxes instead of aborting into a silent shortest-queue fallback and reports `relaxed_rate`; unknown `GNN_DECODE_MODE` fails loud. 15-cell MP-parity gate in flight. **GNN 0.88× still loses to MLP 0.78×** ⇒ next = residual + candidate-restricted edges (both already in `gnn_necessity_ablation.py`) in trainer *and* server, then retrain. |
| 0.46.0 | 2026-08-16 00:20 | Minor | **Corpus size falsified as the GNN lever.** Merged the full healthy `legacy_v0_node_disk_v2_4task` group into `graphs_cache_full_corpus_siv1_dim14` (**2,651** graphs, 3.04× the 873 slice; 31 datasets excluded via oversample allowlist for sweep-min labels off candidate edges, 28 clustered in `contention_v2/ds_00646–00899`). Retrained both under `scale_invariant_v1`/dim14 (wandb `gnn-full-corpus-siv1-aug2026`): GNN val **66.3%** / test **65.3%** (was 62.6/55.7 — a real gain beyond seed noise), MLP val edge 87.7% / test **88.4%**. Live 45-cell trio `full_corpus_siv1_coupled_trio_20260815/` (datalab **685484**, Knative arm symlinked from the v5.5 trio): **GNN 0/15, SUM 12.40× Kn** (15.07× at 873) vs **MLP 10/15, SUM 0.93× Kn**; p90/p99 GNN 0/3. `chosen_queue_vs_min` **mean 3,329** ⇒ queue-blindness unchanged ⇒ **offline accuracy and live placement are decoupled; corpus growth is not the lever.** Also: full `src/` rsync to fix datalab's divergent-history ImportErrors, and a regressed hardcoded `run_simulation.py` `BASE_DIR` that failed all 30 cells. New scripts `scripts_cosim/datalab/{run_,}full_corpus_siv1_{recache,gnn_train,mlp_train}.{sh,sbatch}`. |
| 0.45.0 | 2026-08-13 21:19 | Minor | **siv1 5-seed gate: MLP beats Knative 13/15 + p99 3/3; GNN collapse isolated to its decode.** `contention_v2_873_v5.7_siv1_coupled_trio_20260813/` (datalab **664414**, 30 ML cells; Knative arm symlinked from the v5.5 trio sweep, policy-independent) with `manifest.json` + `compare.json`. Seed-averaged `mlp/kn` **0.74x p35 (5/5) - 0.79x p25 (4/5) - 0.90x skew (4/5)**, SUM **0.775x**; MLP wins **p99 3/3** and p90 2/3. Two losses (p25 s45 1.07x, skew s43 **2.14x**) => claim average+tail, not uniform. **GNN 0/15, 15.07x Kn.** Init-seed variance (n=3/contract, split fixed via new `NEAR_RTT_TRAIN_SEED`) shows test acc `legacy` 55.5%+/-4.2 vs `siv1` 53.9%+/-2.5 with **overlapping ranges** => v5.5's 60.3% was a lucky draw and siv1 is within noise; the GNN's live failure is therefore **structural in its joint 4-task argmax** (collisions 0.179->0.236, `chosen_queue_vs_min` median 734->3676), not features or accuracy. Hardened the datalab path: contract declared+echoed per cell, added to `--export`, and `run_sealed_holdout_one.sh` now refuses a GNN cell with neither sidecar nor explicit contract (would silently serve legacy_v0). |
| 0.44.0 | 2026-08-13 20:05 | Minor | **siv1 live: MLP beats Knative 3/3, GNN regresses.** Trio smoke seed 42, `num_tasks` identical 561848: MLP siv1 rtt **0.747×** Kn on all three cells (12.86/18.89/3.26M vs 17.21/25.25/4.36M), p99 **0.40–0.60×**, p90 lower 3/3; loses p50 on p25/p35. vs MLP v5.5 rtt 0.16–0.29×, p99 0.05–0.07×. Not shortest-queue: `chosen_queue_vs_min` median rose 7→20/9→36 while p95 collapsed 12668→980. **GNN siv1 worse than v5.5** on all three (rtt 236.9/332.7/77.2M), still 14–19× Kn — confounded by fresh weights (test 60.3→55.7). Fixed two plumbing defects the smoke exposed: `mlp_batch` provenance recorded the pre-load default (`legacy_v0`) though `siv1` served — now adopted before `build_run_provenance`; `train_near_rtt.py` seq-val still hardcoded legacy cap-100 + `ratio/5.0` — now delegated (never fired in this CE-only retrain). |
| 0.43.0 | 2026-08-13 18:12 | Minor | **Lever (D) implemented + retrained; live gate open.** New `src/placement/queue_features.py` is the sole owner of dim7/dim13 (was duplicated across 4 call sites), with versioned contracts `legacy_v0` (bit-exact history) and `scale_invariant_v1` (dim7 divisor uncapped, dim13 `log1p(ratio)/log1p(5)`). Contract is carried by cache metadata, MLP ckpt key and GNN `.contract.json` sidecar, checked in `run_provenance`, and a train/serve mismatch **raises** (absent ⇒ `legacy_v0`, so v5.5 still serves). Proofs: 28 contract tests; `legacy_v0` recache numerically identical to deployed v5.5; parity 6/6 incl. live-scale depth (legacy dim7 **28–600** vs v1 **0.007–1.0**). Also fixed: parity verifier was blind (all-idle fixture) → now injects depth + synthesizes temporal state; `train_near_rtt.py` missing repo root on `sys.path`. Retrain on `…873_v5.7_siv1_dim14` (`CACHE_VERSION` 5.7, new `--platform-feature-dim`) into wandb `gnn-queue-scale-invariant-aug2026`: GNN val **62.6%**/test 55.7% (v5.5 61.1/60.3), MLP val edge **91.6%**/test 87.3% (91.4/88.3) ⇒ offline unchanged, live delta attributable to scaling. Gate running: trio smoke seed 42 vs Kn (`sparse_p35` s42 target: `averageQueueTime` 503s→~45s, `chosen_queue_vs_min` median 734→~9). |
| 0.42.0 | 2026-08-13 18:40 | Minor | **Option A falsified; live failure re-diagnosed as queue-feature OOD.** `contention_v2` labels reproduce at HEAD (0/180 mismatch), are pull-free (0/900), and already punish piling (+333%) ⇒ relabelling under serialized FilterStore is a no-op. `contention_v4_deepq` pilot (datalab 663969, 27 ds) **FAILED** the coupling gate: coupled **0.0%** vs v2's 7.1%, colliding **−6.3% better** than spreading — deep queues reward piling. Real mechanism: dim7 `queue_norm` cap **100** binds 0/200 in training (p90 depth 26–70) but always live (depth ≈14.5k from 503s queue ÷ 0.035s exec) ⇒ dim7 ~100× / dim13 `usage_ratio` ~360× OOD with no clipping; GNN picks platforms with median **+411** extra queued tasks (MLP +7). Fix candidate = scale-invariant queue features + recache + retrain, **no new co-sim**. |
| 0.41.1 | 2026-08-13 17:05 | Patch | **RQ3b/re-baseline artifacts sealed.** Rsync verified complete (175/175 JSONs parse, 13.2G) — logs were empty because whole-sweep phases never ran on the per-cell datalab path. Wrote `compare.json` both sweeps (trio Kn 15/15 · re-baseline Kn 20/20, reproducing the RQ3b table exactly); restored `configs/` + `manifest.json` with ckpt md5s, `code_head`, and `incomparable_with`. Extracted `write_sweep_manifest.py` (fail-loud, `--force`-gated) and made manifest identity overridable so re-baselines can't inherit the original's. |
| 0.41.0 | 2026-08-13 16:50 | Minor | **RQ3b FAIL + sealed re-baseline CLOSED.** Post-`25732cf` `node_disk_v2`: Kn wins coupled trio 3/3 (GNN rtt 2.8–4.1× MLP, p99 cliff 0/5) and sealed 20/20 (was MLP 13/Kn 6/GNN 1). 873/v5.5 GNN/MLP pile into serialized FilterStore (`avgQueueTime` ~503s). Datalab CPU-amd 90/90 jobs 663480/663482. Next=training/physics split, not more 873 eval. |
| 0.40.0 | 2026-08-13 13:05 | Minor | **Physics confound + tail metrics.** `warmth_physics` default `platform_reuse_v1` → 110× total_rtt; `run_provenance` in every result JSON + `HEROSIM_REQUIRE_EXPLICIT_PHYSICS`. `sweep_metrics.load_metrics()` p50/p90/p99; 26 sweeps retro-scored. **MLP p99 cliff 783.5s on sparse_p35** (GNN 110.2 / Kn 164.7, n=1). `25732cf` pull-serialization → sparse_p25 knative 6.90M→17.21M (2.50×); pre-08-12 Regime A sweeps incomparable. RQ3b coupled-trio replication running; sealed-holdout re-baseline chained. |
| 0.39.2 | 2026-08-13 01:06 | Patch | PR #3 merged to main (`040fdcb`); restructuring/RQ9/compare docs updated (local gitignored md). |
| 0.39.1 | 2026-08-13 00:56 | Patch | Phase 3.1 committed+pushed `fb4e729` on PR #3; Regime B sprint wrapped (claim bounded to oracle_split_v1). |
| 0.39.0 | 2026-08-12 12:46 | Minor | **Phase 3.1 CLOSED Regime B 125→31.** Cold multiseed ect_pull distill 6000 frames + seq_reforward_pull **31.66s** ≡ ect_pull. Warm/busy v1 mix → 375s REJECTED. Stub jitter + harvest loop shipped. |
| 0.38.1 | 2026-08-12 12:57 | Patch | Handover sync: Phase 3 PARTIAL 62.63s + hard-CE REJECTED 125s; PR #3 thru `129a24a`; NEXT=scale distill corpus / marginal ordinal. |
| 0.38.0 | 2026-08-12 12:06 | Minor | **Phase 3 ect_pull distill PARTIAL.** Harvest 12 live frames; CE+softKL distill; seq_reforward_pull **62.63s** (vs CE 93.95 / ect_pull 31.65). Cross-batch pull ledger. Next=scale corpus / harden imitation. |
| 0.37.0 | 2026-08-12 11:16 | Minor | **Phase 2 soft_combo_conc REJECTED.** oracle_split dim16 τ=0.25 γ=0.02; live frozen_topk/argmax **125.28s** vs CE **93.95s** (hurt −31s) vs ect_pull **31.65s**. Discard Set Transformer. Next=ect_pull distill / N=12 teacher. |
| 0.36.0 | 2026-08-12 11:07 | Minor | **Phase 0.1 parity FROZEN + Phase 1 pull-decode REJECTED.** Golden SSC test 48/48; fixed live target_concurrency / undirected edge_attr / node_edge_index. `seq_reforward_pull`: argmax 93.95s vs pull 125.28s (hurt). PR #3. Next=Phase 2 soft_combo_conc or distill/N=12 teacher. |
| 0.35.6 | 2026-08-12 10:07 | Patch | Handover sync: Active Context verified post negative cosim-retrain; tasks trimmed to residual-lever NEXT + N=4-CE BLOCKED; deploy 873 unchanged. |
| 0.35.5 | 2026-08-12 01:25 | Patch | oracle_split_cosim 48→CACHE5.6→retrain NEGATIVE (MLP125/GNN188); CE topk-fallback save; next=distill/marginal-rollforward not more N=4 CE. |
| 0.35.4 | 2026-08-12 01:20 | Patch | Ship ect_pull intel ceiling (31.65s); oracle_split co-sim pilot 3×2401 FilterStore labels; next=scale+retrain. |
| 0.35.3 | 2026-08-12 01:15 | Patch | Pull-obs diagnosis: features discriminate; co-sim labels warm-path; absolute cost→62s; marginal `ect_pull`→31.65s=oracle (vanilla ECT 376s). Next=marginal lever A/B/C. |
| 0.35.2 | 2026-08-12 00:53 | Patch | Handover sync: pull-obs negative confirmed local; NEXT=diagnose GNN=125/MLP=375; jobs 654424–654427. |
| 0.35.1 | 2026-08-12 00:52 | Patch | Pull-obs zeroshot NEGATIVE: GNN 125.28s (=873), MLP 375.71s (scarce pile). Residual 125→31 still open. Jobs 654424–654427. |
| 0.35.0 | 2026-08-12 00:45 | Minor | Pull observables: CACHE 5.6 plat 14→16 (`node_cold_count`, `estimated_pull_remaining_sec`); dim24 inference; Regime B dim16/dim24 train launchers; `test_pull_observables.py`. Next=datalab FORCE_RECACHE+retrain+pullobs zeroshot. |
| 0.34.9 | 2026-08-12 00:26 | Patch | Handover sync: residual retrain negative; Active Tasks → pull observables (`node_cold_count` / `estimated_pull_remaining_sec`) as sole NEXT for 125→31. Keep 873 intel. |
| 0.34.8 | 2026-08-12 00:30 | Patch | **Regime B 450 retrain DONE — residual gap NOT closed.** Cache 450/5.5 OK; GNN val 61.2% (`b97187c1…`); MLP val edge 91.0% (`4c25cb6a…`). oracle_split zeroshot: MLP 125.28s (=873), GNN 156.87s (worse), Kn 219.48s. Next = pull observables (`node_cold_count`). Keep 873 for intel. |
| 0.34.7 | 2026-08-12 00:15 | Patch | **oracle_split_v1 intelligence cell.** Union seeds open action space; 873 MLP/GNN 125.28s vs Kn 188.17s (margin 62.9s PASS). Keep 873 — no retrain yet. Gate stays scarce_preinit_v2 11.88×. Probe + compare under `…/oracle_split_v1_zeroshot/`. |
| 0.34.6 | 2026-08-11 23:41 | Patch | Handover sync: free headroom unlocked; active tasks → anti-corr lure + Regime B retrain decision. scarce_preinit_v2 DONE. |
| 0.34.5 | 2026-08-11 22:15 | Patch | **scarce_preinit_v2 + FilterStore hold fix.** Seed N cold deferred on node0; Kn/GNN integrate `initial_replicas`; Knative/GNN hold FilterStore during pull (determined parity). Free Kn 11.88×; MLP/GNN ~11.87× tied. MLP fail-loud without `mlp_model_path`. Compare: `regime_b_cold_burst_v1_scarce_preinit_v2_zeroshot/`. Next = anti-corr / Regime B retrain (not decode). |
| 0.34.4 | 2026-08-11 21:35 | Patch | **Co-sim 450/450 + zero-shot DONE.** Gate 11.88× PASS; free Kn/MLP/GNN all ≈31.88s (1.01× oracle) — all-cold stub has no scarce-warm lure; MLP wiring fallback. Next = scarce-warm live stub v2 then re-baseline. Artifacts: zeroshot compare + datalab corpus. |
| 0.34.3 | 2026-08-11 19:45 | Patch | **Regime B co-sim launched on datalab CPU-amd** job 648835 (450 ds, platform_reuse_v1); live stub written; local smoke ds_00000 + placements.jsonl OK; fail-loud if wrong physics. |
| 0.34.2 | 2026-08-11 19:30 | Patch | **Regime B problem spec FROZEN** (`regime_b_cold_burst_v1`): N=12, platform_reuse_v1, scarce FilterStore attractor, ≥10× gate; SoT `scripts_cosim/regime_b_problem_spec.py`; calibrate `--mode target` **PASS 11.88×** (376s/31.65s). Co-sim gen + baselines remain NEXT. |
| 0.34.1 | 2026-08-11 19:15 | Patch | **Radical Regime B brief for next chat:** stop 125-225/RQ3 polish; gate = ≥10× oracle–greedy on new cold-burst before retrain; active tasks = problem spec → co-sim+live stub → baselines only. Hard-stop list in memory §2. |
| 0.34.0 | 2026-08-11 18:55 | Minor | **RQ3 ANSWERED (simple claim):** offline GIN>MLP on CACHE 5.5/873; sealed live MLP 13/20 · Kn 6/20 · GNN 1/20. Rewrote RQ3 + restructuring → Regime B. Memory pending Qs collapsed. **Clarity cleanup:** `models/_archive/` (55+ GNN ckpts) + `simulation_data/_archive/` (~39G caches/sweeps); hot = 873/v5.5 + sealed + regime_*. Live script map in memory §2. |
| 0.33.3 | 2026-08-11 16:15 | Patch | **Sealed multi-seed live holdout DONE:** unused p50/p60/client/server × seeds 42–46; GNN on datalab 616774 (20/20). Paired wins **MLP 13 / Kn 6 / GNN 1**; sums MLP **0.82×** Kn, GNN **1.05×** Kn. Offline GNN advantage does **not** transfer on sealed cells. Report `contention_v2_873_v5.5_sealed_holdout_20260806/compare.json`. |
| 0.33.2 | 2026-08-06 21:12 | Patch | **873/v5.5 deploy retrain DONE** (GNN val/test 61.1/60.3; MLP 91.4/88.3). Fixed bad post-train GNN copy (models/873 was briefly 711 weights). **Sealed multi-seed live holdout RUNNING:** unused p50/p60/client/server × seeds 42–46; runner `run_contention_v2_873_sealed_holdout.sh`; sweep `contention_v2_873_v5.5_sealed_holdout_20260806/`. |
| 0.33.1 | 2026-08-06 22:50 | Patch | **Regime B metrics harness calibrated PASS:** platform_reuse_v1 oracle regret **3.97×** (primary 125.57s vs parallel 31.65s); total_rtt trap 3.99×; node_disk_v2 PASS at ratio 1.0×. Fixed `last_task_elapsed_s` to last-by-task_id. Tests `test_regime_b_metrics.py` 3/3. Reports `simulation_data/regime_b_calibration/`. GNN train_near_rtt parent-safe 70/15/15. **873/v5.5 GNN+MLP retrain RUNNING.** |
| 0.33.0 | 2026-08-04 18:05 | Minor | **contention_v2 v5.5 recache + multi-seed RQ3 ablation DONE:** retained **873** (−26 network-incoherent JSONL); cache `graphs_cache_contention_v2_873_v5.5` validator OK; gnn_base vs pointwise seeds 42/43/44 — top1 88.9±0.8 vs 87.1±0.9, cliff 0 vs 1 (pw max 1578%); coupled-tail mixed. Prepare fails loud on candidate-absent labels. Reports `*_seed{42,43,44}.json` + `*_multiseed.json`. Next: Regime B harness + sealed live holdout + retrain deploy ckpts. |
| 0.32.9 | 2026-08-04 17:50 | Patch | **Training-contract A/B measured + code re-shipped:** labels OLD→NEW nonopt/absent warmth 210→0, sparse 166→0, cont_v2 44→0; 8× split parent-leak 100%→0%; SSC warmth 25–33% platforms have prev_type. Restored deleted prepare/train/cache_io, reapplied 5.5 contract. Report `training_contract_ab_20260804.json`. Recache still required. |
| 0.32.8 | 2026-08-04 14:45 | Patch | **Training-contract code repair shipped (CACHE_VERSION 5.5):** `y`/`opt_rtt` from placements.jsonl sweep minima; SSC `previous_task_type_name` + scheduling-time replicas; `parent_dataset_id` + parent-safe 70/15/15 splits; fail-loud `@os/@seq` RTT lookup; MLP real val split; validator `validate_training_cache_contract.py`. Recache 899 still required before RQ3. |
| 0.32.7 | 2026-08-04 12:30 | Patch | **RQ3 recache blocked by label integrity:** strict 899 cache built, but 26 retained contention labels are absent from sweeps and the pointwise ablation failed loudly after 120 epochs. Audit also confirmed stale sweep labels (warmth 210, sparse 166, v2 44), zeroed batch `is_warm`, `@os` split/RTT leakage, final-state replica features, skew JSONL gap (98/288), and single-seed/pre-fix evidence limits. No retrain/live gate accepted. |
| 0.32.6 | 2026-08-04 10:02 | Patch | **Placement integrity + four-corpus separability audit DONE:** retained warmth **487**, sparse **351**, contention_v2 **899**, contention_v3 **900**; 14 explicit exclusions; 12.29M valid rows. Final collision/coupled>1%: warmth **39.8%/9.7%**, sparse **33.0%/10.3%**, v2 **40.3%/7.1%**, v3 **36.3%/4.2%**. Stale unique-pass 0%/0% claims replaced; frozen report + integrity manifest recorded. |
| 0.32.5 | 2026-06-27 13:15 | Patch | **Merge deployment pipeline CLOSED (mitrix):** validated 30/30 live-gate JSONs; compare tables archived to sweep `compare.txt`; phase markers written; `finish_live_gate_sweeps.sh` + strategic sweep skip-if-exists/`TIMEOUT=18000`. **Jsonl integrity audit:** 5 corrupt ds (4 warmth, 1 cont_v2). **Mitrix corpus:** warmth **503** jsonl, sparse **351**, cont_v2/v3 **900** each; v3 train artifacts datalab-only. Separability re-audit still **pending**. |
| 0.32.4 | 2026-06-20 17:30 | Patch | **contention_v3 end-to-end DONE (datalab):** recache 900 graphs + GNN val 65.6% + MLP val edge 88.2% + live gate 9/9; sums Kn 20.27M / GNN 36.00M / MLP 43.76M — **REJECT** (+81% vs v2 GNN 19.88M). Pipeline scripts `transfer_contention_v3_pipeline_to_datalab.sh` + datalab sbatch chain. Updated `gnn_necessity_separability.md` §9, `compare.md`, `paper/results.md`, `paper/research_questions.md`. |
| 0.32.3 | 2026-06-20 14:30 | Patch | **Merge deployment closed (datalab):** strategic+weighted train+live gates **30/30** JSONs; compare tables archived `logs/strategic_weighted_merge_compare_20260620.log`; merged training **rejected** vs contention-only GNN (+12–17% sum); Kn wins strategic gates 3/3. Fixed `resume_live_gate_sweeps.sh` CRLF self-heal before `set -euo pipefail`. Active tasks → decode repair + Regime B + separability re-audit. `memory/gnn_necessity_separability.md` §10 + `paper/results.md` already synced. |
| 0.32.2 | 2026-06-16 15:30 | Patch | **Simulator landscape + EdgeCloudSim:** cloned `/root/projects/EdgeCloudSim` (Oct 2025, Java 21+); VM-fit benchmark WORST_FIT vs RANDOM_FIT (200 dev, ~15k tasks, 3.54s/2 policies); documented `vm_load_check_interval=0` infinite-loop gotcha. Updated `memory/simulator_landscape_pureedgesim_noserver.md` → five-way guide (HeroSim / PureEdgeSim / EdgeCloudSim / NoServer). |
| 0.32.1 | 2026-06-16 00:20 | Patch | `/update_memory` doc sync: active tasks → recache+decode repair; GNN scope adds contention_v2 deploy ckpts; finisher `--only-missing-jsonl` gotcha; duplicate RQ3 pending removed. Updated `compare.md`, `results.md`, `cosim_grid_and_regen.md` (contention_v3). |
| 0.32.0 | 2026-06-15 23:05 | Minor | **Co-sim non-unique backfill DONE (datalab):** warmth_v2 **492S+9SK/498** jsonl; sparse **348/351** (3 cartesian-full); contention_v2 **900/900** jsonl. Scripts: `generate_non_unique_placements_fast.py --datasets-dir`, datalab `warmth_non_unique_*.sbatch`, `cosim_health_report.sh` monitor (~6h). **wssm autoscale ql sweep:** Kn **8/9** vs GNN (ql=100 Kn all 3). Live gate contention_v2 **9/9 DONE**. Next: rsync→mitrix + recache + separability re-audit. |
| 0.31.1 | 2026-06-15 14:50 | Patch | **contention_v2 live gate DONE:** GNN+MLP trained on 711-graph cache; sweep `contention_v2_live_gate_20260615` sums GNN **19.88M** < Kn 20.73M < MLP 25.35M (−4%/−22%); per-config wins 1/1/1. **contention_v3 900/900 DONE** — coupling decreased (4.2% vs v2 7.2%); sparser hypothesis rejected. Scripts: `run_contention_v2_train_and_live_gate_nohup.sh`, datalab `contention_v2_finish`+`contention_v3_regen`. |
| 0.31.0 | 2026-06-15 12:30 | Minor | **GNN-necessity root cause + fix** (`memory/gnn_necessity_separability.md`): prior MLP==GNN (RQ3) caused by **separable oracle labels** (`--allow-non-unique-replicas` default False → 0% colliding optima, 0% greedy regret). Engineered `contention_v2` (711/900 ds datalab, 41.2% colliding optima, 7.2% coupled). Ablation: **gnn_base beats pointwise** on top1 90.7%/p90 2.39%/opt-recov 69%/max-tail 7% vs 3164%; gnn_node redundant w/ shared_fate. New: `separability_diagnostic.py`, `gnn_necessity_ablation.py`, contention grids, `node_edge_index`+residual GIN, CACHE_VERSION 5.4. **Offline-label only — live gate pending.** |
| 0.30.2 | 2026-06-14 13:42 | Patch | Task types inventory: active config dnn1+dnn2 only; legacy og adds rf+cnn (`nofs-rf`, `nofs-cnn`); peak_configs + generator synthetic types noted |
| 0.30.1 | 2026-06-11 22:14 | Patch | `/update_memory`: ablation scripts in Active Context; mark hub9 ablation done; Regime B active tasks only; decode parity bullet — argmax ignores `live_queues` for pick |
| 0.30.0 | 2026-06-11 21:30 | Minor | **hub9 ablation DONE** (480562, 18/18): seqblend1 **647K (0.78× Kn)** vs MLP argmax **22.5M**; dim22 **1.27M (1.54× Kn)**; decode pathology confirmed (argmax ignores live queue); env too simple for intelligence margin → **Regime B** pivot (metrics → workload → Tier-3 observables) |
| 0.29.3 | 2026-06-11 17:55 | Patch | **hub9 live gate DONE** (480403, 27/27): sparse-merged ce-reduced GNN/MLP+Kn on bipartite v1 · sums Kn 7.44M / GNN 12.31M / MLP 202.53M · Kn 9/9 · MLP queue collapse · inference fixes feature_builder+MLP slice · scripts hub9 sweep · `paper/results.md` updated |
| 0.29.2 | 2026-06-11 17:45 | Patch | **`placements.jsonl` policy** — `memory/placements_jsonl_required.md`; repair/recache ≠ substitute; `--resume` requires JSONL; fail-loud copy in `generate_gnn_datasets_fast.py`; warnings in regen/repair/cache scripts + paper/cosim + cursor rules |
| 0.29.1 | 2026-06-11 17:05 | Patch | Merged **ce-reduced** GNN on datalab **done** (job 480345): rsync 824-graph cache mitrix→datalab, train-only, val acc 29.3%, top5 0.069s; checkpoint pulled; dataloader workers=0 SLURM fix; scripts `transfer_warmth_sparse_merged_ce_reduced_train_to_datalab.sh` |
| 0.29.0 | 2026-06-11 15:07 | Minor | Merged from-scratch training suite **done**: GNN dim14-ce (val acc 29.3%, top5 0.084s); merged seq cache 4097 graphs; MLP full 70.0% + reduced 70.2% val edge acc; pipelines `run_warmth_sparse_merged_{train,mlp_train,mlp_full_train}_nohup.sh`; CRLF fix on new `.sh` |
| 0.28.1 | 2026-06-11 | Patch | **B1 shipped:** `capture_disk_snapshot`, dim13 `node_disk_hit`, seq `src_norm`, seq `is_warm` sandbox fix; `feature_builder` live parity; memory v0.28 consistency pass |
| 0.28.0 | 2026-06-11 18:30 | Minor | Memory v0.28 pass: GNN vs MLP mechanism (bipartite graph, shared decode, metric trap); warmth v2 doc alignment; cosim_warmth_gap historical; merged 824 + skew3 gate in compare/gnn_v2 docs; task dim 2 vs 3; seq is_warm degeneracy |
| 0.27.1 | 2026-06-11 17:20 | Patch | warmth_v2 **skew3 ce-reduced live DONE** (6/6 job 480265): v1 MLP wins 2/3; v2 GNN degree_skew fixed; sparse GNN v2 **1.23M** regression; old-model skew3 9/9 baseline; `${elapsed}` sacct false-FAIL fixed |
| 0.27.0 | 2026-06-11 14:35 | Minor | Sparse co-sim **351/351 DONE** (datalab); `--grid sparse_warmth_v2`; merged cache **824 ds** + sparse finetune **done** (offline weak); warmth finisher cancelled; pipeline cd-path fix |
| 0.26.4 | 2026-06-11 14:19 | Patch | warmth_v2 GNN full+reduced **evaluated offline** — full wins val acc/top5; both oracle_top5≈0; greedy ~8s; live sweep still pending; wandb `gnn-near-rtt-warmth-v2-jun2026` |
| 0.26.3 | 2026-06-11 14:45 | Patch | warmth_v2 **reduced ablation** GNN+MLP **done** (GNN val acc 21.3%/task_acc~68.6%, MLP edge acc 70.3%); MLP reduced pipeline + `reduced_features.py`; documented GNN/MLP offline metric parity (task_acc vs edge acc) |
| 0.26.2 | 2026-06-11 15:45 | Patch | Datalab finisher **480188_0** 491/500; cancelled tail split; sparse co-sim **Option A** analyzed (351 ds); scripts `warmth_regen_finish.sbatch` + `submit_warmth_regen_finish.sh` |
| 0.26.1 | 2026-06-11 13:25 | Patch | warmth_v2 dim14-ce GNN train **done** (val acc 23.4%, `near-rtt-v2-warmth-dim14-ce-only.pt`); MLP seq cache+train **done** (val edge acc 68.2%, `batch_edge_mlp_warmth_v2.pt`); task dim infer fix in train_near_rtt.py |
| 0.26.0 | 2026-06-11 15:05 | Minor | Rsync warmth_v2 **480 ds** datalab→mitrix; `--rewrite-ssc` 473/473; cache warmth_v2 (473 graphs); dim14-ce **blocked** (task dim 3 vs 2); datalab SLURM + `.bf_scratch`/`shutil` fixes |
| 0.25.0 | 2026-06-11 | Minor | Session doc `memory/cosim_grid_and_regen.md`: old vs new co-sim grid, parallel fast-path regen (500 ds, 4 shards, GNN_CAPTURE=0), enrichment A/B PASS, P0/P1 fixes; updated cosim_warmth_gap rollout status |
| 0.24.0 | 2026-06-11 | Minor | Shipped warmth mechanics: `warmth.py`, all Knative-family autoscalers, v2 co-sim defaults, Gate B verified, pilot audit + full regen script |
| 0.23.6 | 2026-06-11 | Patch | Added `memory/cosim_warmth_gap.md` — 1060 corpus pullTime/queueTime audit, co-sim vs A/B label gap, warmth regen + GNN retrain plan; cross-links in warmth_model, storage_contention, compare |
| 0.23.5 | 2026-06-11 | Patch | Cold-start/last-task A/B audit + `memory/warmth_model.md` expert reference (warmth predicate, disk cache gap, GNN alignment, paper wording) |
| 0.23.4 | 2026-06-11 | Patch | Added `memory/storage_contention.md` — FilterStore N× RTT map, metric traps, GNN/MLP feature gap, counterfactuals, file index; verified via `test_memory_contention_ab.py` |
| 0.23.3 | 2026-06-10 21:30 | Patch | seq_reforward k6 results: seek65 beats Kn (−3.3%), seek50 still loses (+7.1%); decode ~200× slower (4 forwards/batch + 208-platform refresh); collision 30%→15%; `memory/compare.md` srf overlay; sprint → Tier 1 collision repair |
| 0.23.2 | 2026-06-10 21:00 | Patch | Doc audit (`audit_doc_claims.py`): fixed paper/results CE 7-config RTT table; retconned cold-start 28%/88% → verified task/platform metrics; weak-baseline Δ pre-fix CE (+25–36% Kn); bipartite sums 21.49M/18.37M; LQB probe vs sweep; Knative default path |
| 0.23.1 | 2026-06-10 20:00 | Patch | Retconned train/serve queue gap across paper/, memory.md, compare.md: 1060 max avgQT 11.5s (3705 6.5s); live 150-150 ~53s / default ~21s (~5–8× not ~200× on avgQueueTime); qvm p95 separate axis; ff warmup A/B verified; source `verify_queue_gap_1060.py` |
| 0.23.0 | 2026-06-10 18:15 | Minor | Bipartite v1 **complete** 9/9 GNN+MLP+Knative; GNN 8/9 vs MLP, 3-way GNN 5/Kn 3/MLP 1; phase boundary k>b confirmed; `compare_bipartite_coordination_sweep.py` + Knative column; `memory/compare.md` bipartite section |
| 0.22.0 | 2026-06-10 17:05 | Minor | Bipartite-coordination sweep v1 (k4/6/8 × seek35/50/65, 5/30ms, b=4); GNN_BATCH_SIZE env + asymmetric infra; datalab jobs 478411/478454; local Knative 9/9; paper RQ3 phase transition; storage Tier1+3 delete ~37G; cancelled 478309/478314; `run_simulation.py` repo-root fix |
| 0.21.9 | 2026-06-10 14:30 | Patch | Tiered-hub 125-225 dim22 sweep (478100, 21/22 ML): GNN k≥4 wins, MLP controls+k2; `memory/compare.md` tiered-hub tables; scripts dim22 125225 + Knative + retry; CRLF tsv fix; topology sketches; Knative 478314 + GNN retry 478309 pending |
| 0.21.8 | 2026-06-10 10:25 | Patch | Tiered-hub results pulled locally (`transfer_tiered_hub_gnn_mlp_from_datalab.sh`); compare run (mlp_dim22 13.30M, gnn_dim22 13.41M, atomic21 rejected); Knative array 478075 cancelled by user; session handover |
| 0.21.7 | 2026-06-10 12:30 | Patch | Tiered-hub sweep ML done on datalab (477929, 36/36): mlp_dim22 sum 13.30M ≈ gnn_dim22 13.41M; atomic21 loses all 9 configs (+16%/+54%); Knative cancelled; micromamba fix for knative runner; scripts `generate_tiered_hub_configs.py`, `compare_tiered_hub_gnn_mlp_sweep.py`, datalab sbatch arrays |
| 0.21.6 | 2026-06-10 10:24 | Patch | Skew-4 125-225 datalab complete (GNN/MLP 8 + Knative 4, job 477953); generated `workload-125-225.json`; scripts `skew4_knative.sbatch`, `run_skew4_knative_one.sh`; `memory/compare.md` cleanup (active dim14/dim22/atomic21 top, archive legacy variants); `.cursor/commands/compare.md` datalab option |
| 0.21.5 | 2026-06-10 07:37 | Patch | Reviewer Triangle all7 on datalab: 7×MLP + 7×XGB pulled (`reviewer_triangle_all7_20260609/`); MLP sum +0.4% vs anchor GNN 17.89M, XGB +70%; rsync scripts + dim14-ce model to datalab; SLURM `reviewer_triangle_gnn_dim14_ce_all7.sbatch`; clarified Regime A=anchor batch loop; `total_rtt` is SimPy not wall clock |
| 0.21.4 | 2026-06-09 21:15 | Patch | Reviewer Triangle Phase 2 (MLP): `PointwiseEdgeMLP(22→64→1, LayerNorm+SiLU)` trained on `batch_edges.parquet` grouped CE, val_edge_acc=67.8%; `mlp_batch` policy wired (mlp_model.py, train_mlp.py, mlp_scheduler.py, mlp_orchestrator.py); 1k smoke status=success; 30k/seed42/default RTT=810,601s; decode_stats sidecars written to `reviewer_triangle_20260609/` |
| 0.21.3 | 2026-06-09 18:55 | Patch | Random+RoundRobin 7/7 weak-baseline sweep done (`random_rr_3cfg_20260609/`); CE-only 7/7 vs both; fair RR fix (per-arrival Knative stack); Δ convention `(policy−ce)/ce`; `memory/compare.md` + paper updated |
| 0.21.2 | 2026-06-09 17:32 | Patch | Post queue-map fix 3-model × 3-config sweep done (ce-only / dim14-1060 / r030 on default/02/04, ~111min); ce-only wins 2/3 sum 8.03M; 1060 default 5.48M (−53% vs pre-fix collapse); r030 rejected (+1.9% sum); `memory/compare.md` updated |
| 0.21.1 | 2026-06-09 15:14 | Patch | Option 1 inference parity shipped (`gnn`+`gnn_hetero` schedulers); 3cfg gate sweep done (+5.7% sum vs anchor, no RTT lift); clarified CE-only `argmax`≡`frozen` (roll-forward telemetry-only); `memory/compare.md` updated; Track B r030 gate 3/4 running |
| 0.21.0 | 2026-06-09 16:30 | Minor | Regime B shipped: `feature_builder.py`, `XGBoostSingleScheduler`, `xgboost_single` sim wiring; 25 marginal 1-task ds + `single_edge_ranker_v2.json` (60% test edge acc); auto SSC on co-sim export; bootstrap `single_edge_ranker.json` (67.8%) |
| 0.20.12 | 2026-06-09 15:15 | Patch | Active GNN scope = dim14 only; `memory/compare.md` simplified and stripped dim13 comparison tables; dim14 checkpoint section consolidated |
| 0.20.11 | 2026-06-09 13:15 | Patch | Track B gate implemented: Phase B checkpoint metric defaults to sequential-reforward val (`regret_seq_reforward`) with queue feature refresh + fail-loud metadata checks; first retry recipe set to r002 constant regret, `margin_cap=4.0`, no ramp; pipeline default tag r002 |
| 0.20.10 | 2026-06-09 12:56 | Patch | Clarified frozen vs roll-forward (both per-task argmax; queue snapshot vs intra-batch increment); documented dim14 `ranking` ≡ dim13 `CE+NearExactRttRanking` loss math (wandb label/trainer infra differ); Track B next = checkpoint gate + low-regret Phase B recipe |
| 0.20.9 | 2026-06-09 12:45 | Patch | dim14-ce **roll-forward A/B** complete: frozen vs argmax on `default`/`01`/`03` — +1.14%/+0.30%/−1.23%; roll-forward not consistent; CE calibration primary; expert handoff + Track B gate = live probe or sequential val |
| 0.20.8 | 2026-06-09 12:05 | Patch | Track B r010 regret ramp **stopped @ep17** (greedy oscillates 1.1–2.1s, no live sweep); deleted partial `near-rtt-v2-dim14-ce-init-r010.pt`; infra done (launcher, pipeline, greedy ckpt+collapse guard); CE≡argmax vs ranking combo-sum mismatch documented; rethink before relaunch |
| 0.20.7 | 2026-06-09 11:45 | Patch | LQB λ=1.5 7/7 complete — sum 18.55M vs CE 17.89M (+3.7%), wins 2/7; λ probe default λ=1.0 +3.8% best; **LQB rejected**; pivot Track B two-phase CE→ranking; updated memory+compare |
| 0.20.6 | 2026-06-09 10:45 | Patch | Regime A wired into compare (`run_simulation.py`, compare.md); 30k Regime A sweep dim14-ce/XGB/kn_batch; `rtt_overview` + orchestrator `total_inference_time`/`total_rtt_plus_inference` (inference wall-clock, not in SimPy RTT); `workload-quick-1000.json`; LQB nohup sweeps in flight |
| 0.20.5 | 2026-06-09 12:17 | Patch | LQB sweep `default` landed: **4.610M** (qvm p95 121, coll 14.1%); beats dim14-full argmax (−60%) but loses to dim14-ce (−4.14M, +11.1%); λ=1.5 over-penalizes; λ tuning (0.8–1.0) planned; sweep running slow (~2h wall/7min CPU per config); updated memory+compare |
| 0.20.4 | 2026-06-09 10:12 | Minor | dim14-ce-only 7/7 complete (wins 3/7: default/00/05); temperature probe confirms T=3 insufficient (10.74M); LQB λ=1.5 recovers dim14-full default to 4.264M (qvm p95 122); `GNN_LQB_LAMBDA` + `GNN_LOGIT_TEMPERATURE` wired into `seq_decode.py`+`scheduler.py`; `adaptive_nonzero` queue norm mode added; LQB full 7-config sweep launched |
| 0.20.3 | 2026-06-09 08:52 | Patch | dim14-ce-only train+sweep started: `train_near_rtt_v2_dim14_ce_only.py`, pipeline+sweep scripts; 100ep val acc 24.4%, test top5 0.191s; `default` 4.14M beats dim13 CE-only 4.34M (−4.5%), dim14 full 11.62M, dim13 clean 15.76M; qvm p95 408; sweep 1/7 `gnn_near_rtt_v2_dim14_ce_only_20260609/` |
| 0.20.2 | 2026-06-08 23:15 | Patch | dim14-1060 train+sweep done: best val top5 0.063s, test top5 0.085s; 7-config argmax `gnn_near_rtt_v2_dim14_1060_20260608/` — vs dim13 4/7, dg-26 5/7, CE-only 4/7; `shared_fate` helps `default`/`00` vs dim13 but CE-only still leads; `run_gnn_near_rtt_v2_dim14_1060_all_configs_nohup.sh`; `compare.md` updated |
| 0.20.1 | 2026-06-09 02:15 | Patch | Regime A executed: fixed seq cache 14-dim platform (v6.5), rebuilt filtered seq cache (883 ds), trained `batch_edge_ranker.json` (64.4% test edge acc); 30k live sweep GNN dim14 726.8k < XGB 894.3k < Knative batch 1.92M RTT; Regime B still blocked on 1-task co-sim |
| 0.20.0 | 2026-06-09 01:30 | Minor | Regime A batch XGBoost scaffold: `src/policy/tabular/*`, `prepare_tabular_dataset.py`, `platform_pos` in cache meta (v5.3/v6.4-seq-tabular), `XGBoostBatchScheduler` subclasses GNNScheduler, policy `xgboost_batch`; two-regime analysis + `PER_ARRIVAL_XGBOOST_PLAN.txt`; nothing run yet |
| 0.19.1 | 2026-06-08 23:36 | Patch | `near-rtt-v2-dim14-1060` training at ep65/100; best val top5 0.0697s @ep50; training healthy (active_pair_frac~0.96, CE=0.86, Rank=0.39) |
| 0.19.0 | 2026-06-08 21:15 | Minor | Fixed `load_all_datasets` bug: `initialized_snapshot` not forwarded from `load_extended_state_data` to dataset dict (was silently zeroing shared_fate for all graphs); rebuilt `graphs_cache_gnn_datasets_4tasks_1060` (1230 graphs, 24.3M RTT rows, v2 sidecar 731,683 combos, trash-cap 192); launched `near-rtt-v2-dim14-1060` retrain; added `train_near_rtt_v2_dim14_1060.py` launcher |
| 0.18.0 | 2026-06-08 20:40 | Minor | `is_warm` fix shipped (`previous_task_type_name` in state_capture → build_graph + gnn/scheduler); `backfill_initialized_snapshot.py` Phase-1-only SSC refresh; 1242/1242 ds backfilled in `artifacts/run_queue_big/gnn_datasets_4tasks_1060`; determined scheduler + `build_system_state_captured` wired for top-level `initialized_snapshot` |
| 0.17.0 | 2026-06-08 22:08 | Minor | FilterStore contention proof (`test_storage_contention.py`, N×T_pull sweep N=1..5, <0.01s error); Day-1 feature fix: `state_capture.py` + `prepare_graphs_cache.py` + `gnn/scheduler.py` for `initialized_snapshot`/`shared_fate_signal` dim 8; platform_feature_dim 13→14 all train scripts + executesimulation.py; co-sim pre-init constraint documented; `is_warm` edge attr bug documented |
| 0.16.2 | 2026-06-08 18:02 | Patch | Added fail-loud queue metadata path for `soft_combo_conc`: graph caches + live scheduler now expose `queue_key_to_platform_meta`; concentration penalty requires metadata and adaptive cap stats; regenerated active clean-1230 cache and v2 sidecar (`--trash-cap 192`), validation passed |
| 0.16.1 | 2026-06-08 17:41 | Patch | Fixed soft-combo guardrails: parent-ID-safe near-RTT lookups for future `@seq` dataset IDs, evaluation parent lookup, CE anchor required for `soft_combo`/`soft_combo_conc` unless explicit ablation env is set; launchers now use `ce_loss_weight=1`; validation passed |
| 0.16.0 | 2026-06-08 17:36 | Minor | Implemented soft-combo near-RTT objectives: `soft_combo_loss.py`, `NEAR_RTT_TRAIN_OBJECTIVE=soft_combo|soft_combo_conc` in `train_near_rtt.py`, launchers for soft-combo and soft-combo+concentration, 5-config argmax/frozen_topk sweep scripts; syntax validation passed |
| 0.15.1 | 2026-06-08 14:05 | Patch | clean-1230 seqblend m2 full 7-config sweep (default 4.10M); CE-only ablation train+sweep (`near-rtt-v2-clean-1230-ce-only`, 7/7 vs dg-26 argmax); collapse analysis; `train_near_rtt_v2_ce_only.py` + CE-only checkpoint path; `compare.md` tables |
| 0.15.0 | 2026-06-08 10:17 | Minor | Ultimate hom run: v2 trash-exp on 1230-ds scheduler_adaptive cache; v2 sidecar 731k; `near-rtt-v2-clean-1230` train (val top5 0.054s, test greedy 0.538s); 7-config sweep beats dg-26 5/7 + ethereal 7/7; `ethereal-lake-8` v1 baseline + sweep also done |
| 0.14.0 | 2026-06-07 17:50 | Minor | Hetero v2: `train_hetero_v2.py` + trash sidecar (520k combos); stripped dead hom GIN from `train_hetero.py`; `hetero-v2-trash-regime` 100ep train; live default `frozen_topk` 10.5M RTT (−23% vs soft-forest-1 argmax 13.6M); `gnn_hetero_gnn_hetero` strategy registered |
| 0.13.2 | 2026-06-07 18:00 | Patch | Canonical 3-model cache map: dg-26→`graphs_cache_gnn_datasets_4tasks` (~3705); woven-totem-52→`…_1060_scheduler_adaptive`; ssc-trash-1→`…_1060` near-RTT variant; ssc-trash-1 7-config live sweep (2/7 wins); soft-forest-1 hetero default live noted |
| 0.13.1 | 2026-06-07 15:03 | Patch | `near-rtt-v2-ssc-trash-1` train complete: best val top5 0.025s, final test top5 0.088s, greedy 9.28s, sidecar hit 40.4%; checkpoint `models/near-rtt-v2-ssc-trash-1.pt` |
| 0.13.0 | 2026-06-07 14:54 | Minor | Implemented [Hetero GNN Pipeline](/root/.cursor/plans/hetero_gnn_pipeline_4e0b40c7.plan.md): `HeteroData` cache wrappers, copied/adapted `train_hetero.py`, manual `gnn_hetero` `HeteroConv` model/scheduler, `gnn_hetero` sim wiring; smoke validations passed |
| 0.12.3 | 2026-06-07 14:45 | Patch | Stopped fresh-microwave-6 early; added loss/sidecar v2 (`train_near_rtt_v2.py`, v2 sidecar 613k rows incl. 111k trash); launched `near-rtt-v2-ssc-trash-1` (`05il3atn`) in `gnn-near-rtt-jun2026` |
| 0.12.2 | 2026-06-07 14:35 | Patch | SSC repair 1139/1139; strict SSC in prepare_graphs_cache*; rebuilt graphs_cache_gnn_datasets_4tasks_1060 (518k sidecar); started fresh-microwave-6 near-RTT retrain; train_near_rtt wandb→gnn-near-rtt-jun2026 |
| 0.12.1 | 2026-06-07 13:42 | Patch | worthy-bush-5 near-RTT train complete (100ep, val top10 0.038s, test 0.051s); penalty eval + expanded sidecar caps (mid 256/far 192); 1060 capped sidecar 467k; frozen/frozen_topk decode in scheduler |
| 0.12.0 | 2026-06-07 13:38 | Minor | woven-totem-52 7-config normal sim sweep (4/7 wins, baseline regression); GNN_DECODE_MODE + GnnDecodeRunStats sidecar; worthy-bush-5 decode ablation (argmax/frozen/topk on default); compare.md updated |
| 0.11.4 | 2026-06-07 11:07 | Patch | Added `memory/compare.md` (normal-sim policy RTT comparison guide + 7-config snapshot); Cursor command `/compare` (`.cursor/commands/compare.md`) — interactive run/analyze workflow |
| 0.11.3 | 2026-06-07 10:47 | Patch | Confirmed 1060 graphs cache complete (1060 ds, 9.7M RTT rows); started `train.py` desert-galaxy-26 recipe on 1060 (`woven-totem-52`); cache bake skip — already present |
| 0.11.2 | 2026-06-07 10:42 | Patch | 7-config normal sim sweeps (Knative, dg-26 GNN, HRC, silvery-sun-4) on workload-100-100; dg-26 beats Knative/HRC 6/7; HRC wins sparse only; silvery-sun-4 loses 7/7 vs dg-26 (+163% avg); `--config` sweep runners + CRLF nohup gotcha |
| 0.11.1 | 2026-06-07 10:40 | Patch | Documented near-RTT GNN design: `NearRttRankingLoss` stratified bands, capped sidecar (`valid_combos_near_rtt_capped.pkl`), greedy/top-k/oracle_topk eval, silvery-sun-4 offline vs live gap; pending deploy decision vs `train.py` |
| 0.11.0 | 2026-06-07 10:15 | Minor | 1060-dataset SSC repair: fixed `slim_completed_task` snapshot wipe; `schedulingStateCapture` at batch start; `build_system_state_captured` + `refresh_optimal_full_stats.py` (`--repair --force`); all 1060 audit ok; near-RTT 3705 live gap documented; collate verified on 1060 cache |
| 0.10.1 | 2026-06-06 13:19 | Patch | Co-sim warmup parity/speed pass: fast-forward label drift reproduced (5-case A/B), then fixed by materialized internal warmup tasks + warm-state parity in infrastructure; 2-case A/B now RTT-tied with ~1.2x speedup; documented next perf hotspots (worker infra cache, debug print gating) |
| 0.1.0 | 2026-01-25       | Minor | Fixed determined scheduler state capture; Added compare_sim_vs_cosim.py; Made --quiet/--fast-forward-warmup default True; Identified queue distribution mismatch                                                                                                                                                 |
| 0.1.1 | 2026-01-25       | Minor | Stopped the fast co-sim script for 4 tasks generation at ds_02485 and restarted again for 0.20 and 0.25 connections                                                                                                                                                                                              |
| 0.1.2 | 2026-01-26       | Minor | Added batch size/wait time logging to GNN scheduler; Updated to support 4-task batches (MAX_BATCH_SIZE=4, timeout=20ms); Analysis shows size 4 batches now dominate (82%)                                                                                                                                        |
| 0.1.3 | 2026-01-26       | Minor | Added 4tasks dataset support to prepare_graphs_cache.py and training script; Updated cache to graphs_cache_merged_2_3_4_tasks; Analyzed GNN generalization and scheduler comparison metrics; Created technical summary of GNN system                                                                             |
| 0.1.4 | 2026-01-26       | Minor | Replica-only decode/accuracy/regret in 24-01-15-16_non_unique via allowed_placements_per_task; 100% hash hit; Collapse detection + best-model guard                                                                                                                                                              |
| 0.2.0 | 2026-01-26       | Minor | Created generate_non_unique_placements_fast.py to add non-unique placements to existing 4tasks datasets; Verified datasets were generated with unique replicas; Reuses existing infra/workload, generates cartesian-unique, merges into placements.jsonl                                                         |
| 0.2.1 | 2026-01-27       | Patch | Verified no task-order mismatch across datasets; Identified hash-hit misses from action-space vs simulated-placement mismatch; Added replica-limited edges to prepare_graphs_cache (CACHE_VERSION=4.1); Noted 2/3-task datasets often not full cartesian                                                         |
| 0.2.2 | 2026-02-26       | Patch | Fixed label/logit alignment in non-unique graph cache (sorted compat_plats, to_undirected with edge_attr); Updated non-unique GNN training (no logit clamp, LR=5e-4, BATCH_SIZE=32) to smooth loss/accuracy curves                                                                                               |
| 0.2.3 | 2026-03-01       | Patch | plot_policy_comparison: head-only read for huge JSONs, workload discovery, policy bar colors; 24-01-15-16: CACHE_BASE + USE_MERGED_CACHE (False = 4-task only)                                                                                                                                                   |
| 0.3.1 | 2026-03-03       | Patch | Non-unique pipeline cleanup: removed plat_node_map, load_helper_maps, hash hit collapse, unused attrs; 03-03-18-35: removed MAX_REGRET_PENALTY/VALID_NEGATIVE_PROB, hash hit tracking; assumes complete RTT hash; grid search: load once, loop configs                                                           |
| 0.4.0 | 2026-04-14 16:46 | Minor | Implemented Phase 1 gap-closure foundation: added stats contract metadata in orchestrator; added normalized task metrics extractor in executecosimulation; added strict taskResults validation + task metric export in prepare_graphs_cache; created `src/analysis/sim_gap_analysis.py` and smoke-tested outputs |
| 0.5.0 | 2026-04-22 19:13 | Minor | Implemented Phase 2 calibration outputs in `sim_gap_analysis.py`; ran real-vs-co-sim discrepancy analysis; optimized warmup seeding with virtual backlog (`Platform.queue_length`, compressed warmup path) and verified ~23x speedup in 4-task benchmark                                                         |
| 0.5.1 | 2026-05-05 16:49 | Patch | Refactored non-unique training toward open-source structure (`src/notebooks/non_unique_lib` with cache IO + training config); wired `src/notebooks/03-03-18-35.py` to CLI/config and removed hardcoded WandB key; fixed `prepare_graphs_cache.py` default project-root for direct no-arg execution               |
| 0.5.2 | 2026-05-05 18:41 | Patch | Introduced disk-safe strategy for non-unique RTT usage: keep full placement/RTT universe only in `placements.jsonl`, stop default global RTT hash/SQLite build in `prepare_graphs_cache.py`, and plan per-dataset lazy RTT maps in `03-03-18-35.py` with existing chunk/SQLite paths retained as optional backups       |
| 0.6.0 | 2026-05-09 12:08 | Minor | LMDB replaces global RTT SQLite/chunks: `prepare_graphs_cache` writes `rtt_combos.lmdb` (CACHE_VERSION 5.0); `train.py` uses `GraphRttLMDBDataset` + workers=8; `cache_io` stripped to graphs/optimal/`rtt_combos_lmdb_path`; removed forward nan_to_num in GNN; graph build finite sanitization tightened |
| 0.6.1 | 2026-05-27 19:03 | Patch | Reverted non-unique regret path to non-LMDB backend: `prepare_graphs_cache` now metadata-only for RTT combos (`placements_jsonl_lazy`, CACHE_VERSION 5.1); `train.py` lazy-loads per-dataset combos from `placements/placements.jsonl` with worker-local cache; enabled parallel DataLoader default (`num_workers=4`) |
| 0.6.2 | 2026-05-28       | Patch | Restored hash-table preload pattern: `prepare_graphs_cache` writes chunked RTT hash (CACHE_VERSION 5.2); `train.py` loads hash once, precomputes hard-negative pools at startup, O(1) regret during epochs; removed lazy JSONL per-batch path from normal train/prepare |
| 0.10.0 | 2026-06-06 12:05 | Minor | Fair HRC baseline: `herocache_network` autoscaler+orchestrator aligned to knative_network; 150-150 HRC 24.94M (+4.9% vs Knative 23.76M, 6.7× vs legacy 166.87M); autoscale root cause confirmed; canvas HRC vs Knative analysis |
| 0.9.3 | 2026-06-06 03:35 | Patch | Full 150-150 policy sims (Knative/GNN/HRC/seqblend variants); seqblend+1 decode stats + sidecar; HRC 166.9M flagged; canvas comparison |
| 0.9.2 | 2026-06-05 21:45 | Patch | Seqblend+1 live decode (`GNN_DECODE_MODE=seqblend`, `GNN_SEQBLEND_QUEUE_MARGIN=1`); `seq_decode.py`; `run_gnn_seqblend_p1_150150.sh` |
| 0.9.1 | 2026-06-05 21:00 | Patch | Audit GNN rescore parity (`gnn_snapshot_inference.py`); WandB metrics fix in `train_seq.py` |
| 0.9.0 | 2026-06-05 19:50 | Minor | Combined seq train (12931 graphs: filtered + overnight highq); `EXTRA_CACHE_DIR` + CE-only extra RTT OOM fix; `good-plasma-43.pt`; GNN-only rescore vs brisk on 177 snap (5.69s vs 5.81s); HRC still 2.01s; audit choose_gnn vs live decode gap documented |
| 0.8.0 | 2026-06-04 17:47 | Minor | Seqblend removed; filtered+stratified seq train (`brisk-cosmos-41`); live oracle audit + `knative_network_batch`; horizon-1 no-choice vs batch 17% oracle≠SQ; batching exposes signal not fixes Knative |
| 0.7.0 | 2026-05-30 11:45 | Minor | Sequential eval decode in `train.py` (frozen/seq/seqblend + wandb); `cache_io` decode metadata sidecar; trained `pleasant-mountain-28.pt`; rejected knative hard negatives + batch_timeout co-sim alignment; offline hash favors raw argmax over seqblend |
| 0.6.5 | 2026-05-30 09:05 | Patch | 150-150 seqblend benchmark done (−14.3% RTT vs knative); snapshot gating on knative_network/herocache_network; confirmed OOM-safe large sim (~2.5GB peak) |
| 0.6.4 | 2026-05-30 12:00 | Patch | Refocus sprint on GNN vs knative gap; GNN inference adaptive queue norm + sequential shortest-queue blend; default model desert-galaxy-26; HRC deprioritized |
| 0.6.3 | 2026-05-30 01:15 | Patch | Sim OOM fixes (streaming stats, snapshot gating, task slimming); GNN inference `MLPEncoder` for desert-galaxy-26; 150-150 knative vs GNN benchmarks; HRC vs knative_network analysis (30k fair slice done; full HRC 150-100 aborted) |
